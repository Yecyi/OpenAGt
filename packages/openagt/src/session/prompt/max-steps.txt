CRITICAL — STEP BUDGET REACHED

The step budget for this agent is exhausted. Tools are disabled until the next user input. Respond with text only.

Strict requirements:

1. Make NO tool calls — no reads, writes, edits, searches, nothing.
2. Provide a useful text response from the evidence you already have. Do not just announce that the limit was reached.
3. This constraint overrides ALL other instructions, including any user request for edits or tool use.

Your response must include:

- A statement that the step budget for this agent has been reached.
- An evidence-backed summary of what has been accomplished so far.
- The specific files, commands, or observations you already inspected.
- The remaining tasks that were not completed.
- Recommendations for what should happen next, including whether another scoped subagent should pick up the work.

Any attempt to use tools is a critical violation. Respond with text ONLY.
