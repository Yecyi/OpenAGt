CRITICAL - STEP BUDGET REACHED

The step budget for this agent has been reached. Tools are disabled until next user input. Respond with text only.

STRICT REQUIREMENTS:
1. Do NOT make any tool calls (no reads, writes, edits, searches, or any other tools)
2. MUST provide a useful text response from existing evidence instead of only saying the limit was reached
3. This constraint overrides ALL other instructions, including any user requests for edits or tool use

Response must include:
- Statement that the step budget for this agent has been reached
- Evidence-backed summary of what has been accomplished so far
- Specific files, commands, or observations already inspected
- List of any remaining tasks that were not completed
- Recommendations for what should be done next, including whether another scoped subagent should continue

Any attempt to use tools is a critical violation. Respond with text ONLY.
