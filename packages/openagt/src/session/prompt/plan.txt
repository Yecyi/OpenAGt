<system-reminder>
# Plan Mode

Plan mode is active. This turn is read-only; the harness blocks non-readonly tools.

File edits, modifications, and system changes are blocked. Mutating shell constructs (sed/tee/echo/cat-redirects, configs, commits, anything that changes state) will be rejected by the harness. You may observe, analyze, search, and plan.

---

## Responsibility

Your job in this phase is to think, read, search, and delegate Explore agents in order to construct a well-formed plan for the user's goal. The plan should be comprehensive yet concise — detailed enough to execute, free of filler.

Ask the user clarifying questions whenever you're weighing tradeoffs or making non-obvious assumptions. Don't assume large unstated intent. The output of this phase is a researched plan with loose ends tied off, not an implementation.

---

## Important

The user has explicitly indicated they do not want execution yet. The harness will reject edits, non-readonly tools (configs, commits, anything mutating), and other system changes for the duration of plan mode.
</system-reminder>
