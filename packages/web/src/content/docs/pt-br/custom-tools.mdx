---
title: Ferramentas Personalizadas
description: Crie ferramentas que o LLM pode chamar no opencode.
---

Ferramentas personalizadas s茫o fun莽玫es que voc锚 cria e que o LLM pode chamar durante as conversas. Elas funcionam junto com as [ferramentas integradas](/docs/tools) do opencode, como `read`, `write` e `bash`.

---

## Criando uma ferramenta

As ferramentas s茫o definidas como arquivos **TypeScript** ou **JavaScript**. No entanto, a defini莽茫o da ferramenta pode invocar scripts escritos em **qualquer linguagem** 鈥?TypeScript ou JavaScript 茅 usado apenas para a defini莽茫o da ferramenta em si.

---

### Localiza莽茫o

Elas podem ser definidas:

- Localmente, colocando-as no diret贸rio `.opencode/tools/` do seu projeto.
- Ou globalmente, colocando-as em `~/.config/opencode/tools/`.

---

### Estrutura

A maneira mais f谩cil de criar ferramentas 茅 usando o helper `tool()`, que fornece seguran莽a de tipo e valida莽茫o.

```ts title=".opencode/tools/database.ts" {1}
import { tool } from "@openagt/plugin"

export default tool({
  description: "Consultar o banco de dados do projeto",
  args: {
    query: tool.schema.string().describe("Consulta SQL para executar"),
  },
  async execute(args) {
    // Sua l贸gica de banco de dados aqui
    return `Consulta executada: ${args.query}`
  },
})
```

O **nome do arquivo** se torna o **nome da ferramenta**. O acima cria uma ferramenta `database`.

---

#### M煤ltiplas ferramentas por arquivo

Voc锚 tamb茅m pode exportar v谩rias ferramentas de um 煤nico arquivo. Cada exporta莽茫o se torna **uma ferramenta separada** com o nome **`<filename>_<exportname>`**:

```ts title=".opencode/tools/math.ts"
import { tool } from "@openagt/plugin"

export const add = tool({
  description: "Adicionar dois n煤meros",
  args: {
    a: tool.schema.number().describe("Primeiro n煤mero"),
    b: tool.schema.number().describe("Segundo n煤mero"),
  },
  async execute(args) {
    return args.a + args.b
  },
})

export const multiply = tool({
  description: "Multiplicar dois n煤meros",
  args: {
    a: tool.schema.number().describe("Primeiro n煤mero"),
    b: tool.schema.number().describe("Segundo n煤mero"),
  },
  async execute(args) {
    return args.a * args.b
  },
})
```

Isso cria duas ferramentas: `math_add` e `math_multiply`.

---

#### Colis玫es de nome com ferramentas integradas

Ferramentas personalizadas s茫o chaveadas pelo nome da ferramenta. Se uma ferramenta personalizada usar o mesmo nome que uma ferramenta integrada, a ferramenta personalizada tem preced锚ncia.

Por exemplo, este arquivo substitui a ferramenta `bash` integrada:

```ts title=".opencode/tools/bash.ts"
import { tool } from "@openagt/plugin"

export default tool({
  description: "Restricted bash wrapper",
  args: {
    command: tool.schema.string(),
  },
  async execute(args) {
    return `blocked: ${args.command}`
  },
})
```

:::note
Prefira nomes 煤nicos, a menos que voc锚 queira intencionalmente substituir uma ferramenta integrada. Se voc锚 quiser desabilitar uma ferramenta integrada, mas n茫o substitu铆-la, use [permiss玫es](/docs/permissions).
:::

---

### Argumentos

Voc锚 pode usar `tool.schema`, que 茅 apenas [Zod](https://zod.dev), para definir tipos de argumentos.

```ts "tool.schema"
args: {
  query: tool.schema.string().describe("Consulta SQL para executar")
}
```

Voc锚 tamb茅m pode importar [Zod](https://zod.dev) diretamente e retornar um objeto simples:

```ts {6}
import { z } from "zod"

export default {
  description: "Descri莽茫o da ferramenta",
  args: {
    param: z.string().describe("Descri莽茫o do par芒metro"),
  },
  async execute(args, context) {
    // Implementa莽茫o da ferramenta
    return "result"
  },
}
```

---

### Contexto

As ferramentas recebem contexto sobre a sess茫o atual:

```ts title=".opencode/tools/project.ts" {8}
import { tool } from "@openagt/plugin"

export default tool({
  description: "Obter informa莽玫es do projeto",
  args: {},
  async execute(args, context) {
    // Acessar informa莽玫es de contexto
    const { agent, sessionID, messageID, directory, worktree } = context
    return `Agent: ${agent}, Session: ${sessionID}, Message: ${messageID}, Directory: ${directory}, Worktree: ${worktree}`
  },
})
```

Use `context.directory` para o diret贸rio de trabalho da sess茫o.
Use `context.worktree` para a raiz do worktree do git.

---

## Exemplos

### Escrevendo uma ferramenta em Python

Voc锚 pode escrever suas ferramentas em qualquer linguagem que desejar. Aqui est谩 um exemplo que adiciona dois n煤meros usando Python.

Primeiro, crie a ferramenta como um script Python:

```python title=".opencode/tools/add.py"
import sys

a = int(sys.argv[1])
b = int(sys.argv[2])
print(a + b)
```

Em seguida, crie a defini莽茫o da ferramenta que a invoca:

```ts title=".opencode/tools/python-add.ts" {10}
import { tool } from "@openagt/plugin"
import path from "path"

export default tool({
  description: "Adicionar dois n煤meros usando Python",
  args: {
    a: tool.schema.number().describe("Primeiro n煤mero"),
    b: tool.schema.number().describe("Segundo n煤mero"),
  },
  async execute(args, context) {
    const script = path.join(context.worktree, ".opencode/tools/add.py")
    const result = await Bun.$`python3 ${script} ${args.a} ${args.b}`.text()
    return result.trim()
  },
})
```

Aqui estamos usando o utilit谩rio [`Bun.$`](https://bun.com/docs/runtime/shell) para executar o script Python.
