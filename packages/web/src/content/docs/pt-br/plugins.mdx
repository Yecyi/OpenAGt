---
title: Plugins
description: Escreva seus pr贸prios plugins para estender o opencode.
---

Plugins permitem que voc锚 estenda o opencode conectando-se a v谩rios eventos e personalizando o comportamento. Voc锚 pode criar plugins para adicionar novos recursos, integrar-se a servi莽os externos ou modificar o comportamento padr茫o do opencode.

Para exemplos, confira os [plugins](/docs/ecosystem#plugins) criados pela comunidade.

---

## Usando um plugin

Existem duas maneiras de carregar plugins.

---

### De arquivos locais

Coloque arquivos JavaScript ou TypeScript no diret贸rio de plugins.

- `.opencode/plugins/` - Plugins em n铆vel de projeto
- `~/.config/opencode/plugins/` - Plugins globais

Os arquivos nesses diret贸rios s茫o carregados automaticamente na inicializa莽茫o.

---

### Do npm

Especifique pacotes npm no seu arquivo de configura莽茫o.

```json title="opencode.json"
{
  "$schema": "https://github.com/Yecyi/OpenAGt/raw/dev/packages/web/dist/config.json",
  "plugin": ["opencode-helicone-session", "opencode-wakatime", "@my-org/custom-plugin"]
}
```

Pacotes npm regulares e escopados s茫o suportados.

Navegue pelos plugins dispon铆veis no [ecossistema](/docs/ecosystem#plugins).

---

### Como os plugins s茫o instalados

**Plugins npm** s茫o instalados automaticamente usando Bun na inicializa莽茫o. Pacotes e suas depend锚ncias s茫o armazenados em cache em `~/.cache/opencode/node_modules/`.

**Plugins locais** s茫o carregados diretamente do diret贸rio de plugins. Para usar pacotes externos, voc锚 deve criar um `package.json` dentro do seu diret贸rio de configura莽茫o (veja [Depend锚ncias](#dependencies)), ou publicar o plugin no npm e [adicion谩-lo 脿 sua configura莽茫o](/docs/config#plugins).

---

### Ordem de carregamento

Os plugins s茫o carregados de todas as fontes e todos os hooks s茫o executados em sequ锚ncia. A ordem de carregamento 茅:

1. Configura莽茫o global (`~/.config/opencode/opencode.json`)
2. Configura莽茫o do projeto (`opencode.json`)
3. Diret贸rio de plugins global (`~/.config/opencode/plugins/`)
4. Diret贸rio de plugins do projeto (`.opencode/plugins/`)

Pacotes npm duplicados com o mesmo nome e vers茫o s茫o carregados uma vez. No entanto, um plugin local e um plugin npm com nomes semelhantes s茫o carregados separadamente.

---

## Criando um plugin

Um plugin 茅 um **m贸dulo JavaScript/TypeScript** que exporta uma ou mais fun莽玫es de plugin. Cada fun莽茫o recebe um objeto de contexto e retorna um objeto de hooks.

---

### Depend锚ncias

Plugins locais e ferramentas personalizadas podem usar pacotes npm externos. Adicione um `package.json` ao seu diret贸rio de configura莽茫o com as depend锚ncias necess谩rias.

```json title=".opencode/package.json"
{
  "dependencies": {
    "shescape": "^2.1.0"
  }
}
```

O opencode executa `bun install` na inicializa莽茫o para instalar esses pacotes. Seus plugins e ferramentas podem ent茫o import谩-los.

```ts title=".opencode/plugins/my-plugin.ts"
import { escape } from "shescape"

export const MyPlugin = async (ctx) => {
  return {
    "tool.execute.before": async (input, output) => {
      if (input.tool === "bash") {
        output.args.command = escape(output.args.command)
      }
    },
  }
}
```

---

### Estrutura b谩sica

```js title=".opencode/plugins/example.js"
export const MyPlugin = async ({ project, client, $, directory, worktree }) => {
  console.log("Plugin initialized!")

  return {
    // Hook implementations go here
  }
}
```

A fun莽茫o do plugin recebe:

- `project`: As informa莽玫es do projeto atual.
- `directory`: O diret贸rio de trabalho atual.
- `worktree`: O caminho do worktree do git.
- `client`: Um cliente SDK do opencode para interagir com a AI.
- `$`: A [API shell](https://bun.com/docs/runtime/shell) do Bun para executar comandos.

---

### Suporte a TypeScript

Para plugins TypeScript, voc锚 pode importar tipos do pacote de plugin:

```ts title="my-plugin.ts" {1}
import type { Plugin } from "@openagt/plugin"

export const MyPlugin: Plugin = async ({ project, client, $, directory, worktree }) => {
  return {
    // Type-safe hook implementations
  }
}
```

---

### Eventos

Plugins podem se inscrever em eventos como visto abaixo na se莽茫o Exemplos. Aqui est谩 uma lista dos diferentes eventos dispon铆veis.

#### Eventos de Comando

- `command.executed`

#### Eventos de Arquivo

- `file.edited`
- `file.watcher.updated`

#### Eventos de Instala莽茫o

- `installation.updated`

#### Eventos LSP

- `lsp.client.diagnostics`
- `lsp.updated`

#### Eventos de Mensagem

- `message.part.removed`
- `message.part.updated`
- `message.removed`
- `message.updated`

#### Eventos de Permiss茫o

- `permission.asked`
- `permission.replied`

#### Eventos de Servidor

- `server.connected`

#### Eventos de Sess茫o

- `session.created`
- `session.compacted`
- `session.deleted`
- `session.diff`
- `session.error`
- `session.idle`
- `session.status`
- `session.updated`

#### Eventos de Todo

- `todo.updated`

#### Eventos de Shell

- `shell.env`

#### Eventos de Ferramenta

- `tool.execute.after`
- `tool.execute.before`

#### Eventos TUI

- `tui.prompt.append`
- `tui.command.execute`
- `tui.toast.show`

---

## Exemplos

Aqui est茫o alguns exemplos de plugins que voc锚 pode usar para estender o opencode.

---

### Enviando notifica莽玫es

Envie notifica莽玫es quando certos eventos ocorrerem:

```js title=".opencode/plugins/notification.js"
export const NotificationPlugin = async ({ project, client, $, directory, worktree }) => {
  return {
    event: async ({ event }) => {
      // Send notification on session completion
      if (event.type === "session.idle") {
        await $`osascript -e 'display notification "Session completed!" with title "opencode"'`
      }
    },
  }
}
```

Estamos usando `osascript` para executar AppleScript no macOS. Aqui estamos usando para enviar notifica莽玫es.

:::note
Se voc锚 estiver usando o aplicativo desktop opencode, ele pode enviar notifica莽玫es do sistema automaticamente quando uma resposta estiver pronta ou quando ocorrer um erro na sess茫o.
:::

---

### Prote莽茫o .env

Impe莽a o opencode de ler arquivos `.env`:

```javascript title=".opencode/plugins/env-protection.js"
export const EnvProtection = async ({ project, client, $, directory, worktree }) => {
  return {
    "tool.execute.before": async (input, output) => {
      if (input.tool === "read" && output.args.filePath.includes(".env")) {
        throw new Error("Do not read .env files")
      }
    },
  }
}
```

---

### Injetando vari谩veis de ambiente

Injete vari谩veis de ambiente em todas as execu莽玫es de shell (ferramentas de AI e terminais de usu谩rio):

```javascript title=".opencode/plugins/inject-env.js"
export const InjectEnvPlugin = async () => {
  return {
    "shell.env": async (input, output) => {
      output.env.MY_API_KEY = "secret"
      output.env.PROJECT_ROOT = input.cwd
    },
  }
}
```

---

### Ferramentas personalizadas

Plugins tamb茅m podem adicionar ferramentas personalizadas ao opencode:

```ts title=".opencode/plugins/custom-tools.ts"
import { type Plugin, tool } from "@openagt/plugin"

export const CustomToolsPlugin: Plugin = async (ctx) => {
  return {
    tool: {
      mytool: tool({
        description: "This is a custom tool",
        args: {
          foo: tool.schema.string(),
        },
        async execute(args, context) {
          const { directory, worktree } = context
          return `Hello ${args.foo} from ${directory} (worktree: ${worktree})`
        },
      }),
    },
  }
}
```

O helper `tool` cria uma ferramenta personalizada que o opencode pode chamar. Ele aceita uma fun莽茫o de esquema Zod e retorna uma defini莽茫o de ferramenta com:

- `description`: O que a ferramenta faz
- `args`: Esquema Zod para os argumentos da ferramenta
- `execute`: Fun莽茫o que 茅 executada quando a ferramenta 茅 chamada

Suas ferramentas personalizadas estar茫o dispon铆veis para o opencode junto com as ferramentas integradas.

:::note
Se uma ferramenta de plugin usar o mesmo nome que uma ferramenta integrada, a ferramenta de plugin tem preced锚ncia.
:::

---

### Registro

Use `client.app.log()` em vez de `console.log` para registro estruturado:

```ts title=".opencode/plugins/my-plugin.ts"
export const MyPlugin = async ({ client }) => {
  await client.app.log({
    body: {
      service: "my-plugin",
      level: "info",
      message: "Plugin initialized",
      extra: { foo: "bar" },
    },
  })
}
```

N铆veis: `debug`, `info`, `warn`, `error`. Veja a [documenta莽茫o do SDK](https://github.com/Yecyi/OpenAGt/docs/sdk) para detalhes.

---

### Hooks de compacta莽茫o

Personalize o contexto inclu铆do quando uma sess茫o 茅 compactada:

```ts title=".opencode/plugins/compaction.ts"
import type { Plugin } from "@openagt/plugin"

export const CompactionPlugin: Plugin = async (ctx) => {
  return {
    "experimental.session.compacting": async (input, output) => {
      // Inject additional context into the compaction prompt
      output.context.push(`
## Custom Context

Include any state that should persist across compaction:
- Current task status
- Important decisions made
- Files being actively worked on
`)
    },
  }
}
```

O hook `experimental.session.compacting` 茅 acionado antes que o LLM gere um resumo de continua莽茫o. Use-o para injetar contexto espec铆fico de dom铆nio que o prompt de compacta莽茫o padr茫o poderia perder.

Voc锚 tamb茅m pode substituir o prompt de compacta莽茫o completamente definindo `output.prompt`:

```ts title=".opencode/plugins/custom-compaction.ts"
import type { Plugin } from "@openagt/plugin"

export const CustomCompactionPlugin: Plugin = async (ctx) => {
  return {
    "experimental.session.compacting": async (input, output) => {
      // Replace the entire compaction prompt
      output.prompt = `
You are generating a continuation prompt for a multi-agent swarm session.

Summarize:
1. The current task and its status
2. Which files are being modified and by whom
3. Any blockers or dependencies between agents
4. The next steps to complete the work

Format as a structured prompt that a new agent can use to resume work.
`
    },
  }
}
```

Quando `output.prompt` 茅 definido, ele substitui completamente o prompt de compacta莽茫o padr茫o. O array `output.context` 茅 ignorado neste caso.
