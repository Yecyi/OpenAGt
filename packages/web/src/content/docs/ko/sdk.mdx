---
title: SDK
description: opencode 靹滊矂毳?鞙勴暅 韮€鞛?鞎堨爠頃?JS 韥措澕鞚挫柛韸?
---

import config from "../../../../config.mjs"
export const typesUrl = `${config.github}/blob/dev/packages/sdk/js/src/gen/types.gen.ts`

opencode JS/TS SDK電?靹滊矂鞕€ 靸來樃 鞛戩毄鞚?鞙勴暅 鞙犿槙 鞎堨爠頃?韥措澕鞚挫柛韸鸽ゼ 鞝滉车頃╇媹雼?
韱淀暕 氚?鞝滌柎 opencode programmatically毳?甑稌頃橂姅 雿?靷毄霅╇媹雼?

[Learn more](/docs/server) 靹滊矂臧€ 鞏措柣瓴?鞛戨彊頃橂倶鞖? 鞓堧ゼ 霌れ柎, 旎る雼堩嫲鞐?鞚橅暣 甑稌 霅?[projects](/docs/ecosystem#projects)毳?頇曥澑頃橃嫮鞁滌槫.

---

## 靹れ箻

npm鞐愳劀 SDK 靹れ箻:

```bash
npm install @openagt/sdk
```

---

## 韥措澕鞚挫柛韸?毵岆摛旮?

opencode鞚?鞚胳姢韯挫姢 毵岆摛旮?

```javascript
import { createOpencode } from "@openagt/sdk"

const { client } = await createOpencode()
```

靹滊矂鞕€ 韥措澕鞚挫柛韸?氇憪 鞁滌瀾

#### 鞓奠厴

| 鞓奠厴       | 鞙犿槙          | 靹る獏                    | 旮半掣        |
| ---------- | ------------- | ----------------------- | ----------- |
| `hostname` | `string`      | 靹滊矂 順胳姢韸鸽獏           | `127.0.0.1` |
| `port`     | `number`      | 靹滊矂韽姼                | `4096`      |
| `signal`   | `AbortSignal` | 旆唽 鞁犿樃               | `undefined` |
| `timeout`  | `number`      | 靹滊矂 鞁滌瀾鞁滌潣 鞁滉皠 鞎勳泝 | `5000`      |
| `config`   | `Config`      | 甑劚 臧濎泊               | `{}`        |

---

## 甑劚

甑劚 臧濎泊毳?鞝勲嫭頃?靾?鞛堨姷雼堧嫟. 鞚胳姢韯挫姢電?鞐爠頌?`opencode.json`毳?靹犿儩頃橃毵?甑劚 鞚鸽澕鞚胳潉 於旉皜 頃?靾?鞛堨姷雼堧嫟.

```javascript
import { createOpencode } from "@openagt/sdk"

const opencode = await createOpencode({
  hostname: "127.0.0.1",
  port: 4096,
  config: {
    model: "anthropic/claude-3-5-sonnet-20241022",
  },
})

console.log(`Server running at ${opencode.server.url}`)

opencode.server.close()
```

## 韥措澕鞚挫柛韸鸽

opencode鞚?鞁ろ枆 鞚胳姢韯挫姢臧€ 鞚措 鞛堧嫟氅?韥措澕鞚挫柛韸?鞚胳姢韯挫姢毳?毵岆摛 靾?鞛堨姷雼堧嫟.

```javascript
import { createOpencodeClient } from "@openagt/sdk"

const client = createOpencodeClient({
  baseUrl: "http://localhost:4096",
})
```

#### 鞓奠厴

| 鞓奠厴            | 鞙犿槙       | 靹る獏                              | 旮半掣                    |
| --------------- | ---------- | --------------------------------- | ----------------------- |
| `baseUrl`       | `string`   | 靹滊矂鞚?URL                        | `http://localhost:4096` |
| `fetch`         | `function` | 靷毄鞛?鞝曥潣 fetch 甑槃            | `globalThis.fetch`      |
| `parseAs`       | `string`   | 鞚戨嫷 韺岇嫳 氚╇矔                    | `auto`                  |
| `responseStyle` | `string`   | 氚橅拡 鞀ろ儉鞚? `data` 霕愲姅 `fields` | `fields`                |
| `throwOnError`  | `boolean`  | 氚橅拡 鞁?鞓る                      | `false`                 |

---

## 韮€鞛?

SDK鞐愲姅 氇摖 API 鞙犿槙鞚?TypeScript 鞝曥潣臧€ 韽暔霅橃柎 鞛堨姷雼堧嫟. 歆侅爲 臧€鞝?鞓り赴 :

```typescript
import type { Session, Message, Part } from "@openagt/sdk"
```

氇摖 鞙犿槙鞚€ 靹滊矂鞚?OpenAPI 靷枒鞐愳劀 靸濎劚霅橂┌ <a href={typesUrl}>types 韺岇澕</a>鞐愳劀 靷毄頃?靾?鞛堨姷雼堧嫟.

---

## 鞓る

SDK電?鞛§潉 靾?鞛堧姅 鞓る毳?雿橃 靾?鞛堨姷雼堧嫟:

```typescript
try {
  await client.session.get({ path: { id: "invalid-id" } })
} catch (error) {
  console.error("Failed to get session:", (error as Error).message)
}
```

---

## 甑“頇旊悳 於滊牓

JSON 鞀ろ偆毵堨檧 頃粯 `format`鞚?歆€鞝曧晿鞐?氇嵏鞐愳劀 甑“頇旊悳 JSON 於滊牓鞚?鞖旍箔頃?靾?鞛堨姷雼堧嫟. 氇嵏鞚€ `StructuredOutput` 霃勱惮毳?靷毄頃橃棳 鞀ろ偆毵堨檧 鞚检箻頃橂姅 瓴€歃濍悳 JSON鞚?氚橅櫂頃╇媹雼?

### 旮半掣 靷毄氩?

```typescript
const result = await client.session.prompt({
  path: { id: sessionId },
  body: {
    parts: [{ type: "text", text: "Research Anthropic and provide company info" }],
    format: {
      type: "json_schema",
      schema: {
        type: "object",
        properties: {
          company: { type: "string", description: "Company name" },
          founded: { type: "number", description: "Year founded" },
          products: {
            type: "array",
            items: { type: "string" },
            description: "Main products",
          },
        },
        required: ["company", "founded"],
      },
    },
  },
})

// Access the structured output
console.log(result.data.info.structured_output)
// { company: "Anthropic", founded: 2021, products: ["Claude", "Claude API"] }
```

### 於滊牓 順曥嫕 鞙犿槙

| 鞙犿槙          | 靹る獏                                              |
| ------------- | ------------------------------------------------- |
| `text`        | 旮半掣臧? 響滌 韰嶌姢韸?鞚戨嫷 (甑“頇旊悳 於滊牓 鞐嗢潓)     |
| `json_schema` | 鞝滉车霅?鞀ろ偆毵堨檧 鞚检箻頃橂姅 瓴€歃濍悳 JSON鞚?氚橅櫂頃╇媹雼?|

### JSON 鞀ろ偆毵?順曥嫕

`type: 'json_schema'`毳?靷毄頃?霑?雼れ潓鞚?鞝滉车頃橃嫮鞁滌槫:

| 頃勲摐         | 鞙犿槙            | 靹る獏                                        |
| ------------ | --------------- | ------------------------------------------- |
| `type`       | `'json_schema'` | 頃勳垬. JSON 鞀ろ偆毵?氇摐毳?歆€鞝曧暕雼堧嫟         |
| `schema`     | `object`        | 頃勳垬. 於滊牓 甑“毳?鞝曥潣頃橂姅 JSON 鞀ろ偆毵?臧濎泊 |
| `retryCount` | `number`        | 靹犿儩 靷暛. 瓴€歃?鞛嫓霃?須熿垬 (旮半掣臧? 2)     |

### 鞓る 觳橂Μ

氇嵏鞚?氇摖 鞛嫓霃?頉勳棎霃?鞙犿毃頃?甑“頇旊悳 於滊牓鞚?靸濎劚頃橃 氇豁晿氅?鞚戨嫷鞐?`StructuredOutputError`臧€ 韽暔霅╇媹雼?

```typescript
if (result.data.info.error?.name === "StructuredOutputError") {
  console.error("Failed to produce structured output:", result.data.info.error.message)
  console.error("Attempts:", result.data.info.error.retries)
}
```

### 氇矓 靷

1. **氇呿檿頃?靹る獏 鞝滉车**: 氇嵏鞚?於旍稖頃?雿办澊韯半ゼ 鞚错暣頃橂姅 雿?霃勳泙鞚?霅橂弰搿?鞀ろ偆毵?靻嶌劚鞐?氇呿檿頃?靹る獏鞚?鞝滉车頃橃嫮鞁滌槫.
2. **`required` 靷毄**: 頃勳垬 頃勲摐毳?歆€鞝曧晿霠る┐ `required`毳?靷毄頃橃嫮鞁滌槫.
3. **鞀ろ偆毵堧ゼ 歆戩鞝侅溂搿?鞙犾**: 氤奠灐頃?欷戩博 鞀ろ偆毵堧姅 氇嵏鞚?鞓皵毳搓矊 毂勳毎旮?雿?鞏措牑鞖?靾?鞛堨姷雼堧嫟.
4. **鞝侅爤頃?`retryCount` 靹れ爼**: 氤奠灐頃?鞀ろ偆毵堨潣 瓴届毎 電橂Μ瓿?雼垳頃?鞀ろ偆毵堨潣 瓴届毎 欷勳澊鞁嫓鞓?

---

## API

SDK電?type-safe 韥措澕鞚挫柛韸鸽ゼ 韱淀暣 氇摖 靹滊矂 API毳?雲胳稖頃╇媹雼?

---

### 旮€搿滊矊

| 氅旍劀霌?           | 靹る獏                   | 鞚戨嫷                                 |
| ----------------- | ---------------------- | ------------------------------------ |
| `global.health()` | 靹滊矂 瓯搓皶 氚?氩勳爠 頇曥澑 | `{ healthy: true, version: string }` |

---

#### 鞓堨牅

```javascript
const health = await client.global.health()
console.log(health.data.version)
```

---

### 鞎?

| 氚╇矔           | 靹る獏                      | 鞚戨嫷                                        |
| -------------- | ------------------------- | ------------------------------------------- |
| `app.log()`    | 搿滉犯 頃 鞛戩劚            | `boolean`                                   |
| `app.agents()` | 鞚挫毄 臧€電ロ暅 氇摖 鞐愳澊鞝勴姼 | <a href={typesUrl}><code>Agent[]</code></a> |

---

#### 鞓堨牅

```javascript
// Write a log entry
await client.app.log({
  body: {
    service: "my-app",
    level: "info",
    message: "Operation completed",
  },
})

// List available agents
const agents = await client.app.agents()
```

---

### 頂勲鞝濏姼

| 氚╇矔                | 靹る獏                    | 鞚戨嫷                                          |
| ------------------- | ----------------------- | --------------------------------------------- |
| `project.list()`    | 氇摖 頂勲鞝濏姼 氤搓赴      | <a href={typesUrl}><code>Project[]</code></a> |
| `project.current()` | 順勳灛 頂勲鞝濏姼 臧€鞝?鞓り赴 | <a href={typesUrl}><code>Project</code></a>   |

---

#### 鞓堨牅

```javascript
// List all projects
const projects = await client.project.list()

// Get current project
const currentProject = await client.project.current()
```

---

### 瓴诫

| 氚╇矔         | 靹る獏                | 鞚戨嫷                                     |
| ------------ | ------------------- | ---------------------------------------- |
| `path.get()` | 順勳灛 瓴诫 臧€鞝?鞓り赴 | <a href={typesUrl}><code>Path</code></a> |

---

#### 鞓堨牅

```javascript
// Get current path information
const pathInfo = await client.path.get()
```

---

### 甑劚

| 氚╇矔                 | 靹る獏                       | 鞚戨嫷                                                                                                  |
| -------------------- | -------------------------- | ----------------------------------------------------------------------------------------------------- |
| `config.get()`       | 甑劚 鞝曤炒                  | <a href={typesUrl}><code>Config</code></a>                                                            |
| `config.providers()` | 氇╇ 鞝滉车鞐呾泊 氚?旮半掣 氇嵏 | `{ providers: `<a href={typesUrl}><code>Provider[]</code></a>`, default: { [key: string]: string } }` |

---

#### 鞓堨牅

```javascript
const config = await client.config.get()

const { providers, default: defaults } = await client.config.providers()
```

---

### 靹胳厴

| 氅旍劀霌?                                                    | 靹る獏                          | 牍勱碃                                                                                                                                                                                                                               |
| ---------------------------------------------------------- | ----------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `session.list()`                                           | 靹胳厴 鞚茧瀸                     | <a href={typesUrl}><code>Session[]</code></a>                                                                                                                                                                                      |
| `session.get({ path })`                                    | 靹胳厴 臧€鞝?鞓り赴                | <a href={typesUrl}><code>Session</code></a>                                                                                                                                                                                        |
| `session.children({ path })`                               | 頃橃渼 靹胳厴 氇╇                | 氚橅拡 <a href={typesUrl}><code>Session</code></a>                                                                                                                                                                                   |
| `session.create({ body })`                                 | 靹胳厴 毵岆摛旮?                  | 毽劥 <a href={typesUrl}><code>Session</code></a>                                                                                                                                                                                   |
| `session.delete({ path })`                                 | 靹胳厴 靷牅                     | `boolean` 氚橅拡                                                                                                                                                                                                                     |
| `session.update({ path, body })`                           | 鞐呺嵃鞚错姼 靹胳厴 靻嶌劚            | 氚橅拡 <a href={typesUrl}><code>Session</code></a>                                                                                                                                                                                   |
| `session.init({ path, body })`                             | 鞎?齑堦赴頇?氚?`AGENTS.md` 攵勳劃 | `boolean`毳?氚橅櫂                                                                                                                                                                                                                   |
| `session.abort({ path })`                                  | 鞖挫榿 欷戩澑 靹胳厴                | 氚橅拡 `boolean`                                                                                                                                                                                                                     |
| `session.share({ path })`                                  | 瓿奠湢 靹胳厴                     | 氚橅拡 <a href={typesUrl}><code>Session</code></a>                                                                                                                                                                                   |
| `session.unshare({ path })`                                | 瓿奠湢 靹胳厴 旆唽                | 氚橅拡 <a href={typesUrl}><code>Session</code></a>                                                                                                                                                                                   |
| `session.summarize({ path, body })`                        | 靹胳厴 鞖旍暯                     | 氚橅拡 `boolean`                                                                                                                                                                                                                     |
| `session.messages({ path })`                               | 靹胳厴鞚?氅旍嫓歆€ 氇╇            | `{ info: `<a href={typesUrl}><code>Message</code></a>`, parts: `<a href={typesUrl}><code>Part</code></a>`}[]`                                                                                                                      |
| `session.message({ path })`                                | 氅旍嫓歆€ 靸侅劯鞝曤炒               | 氚橅拡 `{ info: `<a href={typesUrl}><code>Message</code></a>`, parts: `<a href={typesUrl}><code>Part[]</code></a>`}`                                                                                                                 |
| `session.prompt({ path, body })`                           | prompt 氅旍嫓歆€ 氤措偞旮?         | `body.noReply: true`電?UserMessage(旎厤鞀ろ姼 鞝勳毄)毳?氚橅櫂頃╇媹雼? 旮半掣臧掛潃 AI 鞚戨嫷瓿?頃粯 <a href={typesUrl}><code>AssistantMessage</code></a>毳?氚橅櫂頃╇媹雼? [甑“頇旊悳 於滊牓](#甑“頇旊悳-於滊牓)鞚?鞙勴暅 `body.outputFormat`鞚?歆€鞗愴暕雼堧嫟 |
| `session.command({ path, body })`                          | 靹胳厴鞙茧 氇呺牴鞚?鞝勳啞          | `{ info: `<a href={typesUrl}><code>AssistantMessage</code></a>`, parts: `<a href={typesUrl}><code>Part[]</code></a>`}`                                                                                                             |
| `session.shell({ path, body })`                            | shell 氇呺牴鞚?鞁ろ枆             | <a href={typesUrl}><code>AssistantMessage</code></a>                                                                                                                                                                               |
| `session.revert({ path, body })`                           | 氅旍嫓歆€ 雼れ嫓 氤€頇?             | <a href={typesUrl}><code>Session</code></a>                                                                                                                                                                                        |
| `session.unrevert({ path })`                               | 氅旍嫓歆€ 霅橂弻毽赴 旆唽          | 氚橅拡 <a href={typesUrl}><code>Session</code></a>                                                                                                                                                                                   |
| `postSessionByIdPermissionsByPermissionId({ path, body })` | 項堦皜 鞖旍箔 雽€鞚?               | 氚橅拡 `boolean`                                                                                                                                                                                                                     |

---

#### 鞓堨牅

```javascript
// Create and manage sessions
const session = await client.session.create({
  body: { title: "My session" },
})

const sessions = await client.session.list()

// Send a prompt message
const result = await client.session.prompt({
  path: { id: session.id },
  body: {
    model: { providerID: "anthropic", modelID: "claude-3-5-sonnet-20241022" },
    parts: [{ type: "text", text: "Hello!" }],
  },
})

// Inject context without triggering AI response (useful for plugins)
await client.session.prompt({
  path: { id: session.id },
  body: {
    noReply: true,
    parts: [{ type: "text", text: "You are a helpful assistant." }],
  },
})
```

---

### 韺岇澕

| 氚╇矔                      | 靹る獏                         | 鞚戨嫷                                                                                   |
| ------------------------- | ---------------------------- | -------------------------------------------------------------------------------------- |
| `find.text({ query })`    | 韺岇澕鞐愳劀 韰嶌姢韸?瓴€靸?        | `path`, `lines`, `line_number`, `absolute_offset`, `submatches`鞕€ 鞚检箻頃橂姅 臧滌泊鞚?氚办棿 |
| `find.files({ query })`   | 鞚措鞚?韺岇澕 氚?霐旊爥韱犽Μ 彀娟赴 | `string[]` (瓴诫)                                                                      |
| `find.symbols({ query })` | 鞛戩梾 瓿店皠 鞁臣 彀娟赴          | <a href={typesUrl}><code>Symbol[]</code></a>                                           |
| `file.read({ query })`    | 韺岇澕 氤搓赴                    | `{ type: "raw" \| "patch", content: string }`                                          |
| `file.status({ query? })` | 韸鸽灆 霅?韺岇澕 靸來儨毳?頇曥澑     | <a href={typesUrl}><code>File[]</code></a>                                             |

`find.files`電?氇囯獓 靹犿儩鞝侅澑 臁绊殞 攵勳暭毳?歆€鞗愴暕雼堧嫟:

- `type`: `"file"` 霕愲姅 `"directory"`
- `directory`: 瓴€靸夓棎 雽€頃?頂勲鞝濏姼 耄姼毳?override
- `limit`: 斓滊寑 瓴瓣臣 (1-200)

---

#### 鞓堨牅

```javascript
// Search and read files
const textResults = await client.find.text({
  query: { pattern: "function.*opencode" },
})

const files = await client.find.files({
  query: { query: "*.ts", type: "file" },
})

const directories = await client.find.files({
  query: { query: "packages", type: "directory", limit: 20 },
})

const content = await client.file.read({
  query: { path: "src/index.ts" },
})
```

---

### TUI

| 氚╇矔                           | 靹る獏                     | 鞚戨嫷      |
| ------------------------------ | ------------------------ | --------- |
| `tui.appendPrompt({ body })`   | 頂勲‖頂勴姼鞐?韰嶌姢韸鸽ゼ 攵€鞐?| `boolean` |
| `tui.openHelp()`               | 霃勳泙毵?鞐搓赴              | `boolean` |
| `tui.openSessions()`           | 靹胳厴 靹犿儩 鞎堧偞           | `boolean` |
| `tui.openThemes()`             | 韰岆 靹犿儩 鞐搓赴           | `boolean` |
| `tui.openModels()`             | 氇嵏 靹犿儩 鞎堧偞           | `boolean` |
| `tui.submitPrompt()`           | 順勳灛 頂勲‖頂勴姼 鞝滌稖       | `boolean` |
| `tui.clearPrompt()`            | 頂勲‖頂勴姼 韥措Μ鞏?         | `boolean` |
| `tui.executeCommand({ body })` | 氇呺牴鞏?鞁ろ枆              | `boolean` |
| `tui.showToast({ body })`      | 韱犾姢韸?鞎岆 響滌嫓         | `boolean` |

---

#### 鞓堨牅

```javascript
// Control TUI interface
await client.tui.appendPrompt({
  body: { text: "Add this to prompt" },
})

await client.tui.showToast({
  body: { message: "Task completed", variant: "success" },
})
```

---

### 鞚胳

| 氚╇矔                | 靹る獏           | 鞚戨嫷      |
| ------------------- | -------------- | --------- |
| `auth.set({ ... })` | 鞚胳 鞛愱博 歃濍獏 | `boolean` |

---

#### 鞓堨牅

```javascript
await client.auth.set({
  path: { id: "anthropic" },
  body: { type: "api", key: "your-api-key" },
})
```

---

### 鞚措菠韸?

| 氚╇矔                | 靹る獏                    | 鞚戨嫷                    |
| ------------------- | ----------------------- | ----------------------- |
| `event.subscribe()` | 靹滊矂-sent 鞚措菠韸?鞀ろ姼毽?| 靹滊矂-sent 鞚措菠韸?鞀ろ姼毽?|

---

#### 鞓堨牅

```javascript
// Listen to real-time events
const events = await client.event.subscribe()
for await (const event of events.stream) {
  console.log("Event:", event.type, event.properties)
}
```
