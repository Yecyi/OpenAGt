---
title: Wtyczki
description: Napisz w艂asn膮 wtyczk臋, aby rozszerzy膰 opencode.
---

Wtyczki umo偶liwiaj膮ce wy艂膮czenie opencode poprzez podj臋cie r贸偶nych zdarze艅 i zachowanie. Mo偶na utworzy膰 wtyczk臋, aby sprawdzi膰 nowe funkcje, integrowa膰 si臋 z us艂ugami lub sprawdzaniem zachowania opencode.

Aby znale藕膰 si臋 z przyk艂adami, przejrzyj [wtyczki](/docs/ecosystem#plugins) utworzone przez spo艂eczno艣膰.

---

## U偶yj wtyczki

Istniej膮 dwa sposoby 艂adowania wtyczek.

---

### Z plik贸w lokalnych

Miejsce plik贸w JavaScript lub TypeScript w katalogu wtyczki.

- `.opencode/plugins/` - Wtyczki na poziomie projektu
- `~/.config/opencode/plugins/` - Wtyczki globalne

Pliki w tych katalogach s膮 automatycznie 艂adowane podczas uruchamiania.

---

### Z npm

szczeg贸艂owy pakiet npm w pliku konfiguracyjnym.

```json title="opencode.json"
{
  "$schema": "https://github.com/Yecyi/OpenAGt/raw/dev/packages/web/dist/config.json",
  "plugin": ["opencode-helicone-session", "opencode-wakatime", "@my-org/custom-plugin"]
}
```

Obs艂ugiwane s膮 zar贸wno pakiety zwyk艂e, jak i pakiety npm o okre艣lonym zakresie.

Przegl膮daj dost臋pn膮 wtyczk臋 w [ekosystemie](/docs/ecosystem#plugins).

---

### Spos贸b instalacji wtyczek

**wtyczki npm** s膮 instalowane automatycznie przy u偶yciu Bun podczas uruchamiania. Pakiety i ich zale偶no艣ci s膮 buforowane w `~/.cache/opencode/node_modules/`.

**Wtyczki lokalne** s膮 艂adowane bezpo艣rednio z katalogu wtyczek. Aby uzyska膰 dost臋p do zewn臋trznych pakiet贸w, musisz zainstalowa膰 `package.json` w swoim katalogu konfiguracyjnym (zobacz [Zale偶no艣ci](#dependencies)) lub opublikowa膰 wtyczk臋 w npm i [dodaj j膮 do swojej konfiguracji](/docs/config#plugins).

---

### Kolejno艣膰 艂adowania

Wtyczki s膮 艂adowane ze wszystkich 藕r贸de艂, a wszystkie hooki napisane po kolei. Kolejno艣膰 艂adowania do:

1. Konfiguracja globalna (`~/.config/opencode/opencode.json`)
2. Konfiguracja projektu (`opencode.json`)
3. Globalny katalog wtyczek (`~/.config/opencode/plugins/`)
4. Katalog wtyczek projektu (`.opencode/plugins/`)

Zduplikowane pakiety npm o tej samej nazwie i wersji s膮 艂adowane raz. R贸wnie wa偶ny jest dost臋p lokalny i zewn臋trzny npm o dodatkowych nazwach, kt贸re s膮 zewn臋trzne.

---

## Utw贸rz wtyczk臋

Wtyczka do **modu艂 JavaScript/TypeScript**, kt贸ry eksportuje jeden lub wi臋cej wtyczek
funkcje. dost臋pna funkcja korzystania z obiektu kontekstu i g艂贸wnego obiektu hooks.

---

### Zale偶no艣ci

Lokalne narz臋dzie i narz臋dzie, kt贸re mo偶na wykorzysta膰 z zewn臋trznych pakiet贸w npm. Dodaj `package.json` do swojego katalogu konfiguracyjnego z zale偶no艣ciami.

```json title=".opencode/package.json"
{
  "dependencies": {
    "shescape": "^2.1.0"
  }
}
```

opencode uruchomienie `bun install` przy uruchomieniu, aby je uruchomi膰. Twoje dodatkowe funkcje b臋d膮 dost臋pne po zaimportowaniu.

```ts title=".opencode/plugins/my-plugin.ts"
import { escape } from "shescape"

export const MyPlugin = async (ctx) => {
  return {
    "tool.execute.before": async (input, output) => {
      if (input.tool === "bash") {
        output.args.command = escape(output.args.command)
      }
    },
  }
}
```

---

### Podstawowa struktura

```js title=".opencode/plugins/example.js"
export const MyPlugin = async ({ project, client, $, directory, worktree }) => {
  console.log("Plugin initialized!")

  return {
    // Hook implementations go here
  }
}
```

Funkcja dost臋pu do:

- `project`: Aktualne informacje o projekcie.
- `directory`: Bie偶膮cy katalog roboczy.
- `worktree`: 艢cie偶ka eksploatacyjna Git.
- `client`: Klient SDK z opencode do sztucznej inteligencji.
- `$`: [API obudowy](https://bun.com/docs/runtime/shell) Buna do wykonywania oprogramowania.

---

### Obs艂uga TypeScriptu

W przypadku wtyczek TypeScript mo偶esz importowa膰 typy z pakietu wtyczek:

```ts title="my-plugin.ts" {1}
import type { Plugin } from "@openagt/plugin"

export const MyPlugin: Plugin = async ({ project, client, $, directory, worktree }) => {
  return {
    // Type-safe hook implementations
  }
}
```

---

### Wydarzenia

Wtyczki mog膮 subskrybowa膰 zdarzenia, jak zastosowa膰 poni偶ej sekcji Przyk艂ady. Oto lista r贸偶nych dzia艂a艅.

#### Wydarzenia dowodzenia

- `command.executed`

#### Plik Wydarzenia

- `file.edited`
- `file.watcher.updated`

#### Wydarzenia instalacyjne

- `installation.updated`

#### Wydarzenia LSP

- `lsp.client.diagnostics`
- `lsp.updated`

#### Wydarzenia w wiadomo艣ciach

- `message.part.removed`
- `message.part.updated`
- `message.removed`
- `message.updated`

#### Wydarzenia zwi膮zane z uprawnieniami

- `permission.asked`
- `permission.replied`

#### Wydarzenia serwerowe

- `server.connected`

#### Wydarzenia sesyjne

- `session.created`
- `session.compacted`
- `session.deleted`
- `session.diff`
- `session.error`
- `session.idle`
- `session.status`
- `session.updated`

#### Wydarzenia do zrobienia

- `todo.updated`

#### Wydarzenia shell

- `shell.env`

#### Wydarzenia narz臋dziowe

- `tool.execute.after`
- `tool.execute.before`

#### Wydarzenia TUI

- `tui.prompt.append`
- `tui.command.execute`
- `tui.toast.show`

---

## Przyk艂ady

Oto kilka dost臋pnych wtyczek, kt贸re mo偶na udost臋pni膰 do opencode.

---

### Wysy艂aj powiadomienia

Wysy艂aj powiadomienia, gdy wyst膮pi膮 okre艣lone zdarzenia:

```js title=".opencode/plugins/notification.js"
export const NotificationPlugin = async ({ project, client, $, directory, worktree }) => {
  return {
    event: async ({ event }) => {
      // Send notification on session completion
      if (event.type === "session.idle") {
        await $`osascript -e 'display notification "Session completed!" with title "opencode"'`
      }
    },
  }
}
```

U偶ywamy `osascript` do uruchamiania AppleScript na macOS. Tutaj mo偶esz przej艣膰 do wysy艂ania pozosta艂o艣ci.

:::note
Je艣li korzystasz z aplikacji komputerowej opencode, mo偶e ona automatycznie wys艂a膰 wiadomo艣膰 systemow膮, gdy odpowied藕 jest gotowa lub gdy wyst膮pi膮 b艂臋dy sesji.
:::

---

### ochrona .env

Zablokuj opencode czytanie plik贸w `.env`:

```javascript title=".opencode/plugins/env-protection.js"
export const EnvProtection = async ({ project, client, $, directory, worktree }) => {
  return {
    "tool.execute.before": async (input, output) => {
      if (input.tool === "read" && output.args.filePath.includes(".env")) {
        throw new Error("Do not read .env files")
      }
    },
  }
}
```

---

### Wstrzyknij zmienne 艣rodowiskowe

Wstrzyknij zmienne konsekwencje dla wszystkich wykona艅 (narz臋dzia AI i terminale u偶ytkownika):

```javascript title=".opencode/plugins/inject-env.js"
export const InjectEnvPlugin = async () => {
  return {
    "shell.env": async (input, output) => {
      output.env.MY_API_KEY = "secret"
      output.env.PROJECT_ROOT = input.cwd
    },
  }
}
```

---

### Niestandardowe narz臋dzia

Wtyczki mog膮 r贸wnie偶 dodawa膰 niestandardowe narz臋dzia do opencode:

```ts title=".opencode/plugins/custom-tools.ts"
import { type Plugin, tool } from "@openagt/plugin"

export const CustomToolsPlugin: Plugin = async (ctx) => {
  return {
    tool: {
      mytool: tool({
        description: "This is a custom tool",
        args: {
          foo: tool.schema.string(),
        },
        async execute(args, context) {
          const { directory, worktree } = context
          return `Hello ${args.foo} from ${directory} (worktree: ${worktree})`
        },
      }),
    },
  }
}
```

Pomocnik `tool` tworzy narz臋dzie u偶ytkowe, kt贸re mo偶e wywo艂a膰 kod opencode. Pobierane schematu Zoda i narz臋dzie wyszukiwania za pomoc膮:

- `description`: Co robi do narz臋dzia
- `args`: Schemat Zoda dla narz臋dzi argument贸w
- `execute`: Funkcja uruchamiania po wywo艂aniu narz臋dzia

Twoje narz臋dzie b臋dzie dost臋pne dla otwartego kodu wraz z narz臋dziami u偶ytkowymi.

---

### Logowanie

u偶yj `client.app.log()` zamiast `console.log` do rejestracji strukturalnego:

```ts title=".opencode/plugins/my-plugin.ts"
export const MyPlugin = async ({ client }) => {
  await client.app.log({
    body: {
      service: "my-plugin",
      level: "info",
      message: "Plugin initialized",
      extra: { foo: "bar" },
    },
  })
}
```

Poziomy: `debug`, `info`, `warn`, `error`. Aby uzyska膰 szczeg贸艂owe informacje, zobacz [dokumentacj臋 pakietu SDK](https://github.com/Yecyi/OpenAGt/docs/sdk).

---

### Haki zag臋szczaj膮ce

Dostosuj kontekst do艂膮czony podczas kompaktowania sesji:

```ts title=".opencode/plugins/compaction.ts"
import type { Plugin } from "@openagt/plugin"

export const CompactionPlugin: Plugin = async (ctx) => {
  return {
    "experimental.session.compacting": async (input, output) => {
      // Inject additional context into the compaction prompt
      output.context.push(`
## Custom Context

Include any state that should persist across compaction:
- Current task status
- Important decisions made
- Files being actively worked on
`)
    },
  }
}
```

Hak `experimental.session.compacting` jest uruchamiany, zanim LLM wygeneruje podsumowanie kontynuacji. go, aby wstrzykn膮膰 kontekst kontekstu dla domeny, kt贸rej nie dostrze偶ono przez domy艣lny monit o zag臋szczenie.

Mo偶na tak偶e umie艣ci膰 kompletny monit o zag臋szczenie, ustawiaj膮c `output.prompt`:

```ts title=".opencode/plugins/custom-compaction.ts"
import type { Plugin } from "@openagt/plugin"

export const CustomCompactionPlugin: Plugin = async (ctx) => {
  return {
    "experimental.session.compacting": async (input, output) => {
      // Replace the entire compaction prompt
      output.prompt = `
You are generating a continuation prompt for a multi-agent swarm session.

Summarize:
1. The current task and its status
2. Which files are being modified and by whom
3. Any blockers or dependencies between agents
4. The next steps to complete the work

Format as a structured prompt that a new agent can use to resume work.
`
    },
  }
}
```

Gdy urz膮dzenie jest `output.prompt`, jest ca艂kowicie dost臋pne na domy艣lnym monit o zag臋szczanie. Tablica `output.context` jest w tym przypadku poniesiona.
