---
title: SDK
description: Typowany klient JS dla serwera OpenAGt.
---

import config from "../../../../config.mjs"
export const typesUrl = `${config.github}/blob/dev/packages/sdk/js/src/gen/types.gen.ts`

Pakiet SDK JS/TS OpenAGt zapewnia klienta z bezpiecznym typowaniem (type-safe) do interakcji z serwerem.
U偶yj go do budowania integracji i programowej kontroli OpenAGt.

[Dowiedz si臋 wi臋cej](/docs/server) o dzia艂aniu serwera. Przyk艂ady znajdziesz w [projektach](/docs/ecosystem#projects) stworzonych przez spo艂eczno艣膰.

---

## Instalacja

Zainstaluj pakiet SDK z npm:

```bash
npm install @openagt/sdk
```

---

## Tworzenie klienta

Utw贸rz instancj臋 OpenAGt:

```javascript
import { createOpencode } from "@openagt/sdk"

const { client } = await createOpencode()
```

Spowoduje to uruchomienie zar贸wno serwera, jak i klienta.

#### Opcje

| Opcja      | Typ           | Opis                                      | Domy艣lne    |
| ---------- | ------------- | ----------------------------------------- | ----------- |
| `hostname` | `string`      | Nazwa hosta serwera                       | `127.0.0.1` |
| `port`     | `number`      | Port serwera                              | `4096`      |
| `signal`   | `AbortSignal` | Sygna艂 przerwania w celu anulowania       | `undefined` |
| `timeout`  | `number`      | Limit czasu w ms dla uruchomienia serwera | `5000`      |
| `config`   | `Config`      | Obiekt konfiguracji                       | `{}`        |

---

## Konfiguracja

Mo偶na przekaza膰 obiekt konfiguracyjny, aby dostosowa膰 zachowanie. Instancja nadal pobiera `opencode.json`, ale mo偶esz zast膮pi膰 lub doda膰 konfiguracj臋 bezpo艣rednio:

```javascript
import { createOpencode } from "@openagt/sdk"

const opencode = await createOpencode({
  hostname: "127.0.0.1",
  port: 4096,
  config: {
    model: "anthropic/claude-3-5-sonnet-20241022",
  },
})

console.log(`Server running at ${opencode.server.url}`)

opencode.server.close()
```

## Tylko klient

Je艣li masz ju偶 dzia艂aj膮c膮 instancj臋 OpenAGt, mo偶esz utworzy膰 instancj臋 klienta, aby si臋 z ni膮 po艂膮czy膰:

```javascript
import { createOpencodeClient } from "@openagt/sdk"

const client = createOpencodeClient({
  baseUrl: "http://localhost:4096",
})
```

#### Opcje

| Opcja           | Typ        | Opis                                | Domy艣lne                |
| --------------- | ---------- | ----------------------------------- | ----------------------- |
| `baseUrl`       | `string`   | Adres URL serwera                   | `http://localhost:4096` |
| `fetch`         | `function` | Niestandardowa implementacja fetch  | `globalThis.fetch`      |
| `parseAs`       | `string`   | Metoda parsowania odpowiedzi        | `auto`                  |
| `responseStyle` | `string`   | Styl zwracania: `data` lub `fields` | `fields`                |
| `throwOnError`  | `boolean`  | Rzucaj b艂臋dy zamiast zwraca膰 je     | `false`                 |

---

## Typy

Zestaw SDK zawiera definicje TypeScript dla wszystkich typ贸w API. Zaimportuj je bezpo艣rednio:

```typescript
import type { Session, Message, Part } from "@openagt/sdk"
```

Wszystkie typy s膮 generowane na podstawie specyfikacji OpenAPI serwera i dost臋pne w <a href={typesUrl}>pliku typ贸w</a>.

---

## B艂臋dy

SDK mo偶e generowa膰 b艂臋dy, kt贸re mo偶na przechwyci膰 i obs艂u偶y膰:

```typescript
try {
  await client.session.get({ path: { id: "invalid-id" } })
} catch (error) {
  console.error("Failed to get session:", (error as Error).message)
}
```

---

## Ustrukturyzowane Dane Wyj艣ciowe

Mo偶esz za偶膮da膰 ustrukturyzowanych danych wyj艣ciowych JSON od modelu, okre艣laj膮c `format` za pomoc膮 schematu JSON. Model u偶yje narz臋dzia `StructuredOutput`, aby zwr贸ci膰 zweryfikowany kod JSON pasuj膮cy do Twojego schematu.

### Podstawowe u偶ycie

```typescript
const result = await client.session.prompt({
  path: { id: sessionId },
  body: {
    parts: [{ type: "text", text: "Research Anthropic and provide company info" }],
    format: {
      type: "json_schema",
      schema: {
        type: "object",
        properties: {
          company: { type: "string", description: "Company name" },
          founded: { type: "number", description: "Year founded" },
          products: {
            type: "array",
            items: { type: "string" },
            description: "Main products",
          },
        },
        required: ["company", "founded"],
      },
    },
  },
})

// Access the structured output
console.log(result.data.info.structured_output)
// { company: "Anthropic", founded: 2021, products: ["Claude", "Claude API"] }
```

### Typy formatu wyj艣ciowego

| Typ           | Opis                                                                      |
| ------------- | ------------------------------------------------------------------------- |
| `text`        | Domy艣lny. Standardowa odpowied藕 tekstowa (brak ustrukturyzowanych danych) |
| `json_schema` | Zwraca zweryfikowany JSON pasuj膮cy do dostarczonego schematu              |

### Format JSON Schema

U偶ywaj膮c `type: 'json_schema'`, podaj:

| Pole         | Typ             | Opis                                                         |
| ------------ | --------------- | ------------------------------------------------------------ |
| `type`       | `'json_schema'` | Wymagane. Okre艣la tryb schematu JSON                         |
| `schema`     | `object`        | Wymagane. Obiekt JSON Schema definiuj膮cy struktur臋 wyj艣ciow膮 |
| `retryCount` | `number`        | Opcjonalne. Liczba ponownych pr贸b walidacji (domy艣lnie: 2)   |

### Obs艂uga b艂臋d贸w

Je艣li model nie wygeneruje prawid艂owych ustrukturyzowanych danych wyj艣ciowych po wszystkich pr贸bach, odpowied藕 b臋dzie zawiera膰 `StructuredOutputError`:

```typescript
if (result.data.info.error?.name === "StructuredOutputError") {
  console.error("Failed to produce structured output:", result.data.info.error.message)
  console.error("Attempts:", result.data.info.error.retries)
}
```

### Najlepsze praktyki

1. **Podawaj jasne opisy** we w艂a艣ciwo艣ciach schematu, aby pom贸c modelowi zrozumie膰, jakie dane wyodr臋bni膰
2. **U偶ywaj `required`**, aby okre艣li膰, kt贸re pola musz膮 by膰 obecne
3. **Zachowaj schematy skoncentrowane** - z艂o偶one zagnie偶d偶one schematy mog膮 by膰 trudniejsze dla modelu do poprawnego wype艂nienia
4. **Ustaw odpowiedni `retryCount`** - zwi臋ksz dla z艂o偶onych schemat贸w, zmniejsz dla prostych

---

## API

Zestaw SDK udost臋pnia wszystkie interfejsy API serwera za po艣rednictwem klienta bezpiecznego typu.

---

### Globalne

| Metoda            | Opis                          | Odpowied藕                            |
| ----------------- | ----------------------------- | ------------------------------------ |
| `global.health()` | Sprawd藕 stan i wersj臋 serwera | `{ healthy: true, version: string }` |

---

#### Przyk艂ady

```javascript
const health = await client.global.health()
console.log(health.data.version)
```

---

### Aplikacja

| Metoda         | Opis                                | Odpowied藕                                   |
| -------------- | ----------------------------------- | ------------------------------------------- |
| `app.log()`    | Zapisz wpis logu                    | `boolean`                                   |
| `app.agents()` | Lista wszystkich dost臋pnych agent贸w | <a href={typesUrl}><code>Agent[]</code></a> |

---

#### Przyk艂ady

```javascript
// Write a log entry
await client.app.log({
  body: {
    service: "my-app",
    level: "info",
    message: "Operation completed",
  },
})

// List available agents
const agents = await client.app.agents()
```

---

### Projekt

| Metoda              | Opis                       | Odpowied藕                                     |
| ------------------- | -------------------------- | --------------------------------------------- |
| `project.list()`    | Lista wszystkich projekt贸w | <a href={typesUrl}><code>Project[]</code></a> |
| `project.current()` | Pobierz bie偶膮cy projekt    | <a href={typesUrl}><code>Project</code></a>   |

---

#### Przyk艂ady

```javascript
// List all projects
const projects = await client.project.list()

// Get current project
const currentProject = await client.project.current()
```

---

### 艢cie偶ka

| Metoda       | Opis                    | Odpowied藕                                |
| ------------ | ----------------------- | ---------------------------------------- |
| `path.get()` | Pobierz bie偶膮c膮 艣cie偶k臋 | <a href={typesUrl}><code>Path</code></a> |

---

#### Przyk艂ady

```javascript
// Get current path information
const pathInfo = await client.path.get()
```

---

### Konfiguracja

| Metoda               | Opis                                | Odpowied藕                                                                                             |
| -------------------- | ----------------------------------- | ----------------------------------------------------------------------------------------------------- |
| `config.get()`       | Pobierz informacje o konfiguracji   | <a href={typesUrl}><code>Config</code></a>                                                            |
| `config.providers()` | Lista dostawc贸w i modeli domy艣lnych | `{ providers: `<a href={typesUrl}><code>Provider[]</code></a>`, default: { [key: string]: string } }` |

---

#### Przyk艂ady

```javascript
const config = await client.config.get()

const { providers, default: defaults } = await client.config.providers()
```

---

### Sesje

| Metoda                                                     | Opis                                        | Uwagi                                                                                                                                            |
| ---------------------------------------------------------- | ------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------ |
| `session.list()`                                           | Lista sesji                                 | Zwraca <a href={typesUrl}><code>Session[]</code></a>                                                                                             |
| `session.get({ path })`                                    | Pobierz sesj臋                               | Zwraca <a href={typesUrl}><code>Session</code></a>                                                                                               |
| `session.children({ path })`                               | Lista sesji podrz臋dnych                     | Zwraca <a href={typesUrl}><code>Session[]</code></a>                                                                                             |
| `session.create({ body })`                                 | Utw贸rz sesj臋                                | Zwraca <a href={typesUrl}><code>Session</code></a>                                                                                               |
| `session.delete({ path })`                                 | Usu艅 sesj臋                                  | Zwraca `boolean`                                                                                                                                 |
| `session.update({ path, body })`                           | Aktualizuj w艂a艣ciwo艣ci sesji                | Zwraca <a href={typesUrl}><code>Session</code></a>                                                                                               |
| `session.init({ path, body })`                             | Przeanalizuj aplikacj臋 i utw贸rz `AGENTS.md` | Zwraca `boolean`                                                                                                                                 |
| `session.abort({ path })`                                  | Przerwij trwaj膮c膮 sesj臋                     | Zwraca `boolean`                                                                                                                                 |
| `session.share({ path })`                                  | Udost臋pnij sesj臋                            | Zwraca <a href={typesUrl}><code>Session</code></a>                                                                                               |
| `session.unshare({ path })`                                | Cofnij udost臋pnianie sesji                  | Zwraca <a href={typesUrl}><code>Session</code></a>                                                                                               |
| `session.summarize({ path, body })`                        | Podsumuj sesj臋                              | Zwraca `boolean`                                                                                                                                 |
| `session.messages({ path })`                               | Lista wiadomo艣ci w sesji                    | Zwraca `{ info: `<a href={typesUrl}><code>Message</code></a>`, parts: `<a href={typesUrl}><code>Part[]</code></a>`}[]`                           |
| `session.message({ path })`                                | Pobierz szczeg贸艂y wiadomo艣ci                | Zwraca `{ info: `<a href={typesUrl}><code>Message</code></a>`, parts: `<a href={typesUrl}><code>Part[]</code></a>`}`                             |
| `session.prompt({ path, body })`                           | Wy艣lij wiadomo艣膰                            | `body.noReply: true` zwraca UserMessage (tylko kontekst). Domy艣lnie zwraca <a href={typesUrl}><code>AssistantMessage</code></a> z odpowiedzi膮 AI |
| `session.command({ path, body })`                          | Wy艣lij polecenie do sesji                   | Zwraca `{ info: `<a href={typesUrl}><code>AssistantMessage</code></a>`, parts: `<a href={typesUrl}><code>Part[]</code></a>`}`                    |
| `session.shell({ path, body })`                            | Uruchom polecenie shell                     | Zwraca <a href={typesUrl}><code>AssistantMessage</code></a>                                                                                      |
| `session.revert({ path, body })`                           | Cofnij wiadomo艣膰 (revert)                   | Zwraca <a href={typesUrl}><code>Session</code></a>                                                                                               |
| `session.unrevert({ path })`                               | Przywr贸膰 cofni臋te wiadomo艣ci                | Zwraca <a href={typesUrl}><code>Session</code></a>                                                                                               |
| `postSessionByIdPermissionsByPermissionId({ path, body })` | Odpowiedz na 偶膮danie uprawnienia            | Zwraca `boolean`                                                                                                                                 |

---

#### Przyk艂ady

```javascript
// Create and manage sessions
const session = await client.session.create({
  body: { title: "My session" },
})

const sessions = await client.session.list()

// Send a prompt message
const result = await client.session.prompt({
  path: { id: session.id },
  body: {
    model: { providerID: "anthropic", modelID: "claude-3-5-sonnet-20241022" },
    parts: [{ type: "text", text: "Hello!" }],
  },
})

// Inject context without triggering AI response (useful for plugins)
await client.session.prompt({
  path: { id: session.id },
  body: {
    noReply: true,
    parts: [{ type: "text", text: "You are a helpful assistant." }],
  },
})
```

---

### Pliki

| Metoda                    | Opis                                 | Odpowied藕                                                                                      |
| ------------------------- | ------------------------------------ | ---------------------------------------------------------------------------------------------- |
| `find.text({ query })`    | Szukaj tekstu w plikach              | Tablica obiekt贸w dopasowania z `path`, `lines`, `line_number`, `absolute_offset`, `submatches` |
| `find.files({ query })`   | Znajd藕 pliki i katalogi wed艂ug nazwy | `string[]` (艣cie偶ki)                                                                           |
| `find.symbols({ query })` | Znajd藕 symbole w obszarze roboczym   | <a href={typesUrl}><code>Symbol[]</code></a>                                                   |
| `file.read({ query })`    | Odczytaj plik                        | `{ type: "raw" \| "patch", content: string }`                                                  |
| `file.status({ query? })` | Pobierz status 艣ledzonych plik贸w     | <a href={typesUrl}><code>File[]</code></a>                                                     |

`find.files` obs艂uguje kilka opcjonalnych p贸l zapytania:

- `type`: `"file"` lub `"directory"`
- `directory`: zast膮p katalog g艂贸wny projektu dla wyszukiwania
- `limit`: maks. wynik贸w (1鈥?00)

---

#### Przyk艂ady

```javascript
// Search and read files
const textResults = await client.find.text({
  query: { pattern: "function.*opencode" },
})

const files = await client.find.files({
  query: { query: "*.ts", type: "file" },
})

const directories = await client.find.files({
  query: { query: "packages", type: "directory", limit: 20 },
})

const content = await client.file.read({
  query: { path: "src/index.ts" },
})
```

---

### TUI

| Metoda                         | Opis                        | Odpowied藕 |
| ------------------------------ | --------------------------- | --------- |
| `tui.appendPrompt({ body })`   | Do艂膮cz tekst do promptu     | `boolean` |
| `tui.openHelp()`               | Otw贸rz okno pomocy          | `boolean` |
| `tui.openSessions()`           | Otw贸rz selektor sesji       | `boolean` |
| `tui.openThemes()`             | Otw贸rz selektor motyw贸w     | `boolean` |
| `tui.openModels()`             | Otw贸rz selektor modelu      | `boolean` |
| `tui.submitPrompt()`           | Prze艣lij bie偶膮cy prompt     | `boolean` |
| `tui.clearPrompt()`            | Wyczy艣膰 prompt              | `boolean` |
| `tui.executeCommand({ body })` | Wykonaj polecenie           | `boolean` |
| `tui.showToast({ body })`      | Poka偶 powiadomienie (toast) | `boolean` |

---

#### Przyk艂ady

```javascript
// Control TUI interface
await client.tui.appendPrompt({
  body: { text: "Add this to prompt" },
})

await client.tui.showToast({
  body: { message: "Task completed", variant: "success" },
})
```

---

### Uwierzytelnianie

| Metoda              | Opis                                 | Odpowied藕 |
| ------------------- | ------------------------------------ | --------- |
| `auth.set({ ... })` | Ustaw po艣wiadczenia uwierzytelniania | `boolean` |

---

#### Przyk艂ady

```javascript
await client.auth.set({
  path: { id: "anthropic" },
  body: { type: "api", key: "your-api-key" },
})
```

---

### Zdarzenia

| Metoda              | Opis                                     | Odpowied藕                                |
| ------------------- | ---------------------------------------- | ---------------------------------------- |
| `event.subscribe()` | Strumie艅 zdarze艅 wysy艂anych przez serwer | Strumie艅 zdarze艅 wysy艂anych przez serwer |

---

#### Przyk艂ady

```javascript
// Listen to real-time events
const events = await client.event.subscribe()
for await (const event of events.stream) {
  console.log("Event:", event.type, event.properties)
}
```
