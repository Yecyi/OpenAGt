---
title: Narz臋dzia specjalistyczne
description: Tw贸rz narz臋dzi, kt贸re LLM mog膮 by膰 uruchamiane w otwartym kodzie.
---

Narz臋dzia specjalistyczne to funkcje, kt贸re tworzysz i kt贸re LLM mo偶e wywo艂ywa膰 podczas rozm贸w. Wsp贸艂pracuj膮 one z [wbudowanymi narz臋dziami](/docs/tools) opencode, takimi jak `read`, `write` i `bash`.

---

## Tworzenie narz臋dzia

Narz臋dzia tworz膮 pliki **TypeScript** lub **JavaScript**. Definicja narz臋dzia mo偶e by膰 odwo艂ywana do skrypt贸w napisanych w **dowolnym j臋zyku** 鈥?TypeScript lub JavaScript s膮 u偶ywane tylko w tym samym narz臋dziu.

---

### Lokalizacja

Mo偶na je uruchomi膰:

- Lokalnie, umieszczaj膮c je w katalogu `.opencode/tools/` swojego projektu.
- Lub globalnie, umieszczaj膮c je w `~/.config/opencode/tools/`.

---

### Struktura

Naj艂atwiejszym sposobem tworzenia narz臋dzia jest pomocnika `tool()`, kt贸ry zapewnia bezpiecze艅stwo i sprawdzanie poprawno艣ci.

```ts title=".opencode/tools/database.ts" {1}
import { tool } from "@openagt/plugin"

export default tool({
  description: "Query the project database",
  args: {
    query: tool.schema.string().describe("SQL query to execute"),
  },
  async execute(args) {
    // Your database logic here
    return `Executed query: ${args.query}`
  },
})
```

**Nazwa pliku** staje si臋 **nazw膮 narz臋dzia**. Pierwotne narz臋dzie `database`.

---

#### Wiele narz臋dzi na plik

Mo偶na tak偶e eksportowa膰 wiele narz臋dzi z jednego pliku. Ka偶dy eksport staje si臋 **oddzielnym j臋zykiem** o nazwie **`<filename>_<exportname>`**:

```ts title=".opencode/tools/math.ts"
import { tool } from "@openagt/plugin"

export const add = tool({
  description: "Add two numbers",
  args: {
    a: tool.schema.number().describe("First number"),
    b: tool.schema.number().describe("Second number"),
  },
  async execute(args) {
    return args.a + args.b
  },
})

export const multiply = tool({
  description: "Multiply two numbers",
  args: {
    a: tool.schema.number().describe("First number"),
    b: tool.schema.number().describe("Second number"),
  },
  async execute(args) {
    return args.a * args.b
  },
})
```

Tworzy to dwa narz臋dzia: `math_add` i `math_multiply`.

---

#### Kolizje nazw z wbudowanymi narz臋dziami

Niestandardowe narz臋dzia s膮 identyfikowane wed艂ug nazwy narz臋dzia. Je艣li niestandardowe narz臋dzie u偶ywa tej samej nazwy co wbudowane narz臋dzie, niestandardowe narz臋dzie ma pierwsze艅stwo.

Na przyk艂ad ten plik zast臋puje wbudowane narz臋dzie `bash`:

```ts title=".opencode/tools/bash.ts"
import { tool } from "@openagt/plugin"

export default tool({
  description: "Restricted bash wrapper",
  args: {
    command: tool.schema.string(),
  },
  async execute(args) {
    return `blocked: ${args.command}`
  },
})
```

:::note
Preferuj unikalne nazwy, chyba 偶e celowo chcesz zast膮pi膰 wbudowane narz臋dzie. Je艣li chcesz wy艂膮czy膰 wbudowane narz臋dzie, ale go nie nadpisywa膰, u偶yj [uprawnie艅](/docs/permissions).
:::

---

### Argumenty

Do zdefiniowania argument贸w mo偶na zastosowa膰 `tool.schema`, czyli po prostu [Zod](https://zod.dev).

```ts "tool.schema"
args: {
  query: tool.schema.string().describe("SQL query to execute")
}
```

Mo偶na tak偶e bezpo艣rednio zaimportowa膰 [Zod](https://zod.dev) i zwyczajowo zwyk艂y obiekt:

```ts {6}
import { z } from "zod"

export default {
  description: "Tool description",
  args: {
    param: z.string().describe("Parameter description"),
  },
  async execute(args, context) {
    // Tool implementation
    return "result"
  },
}
```

---

### Kontekst

Narz臋dzia kontekstowe charakterystycznej sesji:

```ts title=".opencode/tools/project.ts" {8}
import { tool } from "@openagt/plugin"

export default tool({
  description: "Get project information",
  args: {},
  async execute(args, context) {
    // Access context information
    const { agent, sessionID, messageID, directory, worktree } = context
    return `Agent: ${agent}, Session: ${sessionID}, Message: ${messageID}, Directory: ${directory}, Worktree: ${worktree}`
  },
})
```

`context.directory` jako katalogowy pracodawczy.
`context.worktree` dla katalogu g艂贸wnego zwalczaj膮cego git.

---

## Przyk艂ad

### Napisz narz臋dzie w Pythonie

Mo偶esz pisa膰 swoje narz臋dzia w dowolnym j臋zyku. Oto przyk艂ad dodania dw贸ch liczb przy u偶yciu j臋zyka Python.

Najpierw utw贸rz narz臋dzie jako skrypt w j臋zyku Python:

```python title=".opencode/tools/add.py"
import sys

a = int(sys.argv[1])
b = int(sys.argv[2])
print(a + b)
```

Nast臋pnie utw贸rz definicj臋 narz臋dzia, kt贸ra go wywo艂uje:

```ts title=".opencode/tools/python-add.ts" {10}
import { tool } from "@openagt/plugin"
import path from "path"

export default tool({
  description: "Add two numbers using Python",
  args: {
    a: tool.schema.number().describe("First number"),
    b: tool.schema.number().describe("Second number"),
  },
  async execute(args, context) {
    const script = path.join(context.worktree, ".opencode/tools/add.py")
    const result = await Bun.$`python3 ${script} ${args.a} ${args.b}`.text()
    return result.trim()
  },
})
```

Tutaj u偶ywamy narz臋dzia [`Bun.$`](https://bun.com/docs/runtime/shell) do uruchomienia skryptu w j臋zyku Python.
