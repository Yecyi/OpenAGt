---
title: Outils personnalis茅s
description: Cr茅ez des outils que le LLM peut appeler dans opencode.
---

Les outils personnalis茅s sont des fonctions que vous cr茅ez et que le LLM peut appeler pendant les conversations. Ils fonctionnent avec les [outils int茅gr茅s](/docs/tools) de opencode comme `read`, `write` et `bash`.

---

## Cr茅ation d'un outil

Les outils sont d茅finis sous forme de fichiers **TypeScript** ou **JavaScript**. Cependant, la d茅finition de l'outil peut appeler des scripts 茅crits dans **n'importe quel langage** : TypeScript ou JavaScript n'est utilis茅 que pour la d茅finition de l'outil elle-m锚me.

---

### Emplacement

Ils peuvent 锚tre d茅finis :

- Localement en les pla莽ant dans le r茅pertoire `.opencode/tools/` de votre projet.
- Ou globalement, en les pla莽ant dans `~/.config/opencode/tools/`.

---

### Structure

Le moyen le plus simple de cr茅er des outils est d'utiliser l'assistant `tool()` qui fournit la s茅curit茅 et la validation de type.

```ts title=".opencode/tools/database.ts" {1}
import { tool } from "@openagt/plugin"

export default tool({
  description: "Query the project database",
  args: {
    query: tool.schema.string().describe("SQL query to execute"),
  },
  async execute(args) {
    // Your database logic here
    return `Executed query: ${args.query}`
  },
})
```

Le **nom de fichier** devient le **nom de l'outil**. Ce qui pr茅c猫de cr茅e un outil `database`.

---

#### Plusieurs outils par fichier

Vous pouvez 茅galement exporter plusieurs outils 脿 partir d'un seul fichier. Chaque exportation devient **un outil distinct** portant le nom **`<filename>_<exportname>`** :

```ts title=".opencode/tools/math.ts"
import { tool } from "@openagt/plugin"

export const add = tool({
  description: "Add two numbers",
  args: {
    a: tool.schema.number().describe("First number"),
    b: tool.schema.number().describe("Second number"),
  },
  async execute(args) {
    return args.a + args.b
  },
})

export const multiply = tool({
  description: "Multiply two numbers",
  args: {
    a: tool.schema.number().describe("First number"),
    b: tool.schema.number().describe("Second number"),
  },
  async execute(args) {
    return args.a * args.b
  },
})
```

Cela cr茅e deux outils : `math_add` et `math_multiply`.

---

### Arguments

Vous pouvez utiliser `tool.schema`, qui est simplement [Zod](https://zod.dev), pour d茅finir les types d'arguments.

```ts "tool.schema"
args: {
  query: tool.schema.string().describe("SQL query to execute")
}
```

Vous pouvez 茅galement importer [Zod](https://zod.dev) directement et renvoyer un objet simple :

```ts {6}
import { z } from "zod"

export default {
  description: "Tool description",
  args: {
    param: z.string().describe("Parameter description"),
  },
  async execute(args, context) {
    // Tool implementation
    return "result"
  },
}
```

---

### Contexte

Les outils re莽oivent le contexte de la session en cours :

```ts title=".opencode/tools/project.ts" {8}
import { tool } from "@openagt/plugin"

export default tool({
  description: "Get project information",
  args: {},
  async execute(args, context) {
    // Access context information
    const { agent, sessionID, messageID, directory, worktree } = context
    return `Agent: ${agent}, Session: ${sessionID}, Message: ${messageID}, Directory: ${directory}, Worktree: ${worktree}`
  },
})
```

Utilisez `context.directory` pour le r茅pertoire de travail de la session.
Utilisez `context.worktree` pour la racine du worktree git.

---

## Exemples

### 脡crire un outil en Python

Vous pouvez 茅crire vos outils dans le langage de votre choix. Voici un exemple qui ajoute deux nombres 脿 l'aide de Python.

Tout d'abord, cr茅ez l'outil en tant que script Python :

```python title=".opencode/tools/add.py"
import sys

a = int(sys.argv[1])
b = int(sys.argv[2])
print(a + b)
```

Cr茅ez ensuite la d茅finition d'outil qui l'invoque :

```ts title=".opencode/tools/python-add.ts" {10}
import { tool } from "@openagt/plugin"
import path from "path"

export default tool({
  description: "Add two numbers using Python",
  args: {
    a: tool.schema.number().describe("First number"),
    b: tool.schema.number().describe("Second number"),
  },
  async execute(args, context) {
    const script = path.join(context.worktree, ".opencode/tools/add.py")
    const result = await Bun.$`python3 ${script} ${args.a} ${args.b}`.text()
    return result.trim()
  },
})
```

Ici, nous utilisons l'utilitaire [`Bun.$`](https://bun.com/docs/runtime/shell) pour ex茅cuter le script Python.
