---
title: SDK
description: Client JS de type s茅curis茅 pour le serveur opencode.
---

import config from "../../../../config.mjs"
export const typesUrl = `${config.github}/blob/dev/packages/sdk/js/src/gen/types.gen.ts`

Le SDK opencode JS/TS fournit un client de type s茅curis茅 pour interagir avec le serveur.
Utilisez-le pour cr茅er des int茅grations et contr么ler opencode par programme.

[En savoir plus](/docs/server) sur le fonctionnement du serveur. Pour des exemples, consultez les [projects](/docs/ecosystem#projects) construits par la communaut茅.

---

## Installer

Installez le SDK 脿 partir de npm聽:

```bash
npm install @openagt/sdk
```

---

## Cr茅er un client

Cr茅ez une instance de opencode聽:

```javascript
import { createOpencode } from "@openagt/sdk"

const { client } = await createOpencode()
```

Cela d茅marre 脿 la fois un serveur et un client

#### Options

| Options    | Tapez         | Descriptif                                         | Par d茅faut  |
| ---------- | ------------- | -------------------------------------------------- | ----------- |
| `hostname` | `string`      | Nom d'h么te du serveur                              | `127.0.0.1` |
| `port`     | `number`      | Port du serveur                                    | `4096`      |
| `signal`   | `AbortSignal` | Signal d'abandon pour annulation                   | `undefined` |
| `timeout`  | `number`      | D茅lai d'attente en ms pour le d茅marrage du serveur | `5000`      |
| `config`   | `Config`      | Objet de configuration                             | `{}`        |

---

## Configuration

Vous pouvez transmettre un objet de configuration pour personnaliser le comportement. L'instance r茅cup猫re toujours votre `opencode.json`, mais vous pouvez remplacer ou ajouter une configuration en ligne聽:

```javascript
import { createOpencode } from "@openagt/sdk"

const opencode = await createOpencode({
  hostname: "127.0.0.1",
  port: 4096,
  config: {
    model: "anthropic/claude-3-5-sonnet-20241022",
  },
})

console.log(`Server running at ${opencode.server.url}`)

opencode.server.close()
```

## Client uniquement

Si vous disposez d茅j脿 d'une instance en cours d'ex茅cution de opencode, vous pouvez cr茅er une instance client pour vous y connecter聽:

```javascript
import { createOpencodeClient } from "@openagt/sdk"

const client = createOpencodeClient({
  baseUrl: "http://localhost:4096",
})
```

#### Options

| Options         | Tapez      | Descriptif                                   | Par d茅faut              |
| --------------- | ---------- | -------------------------------------------- | ----------------------- |
| `baseUrl`       | `string`   | URL du serveur                               | `http://localhost:4096` |
| `fetch`         | `function` | Impl茅mentation de r茅cup茅ration personnalis茅e | `globalThis.fetch`      |
| `parseAs`       | `string`   | M茅thode d'analyse des r茅ponses               | `auto`                  |
| `responseStyle` | `string`   | Style de retour聽: `data` ou `fields`         | `fields`                |
| `throwOnError`  | `boolean`  | Lancez des erreurs au lieu de return         | `false`                 |

---

## Types

Le SDK inclut des d茅finitions TypeScript pour tous les types API. Importez-les directement聽:

```typescript
import type { Session, Message, Part } from "@openagt/sdk"
```

Tous les types sont g茅n茅r茅s 脿 partir de la sp茅cification OpenAPI du serveur et disponibles dans le <a href={typesUrl}>fichier de types</a>.

---

## Erreurs

Le SDK peut g茅n茅rer des erreurs que vous pouvez d茅tecter et g茅rer聽:

```typescript
try {
  await client.session.get({ path: { id: "invalid-id" } })
} catch (error) {
  console.error("Failed to get session:", (error as Error).message)
}
```

---

## Sortie structur茅e

Vous pouvez demander une sortie JSON structur茅e au mod猫le en sp茅cifiant un `format` avec un sch茅ma JSON. Le mod猫le utilisera un outil `StructuredOutput` pour renvoyer un JSON valid茅 correspondant 脿 votre sch茅ma.

### Utilisation de base

```typescript
const result = await client.session.prompt({
  path: { id: sessionId },
  body: {
    parts: [{ type: "text", text: "Research Anthropic and provide company info" }],
    format: {
      type: "json_schema",
      schema: {
        type: "object",
        properties: {
          company: { type: "string", description: "Company name" },
          founded: { type: "number", description: "Year founded" },
          products: {
            type: "array",
            items: { type: "string" },
            description: "Main products",
          },
        },
        required: ["company", "founded"],
      },
    },
  },
})

// Access the structured output
console.log(result.data.info.structured_output)
// { company: "Anthropic", founded: 2021, products: ["Claude", "Claude API"] }
```

### Types de format de sortie

| Type          | Description                                                       |
| ------------- | ----------------------------------------------------------------- |
| `text`        | Par d茅faut. R茅ponse textuelle standard (pas de sortie structur茅e) |
| `json_schema` | Renvoie un JSON valid茅 correspondant au sch茅ma fourni             |

### Format de sch茅ma JSON

Lors de l'utilisation de `type: 'json_schema'`, fournissez :

| Champ        | Type            | Description                                                     |
| ------------ | --------------- | --------------------------------------------------------------- |
| `type`       | `'json_schema'` | Requis. Sp茅cifie le mode de sch茅ma JSON                         |
| `schema`     | `object`        | Requis. Objet JSON Schema d茅finissant la structure de sortie    |
| `retryCount` | `number`        | Facultatif. Nombre de tentatives de validation (par d茅faut : 2) |

### Gestion des erreurs

Si le mod猫le ne parvient pas 脿 produire une sortie structur茅e valide apr猫s toutes les tentatives, la r茅ponse inclura une `StructuredOutputError` :

```typescript
if (result.data.info.error?.name === "StructuredOutputError") {
  console.error("Failed to produce structured output:", result.data.info.error.message)
  console.error("Attempts:", result.data.info.error.retries)
}
```

### Bonnes pratiques

1. **Fournissez des descriptions claires** dans les propri茅t茅s de votre sch茅ma pour aider le mod猫le 脿 comprendre quelles donn茅es extraire
2. **Utilisez `required`** pour sp茅cifier quels champs doivent 锚tre pr茅sents
3. **Gardez les sch茅mas cibl茅s** - les sch茅mas imbriqu茅s complexes peuvent 锚tre plus difficiles 脿 remplir correctement pour le mod猫le
4. **D茅finissez un `retryCount` appropri茅** - augmentez-le pour les sch茅mas complexes, diminuez-le pour les simples

---

## APIs

Le SDK expose toutes les API du serveur via un client de type s茅curis茅.

---

### Mondial

| M茅thode           | Descriptif                               | R茅ponse                              |
| ----------------- | ---------------------------------------- | ------------------------------------ |
| `global.health()` | V茅rifier l'茅tat et la version du serveur | `{ healthy: true, version: string }` |

---

#### Exemples

```javascript
const health = await client.global.health()
console.log(health.data.version)
```

---

### Application

| M茅thode        | Descriptif                        | R茅ponse                                     |
| -------------- | --------------------------------- | ------------------------------------------- |
| `app.log()`    | 脡crire une entr茅e de journal      | `boolean`                                   |
| `app.agents()` | Liste tous les agents disponibles | <a href={typesUrl}><code>Agent[]</code></a> |

---

#### Exemples

```javascript
// Write a log entry
await client.app.log({
  body: {
    service: "my-app",
    level: "info",
    message: "Operation completed",
  },
})

// List available agents
const agents = await client.app.agents()
```

---

### Projet

| M茅thode             | Descriptif                 | R茅ponse                                       |
| ------------------- | -------------------------- | --------------------------------------------- |
| `project.list()`    | Lister tous les projets    | <a href={typesUrl}><code>Project[]</code></a> |
| `project.current()` | Obtenir le projet en cours | <a href={typesUrl}><code>Project</code></a>   |

---

#### Exemples

```javascript
// List all projects
const projects = await client.project.list()

// Get current project
const currentProject = await client.project.current()
```

---

### Chemin

| M茅thode      | Descriptif               | R茅ponse                                  |
| ------------ | ------------------------ | ---------------------------------------- |
| `path.get()` | Obtenir le chemin actuel | <a href={typesUrl}><code>Path</code></a> |

---

#### Exemples

```javascript
// Get current path information
const pathInfo = await client.path.get()
```

---

### Configuration

| M茅thode              | Descriptif                                   | R茅ponse                                                                                               |
| -------------------- | -------------------------------------------- | ----------------------------------------------------------------------------------------------------- |
| `config.get()`       | Obtenir des informations de configuration    | <a href={typesUrl}><code>Config</code></a>                                                            |
| `config.providers()` | Liste des fournisseurs et mod猫les par d茅faut | `{ providers: `<a href={typesUrl}><code>Provider[]</code></a>`, default: { [key: string]: string } }` |

---

#### Exemples

```javascript
const config = await client.config.get()

const { providers, default: defaults } = await client.config.providers()
```

---

### S茅ances

| M茅thode                                                    | Descriptif                                  | Remarques                                                                                                                                                                                                                                           |
| ---------------------------------------------------------- | ------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `session.list()`                                           | Liste des s茅ances                           | Renvoie <a href={typesUrl}><code>Session[]</code></a>                                                                                                                                                                                               |
| `session.get({ path })`                                    | Obtenir une session                         | Renvoie <a href={typesUrl}><code>Session</code></a>                                                                                                                                                                                                 |
| `session.children({ path })`                               | Liste des sessions enfants                  | Renvoie <a href={typesUrl}><code>Session[]</code></a>                                                                                                                                                                                               |
| `session.create({ body })`                                 | Cr茅er une s茅ance                            | Renvoie <a href={typesUrl}><code>Session</code></a>                                                                                                                                                                                                 |
| `session.delete({ path })`                                 | Supprimer la s茅ance                         | Renvoie `boolean`                                                                                                                                                                                                                                   |
| `session.update({ path, body })`                           | Mettre 脿 jour les propri茅t茅s de la session  | Renvoie <a href={typesUrl}><code>Session</code></a>                                                                                                                                                                                                 |
| `session.init({ path, body })`                             | Analysez l'application et cr茅ez `AGENTS.md` | Renvoie `boolean`                                                                                                                                                                                                                                   |
| `session.abort({ path })`                                  | Abandonner une session en cours             | Renvoie `boolean`                                                                                                                                                                                                                                   |
| `session.share({ path })`                                  | S茅ance de partage                           | Renvoie <a href={typesUrl}><code>Session</code></a>                                                                                                                                                                                                 |
| `session.unshare({ path })`                                | Annuler le partage de la session            | Renvoie <a href={typesUrl}><code>Session</code></a>                                                                                                                                                                                                 |
| `session.summarize({ path, body })`                        | R茅sumer la s茅ance                           | Renvoie `boolean`                                                                                                                                                                                                                                   |
| `session.messages({ path })`                               | Liste des messages dans une session         | Renvoie `{ info: `<a href={typesUrl}><code>Message</code></a>`, parts: `<a href={typesUrl}><code>Part[]</code></a>`}[]`                                                                                                                             |
| `session.message({ path })`                                | Obtenir les d茅tails du message              | Renvoie `{ info: `<a href={typesUrl}><code>Message</code></a>`, parts: `<a href={typesUrl}><code>Part[]</code></a>`}`                                                                                                                               |
| `session.prompt({ path, body })`                           | Envoyer un message d'invite                 | `body.noReply: true` renvoie UserMessage (contexte uniquement). La valeur par d茅faut renvoie <a href={typesUrl}><code>AssistantMessage</code></a> avec r茅ponse IA. Prend en charge `body.outputFormat` pour [sortie structur茅e](#structured-output) |
| `session.command({ path, body })`                          | Envoyer la commande 脿 la session            | Renvoie `{ info: `<a href={typesUrl}><code>AssistantMessage</code></a>`, parts: `<a href={typesUrl}><code>Part[]</code></a>`}`                                                                                                                      |
| `session.shell({ path, body })`                            | Ex茅cuter une commande shell                 | Renvoie <a href={typesUrl}><code>AssistantMessage</code></a>                                                                                                                                                                                        |
| `session.revert({ path, body })`                           | R茅tablir un message                         | Renvoie <a href={typesUrl}><code>Session</code></a>                                                                                                                                                                                                 |
| `session.unrevert({ path })`                               | Restaurer les messages annul茅s              | Renvoie <a href={typesUrl}><code>Session</code></a>                                                                                                                                                                                                 |
| `postSessionByIdPermissionsByPermissionId({ path, body })` | R茅pondre 脿 une demande d'autorisation       | Renvoie `boolean`                                                                                                                                                                                                                                   |

---

#### Exemples

```javascript
// Create and manage sessions
const session = await client.session.create({
  body: { title: "My session" },
})

const sessions = await client.session.list()

// Send a prompt message
const result = await client.session.prompt({
  path: { id: session.id },
  body: {
    model: { providerID: "anthropic", modelID: "claude-3-5-sonnet-20241022" },
    parts: [{ type: "text", text: "Hello!" }],
  },
})

// Inject context without triggering AI response (useful for plugins)
await client.session.prompt({
  path: { id: session.id },
  body: {
    noReply: true,
    parts: [{ type: "text", text: "You are a helpful assistant." }],
  },
})
```

---

### Fichiers

| M茅thode                   | Descriptif                                         | R茅ponse                                                                                             |
| ------------------------- | -------------------------------------------------- | --------------------------------------------------------------------------------------------------- |
| `find.text({ query })`    | Rechercher du texte dans des fichiers              | Tableau d'objets correspondant avec `path`, `lines`, `line_number`, `absolute_offset`, `submatches` |
| `find.files({ query })`   | Rechercher des fichiers et des r茅pertoires par nom | `string[]` (chemins)                                                                                |
| `find.symbols({ query })` | Rechercher des symboles d'espace de travail        | <a href={typesUrl}><code>Symbol[]</code></a>                                                        |
| `file.read({ query })`    | Lire un fichier                                    | `{ type: "raw" \| "patch", content: string }`                                                       |
| `file.status({ query? })` | Obtenir le statut des fichiers suivis              | <a href={typesUrl}><code>File[]</code></a>                                                          |

`find.files` prend en charge quelques champs de requ锚te facultatifs聽:

- `type`聽: `"file"` ou `"directory"`
- `directory`聽: remplace la racine du projet pour la recherche
- `limit`聽: r茅sultats maximum (1 脿 200)

---

#### Exemples

```javascript
// Search and read files
const textResults = await client.find.text({
  query: { pattern: "function.*opencode" },
})

const files = await client.find.files({
  query: { query: "*.ts", type: "file" },
})

const directories = await client.find.files({
  query: { query: "packages", type: "directory", limit: 20 },
})

const content = await client.file.read({
  query: { path: "src/index.ts" },
})
```

---

### TUI

| M茅thode                        | Descriptif                         | R茅ponse   |
| ------------------------------ | ---------------------------------- | --------- |
| `tui.appendPrompt({ body })`   | Ajouter du texte 脿 l'invite        | `boolean` |
| `tui.openHelp()`               | Ouvrir la bo卯te de dialogue d'aide | `boolean` |
| `tui.openSessions()`           | Ouvrez le s茅lecteur de session     | `boolean` |
| `tui.openThemes()`             | Ouvrez le s茅lecteur de th猫me       | `boolean` |
| `tui.openModels()`             | Ouvrez le s茅lecteur de mod猫le      | `boolean` |
| `tui.submitPrompt()`           | Soumettre l'invite actuelle        | `boolean` |
| `tui.clearPrompt()`            | Effacez l'invite                   | `boolean` |
| `tui.executeCommand({ body })` | Ex茅cuter une commande              | `boolean` |
| `tui.showToast({ body })`      | Afficher la notification toast     | `boolean` |

---

#### Exemples

```javascript
// Control TUI interface
await client.tui.appendPrompt({
  body: { text: "Add this to prompt" },
})

await client.tui.showToast({
  body: { message: "Task completed", variant: "success" },
})
```

---

### Authentification

| M茅thode             | Descriptif                                  | R茅ponse   |
| ------------------- | ------------------------------------------- | --------- |
| `auth.set({ ... })` | D茅finir les informations d'authentification | `boolean` |

---

#### Exemples

```javascript
await client.auth.set({
  path: { id: "anthropic" },
  body: { type: "api", key: "your-api-key" },
})
```

---

### 脡v茅nements

| M茅thode             | Descriptif                               | R茅ponse                                  |
| ------------------- | ---------------------------------------- | ---------------------------------------- |
| `event.subscribe()` | Flux d'茅v茅nements envoy茅s par le serveur | Flux d'茅v茅nements envoy茅s par le serveur |

---

#### Exemples

```javascript
// Listen to real-time events
const events = await client.event.subscribe()
for await (const event of events.stream) {
  console.log("Event:", event.type, event.properties)
}
```
