---
title: Plugins
description: 脡crivez vos propres plugins pour 茅tendre OpenAGt.
---

Les plugins vous permettent d'茅tendre OpenAGt en vous connectant 脿 divers 茅v茅nements et en personnalisant le comportement. Vous pouvez cr茅er des plugins pour ajouter de nouvelles fonctionnalit茅s, int茅grer des services externes ou modifier le comportement par d茅faut de OpenAGt.

Pour des exemples, consultez les [plugins](/docs/ecosystem#plugins) cr茅茅s par la communaut茅.

---

## Utiliser un plugin

Il existe deux mani猫res de charger des plugins.

---

### 脌 partir de fichiers locaux

Placez les fichiers JavaScript ou TypeScript dans le r茅pertoire du plugin.

- `.opencode/plugins/` - Plugins au niveau du projet
- `~/.config/opencode/plugins/` - Plugins globaux

Les fichiers de ces r茅pertoires sont automatiquement charg茅s au d茅marrage.

---

### Depuis npm

Sp茅cifiez les packages npm dans votre fichier de configuration.

```json title="opencode.json"
{
  "$schema": "https://github.com/Yecyi/OpenAGt/raw/dev/packages/web/dist/config.json",
  "plugin": ["opencode-helicone-session", "opencode-wakatime", "@my-org/custom-plugin"]
}
```

Les packages npm scop茅s et standards sont pris en charge.

Parcourez les plugins disponibles dans le [ecosystem](/docs/ecosystem#plugins).

---

### Comment les plugins sont install茅s

Les **plugins npm** sont install茅s automatiquement 脿 l'aide de Bun au d茅marrage. Les packages et leurs d茅pendances sont mis en cache dans `~/.cache/opencode/node_modules/`.

Les **plugins locaux** sont charg茅s directement depuis le r茅pertoire des plugins. Pour utiliser des packages externes, vous devez cr茅er un `package.json` dans votre r茅pertoire de configuration (voir [D茅pendances](#dependencies)), ou publier le plugin sur npm et [l'ajouter 脿 votre config](/docs/config#plugins).

---

### Ordre de chargement

Les plugins sont charg茅s 脿 partir de toutes les sources et tous les hooks s'ex茅cutent dans l'ordre. L'ordre de chargement est le suivant :

1. Configuration globale (`~/.config/opencode/opencode.json`)
2. Configuration du projet (`opencode.json`)
3. R茅pertoire global des plugins (`~/.config/opencode/plugins/`)
4. R茅pertoire des plugins du projet (`.opencode/plugins/`)

Les packages npm en double avec le m锚me nom et la m锚me version sont charg茅s une fois. Cependant, un plugin local et un plugin npm portant des noms similaires sont tous deux charg茅s s茅par茅ment.

---

## Cr茅er un plugin

Un plugin est un **module JavaScript/TypeScript** qui exporte une ou plusieurs fonctions de plugin. Chaque fonction re莽oit un objet contextuel et renvoie un objet hooks.

---

### D茅pendances

Les plugins locaux et les outils personnalis茅s peuvent utiliser des packages npm externes. Ajoutez un `package.json` 脿 votre r茅pertoire de configuration avec les d茅pendances dont vous avez besoin.

```json title=".opencode/package.json"
{
  "dependencies": {
    "shescape": "^2.1.0"
  }
}
```

OpenAGt ex茅cute `bun install` au d茅marrage pour les installer. Vos plugins et outils peuvent ensuite les importer.

```ts title=".opencode/plugins/my-plugin.ts"
import { escape } from "shescape"

export const MyPlugin = async (ctx) => {
  return {
    "tool.execute.before": async (input, output) => {
      if (input.tool === "bash") {
        output.args.command = escape(output.args.command)
      }
    },
  }
}
```

---

### Structure de base

```js title=".opencode/plugins/example.js"
export const MyPlugin = async ({ project, client, $, directory, worktree }) => {
  console.log("Plugin initialized!")

  return {
    // Hook implementations go here
  }
}
```

La fonction plugin re莽oit :

- `project` : informations sur le projet actuel.
- `directory` : le r茅pertoire de travail actuel.
- `worktree` : le chemin de l'arbre de travail git.
- `client` : un client SDK opencode pour interagir avec l'IA.
- `$` : [shell API](https://bun.com/docs/runtime/shell) de Bun pour l'ex茅cution de commandes.

---

### Prise en charge de TypeScript

Pour les plugins TypeScript, vous pouvez importer des types 脿 partir du package du plugin :

```ts title="my-plugin.ts" {1}
import type { Plugin } from "@openagt/plugin"

export const MyPlugin: Plugin = async ({ project, client, $, directory, worktree }) => {
  return {
    // Type-safe hook implementations
  }
}
```

---

### 脡v茅nements

Les plugins peuvent s'abonner 脿 des 茅v茅nements comme indiqu茅 ci-dessous dans la section Exemples. Voici une liste des diff茅rents 茅v茅nements disponibles.

#### 脡v茅nements de commande

- `command.executed`

#### 脡v茅nements de fichier

- `file.edited`
- `file.watcher.updated`

#### 脡v茅nements d'installation

- `installation.updated`

#### LSP 脡v茅nements

- `lsp.client.diagnostics`
- `lsp.updated`

#### 脡v茅nements de messages

- `message.part.removed`
- `message.part.updated`
- `message.removed`
- `message.updated`

#### 脡v茅nements d'autorisation

- `permission.asked`
- `permission.replied`

#### 脡v茅nements du serveur

- `server.connected`

#### 脡v茅nements de session

- `session.created`
- `session.compacted`
- `session.deleted`
- `session.diff`
- `session.error`
- `session.idle`
- `session.status`
- `session.updated`

#### 脡v茅nements 脿 faire

- `todo.updated`

#### 脡v茅nements Shell

- `shell.env`

#### 脡v茅nements d'outils

- `tool.execute.after`
- `tool.execute.before`

#### TUI 脡v茅nements

- `tui.prompt.append`
- `tui.command.execute`
- `tui.toast.show`

---

## Exemples

Voici quelques exemples de plugins que vous pouvez utiliser pour 茅tendre opencode.

---

### Envoyer des notifications

Envoyez des notifications lorsque certains 茅v茅nements se produisent :

```js title=".opencode/plugins/notification.js"
export const NotificationPlugin = async ({ project, client, $, directory, worktree }) => {
  return {
    event: async ({ event }) => {
      // Send notification on session completion
      if (event.type === "session.idle") {
        await $`osascript -e 'display notification "Session completed!" with title "opencode"'`
      }
    },
  }
}
```

Nous utilisons `osascript` pour ex茅cuter AppleScript sur macOS. Ici, nous l'utilisons pour envoyer des notifications.

:::note
Si vous utilisez l'application de bureau OpenAGt, elle peut envoyer automatiquement des notifications syst猫me lorsqu'une r茅ponse est pr锚te ou en cas d'erreur de session.
:::

---

### Protection .env

Emp锚chez opencode de lire les fichiers `.env` :

```javascript title=".opencode/plugins/env-protection.js"
export const EnvProtection = async ({ project, client, $, directory, worktree }) => {
  return {
    "tool.execute.before": async (input, output) => {
      if (input.tool === "read" && output.args.filePath.includes(".env")) {
        throw new Error("Do not read .env files")
      }
    },
  }
}
```

---

### Injecter des variables d'environnement

Injectez des variables d'environnement dans toutes les ex茅cutions du shell (outils d'IA et terminal utilisateur) :

```javascript title=".opencode/plugins/inject-env.js"
export const InjectEnvPlugin = async () => {
  return {
    "shell.env": async (input, output) => {
      output.env.MY_API_KEY = "secret"
      output.env.PROJECT_ROOT = input.cwd
    },
  }
}
```

---

### Outils personnalis茅s

Les plugins peuvent 茅galement ajouter des outils personnalis茅s 脿 opencode :

```ts title=".opencode/plugins/custom-tools.ts"
import { type Plugin, tool } from "@openagt/plugin"

export const CustomToolsPlugin: Plugin = async (ctx) => {
  return {
    tool: {
      mytool: tool({
        description: "This is a custom tool",
        args: {
          foo: tool.schema.string(),
        },
        async execute(args, context) {
          const { directory, worktree } = context
          return `Hello ${args.foo} from ${directory} (worktree: ${worktree})`
        },
      }),
    },
  }
}
```

L'assistant `tool` cr茅e un outil personnalis茅 que opencode peut appeler. Il prend une fonction de sch茅ma Zod et renvoie une d茅finition d'outil avec :

- `description` : ce que fait l'outil
- `args` : sch茅ma Zod pour les arguments de l'outil
- `execute` : Fonction qui s'ex茅cute lorsque l'outil est appel茅

Vos outils personnalis茅s seront disponibles pour opencode aux c么t茅s des outils int茅gr茅s.

---

### Journalisation

Utilisez `client.app.log()` au lieu de `console.log` pour la journalisation structur茅e :

```ts title=".opencode/plugins/my-plugin.ts"
export const MyPlugin = async ({ client }) => {
  await client.app.log({
    body: {
      service: "my-plugin",
      level: "info",
      message: "Plugin initialized",
      extra: { foo: "bar" },
    },
  })
}
```

Niveaux : `debug`, `info`, `warn`, `error`. Voir la [documentation du SDK](https://github.com/Yecyi/OpenAGt/docs/sdk) pour plus de d茅tails.

---

### Hooks de compactage

Personnalisez le contexte inclus lorsqu'une session est compact茅e :

```ts title=".opencode/plugins/compaction.ts"
import type { Plugin } from "@openagt/plugin"

export const CompactionPlugin: Plugin = async (ctx) => {
  return {
    "experimental.session.compacting": async (input, output) => {
      // Inject additional context into the compaction prompt
      output.context.push(`
## Custom Context

Include any state that should persist across compaction:
- Current task status
- Important decisions made
- Files being actively worked on
`)
    },
  }
}
```

Le hook `experimental.session.compacting` se d茅clenche avant que le LLM ne g茅n猫re un r茅sum茅 de continuation. Utilisez-le pour injecter un contexte sp茅cifique au domaine que le prompt de compactage par d茅faut manquerait.

Vous pouvez 茅galement remplacer enti猫rement le prompt de compactage en d茅finissant `output.prompt` :

```ts title=".opencode/plugins/custom-compaction.ts"
import type { Plugin } from "@openagt/plugin"

export const CustomCompactionPlugin: Plugin = async (ctx) => {
  return {
    "experimental.session.compacting": async (input, output) => {
      // Replace the entire compaction prompt
      output.prompt = `
You are generating a continuation prompt for a multi-agent swarm session.

Summarize:
1. The current task and its status
2. Which files are being modified and by whom
3. Any blockers or dependencies between agents
4. The next steps to complete the work

Format as a structured prompt that a new agent can use to resume work.
`
    },
  }
}
```

Lorsque `output.prompt` est d茅fini, il remplace compl猫tement le prompt de compactage par d茅faut. Le tableau `output.context` est ignor茅 dans ce cas.
