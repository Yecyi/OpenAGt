---
title: Herramientas personalizadas
description: Cree herramientas que LLM pueda llamar en opencode.
---

Las herramientas personalizadas son funciones que usted crea y que el LLM puede llamar durante las conversaciones. Trabajan junto con las [herramientas integradas](/docs/tools) de opencode como `read`, `write` y `bash`.

---

## Crear una herramienta

Las herramientas se definen como archivos **TypeScript** o **JavaScript**. Sin embargo, la definici贸n de la herramienta puede invocar secuencias de comandos escritas en **cualquier idioma**: TypeScript o JavaScript solo se utilizan para la definici贸n de la herramienta en s铆.

---

### Ubicaci贸n

Se pueden definir:

- Localmente coloc谩ndolos en el directorio `.opencode/tools/` de tu proyecto.
- O de forma global, coloc谩ndolos en `~/.config/opencode/tools/`.

---

### Estructura

La forma m谩s sencilla de crear herramientas es utilizar el asistente `tool()` que proporciona seguridad de tipos y validaci贸n.

```ts title=".opencode/tools/database.ts" {1}
import { tool } from "@openagt/plugin"

export default tool({
  description: "Query the project database",
  args: {
    query: tool.schema.string().describe("SQL query to execute"),
  },
  async execute(args) {
    // Your database logic here
    return `Executed query: ${args.query}`
  },
})
```

El **nombre de archivo** se convierte en el **nombre de la herramienta**. Lo anterior crea una herramienta `database`.

---

#### M煤ltiples herramientas por archivo

Tambi茅n puede exportar varias herramientas desde un solo archivo. Cada exportaci贸n se convierte en **una herramienta independiente** con el nombre **`<filename>_<exportname>`**:

```ts title=".opencode/tools/math.ts"
import { tool } from "@openagt/plugin"

export const add = tool({
  description: "Add two numbers",
  args: {
    a: tool.schema.number().describe("First number"),
    b: tool.schema.number().describe("Second number"),
  },
  async execute(args) {
    return args.a + args.b
  },
})

export const multiply = tool({
  description: "Multiply two numbers",
  args: {
    a: tool.schema.number().describe("First number"),
    b: tool.schema.number().describe("Second number"),
  },
  async execute(args) {
    return args.a * args.b
  },
})
```

Esto crea dos herramientas: `math_add` y `math_multiply`.

---

#### Colisiones de nombres con herramientas integradas

Las herramientas personalizadas est谩n codificadas por el nombre de la herramienta. Si una herramienta personalizada utiliza el mismo nombre que una herramienta integrada, la herramienta personalizada tiene prioridad.

Por ejemplo, este archivo reemplaza la herramienta `bash` integrada:

```ts title=".opencode/tools/bash.ts"
import { tool } from "@openagt/plugin"

export default tool({
  description: "Restricted bash wrapper",
  args: {
    command: tool.schema.string(),
  },
  async execute(args) {
    return `blocked: ${args.command}`
  },
})
```

:::note
Prefiera nombres 煤nicos a menos que intencionalmente desee reemplazar una herramienta integrada. Si desea deshabilitar una herramienta integrada pero no anularla, use [permisos](/docs/permissions).
:::

---

### Argumentos

Puedes usar `tool.schema`, que es simplemente [Zod](https://zod.dev), para definir tipos de argumentos.

```ts "tool.schema"
args: {
  query: tool.schema.string().describe("SQL query to execute")
}
```

Tambi茅n puedes importar [Zod](https://zod.dev) directamente y devolver un objeto simple:

```ts {6}
import { z } from "zod"

export default {
  description: "Tool description",
  args: {
    param: z.string().describe("Parameter description"),
  },
  async execute(args, context) {
    // Tool implementation
    return "result"
  },
}
```

---

### Contexto

Las herramientas reciben contexto sobre la sesi贸n actual:

```ts title=".opencode/tools/project.ts" {8}
import { tool } from "@openagt/plugin"

export default tool({
  description: "Get project information",
  args: {},
  async execute(args, context) {
    // Access context information
    const { agent, sessionID, messageID, directory, worktree } = context
    return `Agent: ${agent}, Session: ${sessionID}, Message: ${messageID}, Directory: ${directory}, Worktree: ${worktree}`
  },
})
```

Utilice `context.directory` para el directorio de trabajo de la sesi贸n.
Utilice `context.worktree` para la ra铆z del 谩rbol de trabajo de git.

---

## Ejemplos

### Escribir una herramienta en Python

Puede escribir sus herramientas en cualquier idioma que desee. Aqu铆 hay un ejemplo que suma dos n煤meros usando Python.

Primero, cree la herramienta como un script de Python:

```python title=".opencode/tools/add.py"
import sys

a = int(sys.argv[1])
b = int(sys.argv[2])
print(a + b)
```

Luego cree la definici贸n de herramienta que la invoca:

```ts title=".opencode/tools/python-add.ts" {10}
import { tool } from "@openagt/plugin"
import path from "path"

export default tool({
  description: "Add two numbers using Python",
  args: {
    a: tool.schema.number().describe("First number"),
    b: tool.schema.number().describe("Second number"),
  },
  async execute(args, context) {
    const script = path.join(context.worktree, ".opencode/tools/add.py")
    const result = await Bun.$`python3 ${script} ${args.a} ${args.b}`.text()
    return result.trim()
  },
})
```

Aqu铆 estamos usando la utilidad [`Bun.$`](https://bun.com/docs/runtime/shell) para ejecutar el script Python.
