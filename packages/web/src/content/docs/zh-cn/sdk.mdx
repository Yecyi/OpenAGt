---
title: SDK
description: opencode 鏈嶅姟鍣ㄧ殑绫诲瀷瀹夊叏 JS 瀹㈡埛绔€?
---

import config from "../../../../config.mjs"
export const typesUrl = `${config.github}/blob/dev/packages/sdk/js/src/gen/types.gen.ts`

opencode JS/TS SDK 鎻愪緵浜嗕竴涓被鍨嬪畨鍏ㄧ殑瀹㈡埛绔紝鐢ㄤ簬涓庢湇鍔″櫒杩涜浜や簰銆?
浣犲彲浠ョ敤瀹冩潵鏋勫缓闆嗘垚鏂规锛屽苟浠ョ紪绋嬫柟寮忔帶鍒?opencode銆?

[浜嗚В鏇村](/docs/server)鍏充簬鏈嶅姟鍣ㄧ殑宸ヤ綔鍘熺悊銆傚闇€绀轰緥锛岃鏌ョ湅绀惧尯鏋勫缓鐨刐椤圭洰](/docs/ecosystem#projects)銆?

---

## 瀹夎

浠?npm 瀹夎 SDK锛?

```bash
npm install @openagt/sdk
```

---

## 鍒涘缓瀹㈡埛绔?

鍒涘缓涓€涓?opencode 瀹炰緥锛?

```javascript
import { createOpencode } from "@openagt/sdk"

const { client } = await createOpencode()
```

杩欎細鍚屾椂鍚姩鏈嶅姟鍣ㄥ拰瀹㈡埛绔€?

#### 閫夐」

| 閫夐」       | 绫诲瀷          | 鎻忚堪                       | 榛樿鍊?     |
| ---------- | ------------- | -------------------------- | ----------- |
| `hostname` | `string`      | 鏈嶅姟鍣ㄤ富鏈哄悕               | `127.0.0.1` |
| `port`     | `number`      | 鏈嶅姟鍣ㄧ鍙?                | `4096`      |
| `signal`   | `AbortSignal` | 鐢ㄤ簬鍙栨秷鎿嶄綔鐨勪腑姝俊鍙?    | `undefined` |
| `timeout`  | `number`      | 鏈嶅姟鍣ㄥ惎鍔ㄨ秴鏃舵椂闂达紙姣锛?| `5000`      |
| `config`   | `Config`      | 閰嶇疆瀵硅薄                   | `{}`        |

---

## 閰嶇疆

浣犲彲浠ヤ紶鍏ヤ竴涓厤缃璞℃潵鑷畾涔夎涓恒€傚疄渚嬩粛鐒朵細璇诲彇浣犵殑 `opencode.json`锛屼絾浣犲彲浠ラ€氳繃鍐呰仈鏂瑰紡瑕嗙洊鎴栨坊鍔犻厤缃細

```javascript
import { createOpencode } from "@openagt/sdk"

const opencode = await createOpencode({
  hostname: "127.0.0.1",
  port: 4096,
  config: {
    model: "anthropic/claude-3-5-sonnet-20241022",
  },
})

console.log(`Server running at ${opencode.server.url}`)

opencode.server.close()
```

## 浠呭鎴风妯″紡

濡傛灉浣犲凡缁忔湁涓€涓鍦ㄨ繍琛岀殑 opencode 瀹炰緥锛屽彲浠ュ垱寤轰竴涓鎴风瀹炰緥鏉ヨ繛鎺ュ畠锛?

```javascript
import { createOpencodeClient } from "@openagt/sdk"

const client = createOpencodeClient({
  baseUrl: "http://localhost:4096",
})
```

#### 閫夐」

| 閫夐」            | 绫诲瀷       | 鎻忚堪                         | 榛樿鍊?                 |
| --------------- | ---------- | ---------------------------- | ----------------------- |
| `baseUrl`       | `string`   | 鏈嶅姟鍣?URL                   | `http://localhost:4096` |
| `fetch`         | `function` | 鑷畾涔?fetch 瀹炵幇            | `globalThis.fetch`      |
| `parseAs`       | `string`   | 鍝嶅簲瑙ｆ瀽鏂瑰紡                 | `auto`                  |
| `responseStyle` | `string`   | 杩斿洖椋庢牸锛歚data` 鎴?`fields` | `fields`                |
| `throwOnError`  | `boolean`  | 鎶涘嚭閿欒鑰岄潪杩斿洖閿欒         | `false`                 |

---

## 绫诲瀷

SDK 鍖呭惈鎵€鏈?API 绫诲瀷鐨?TypeScript 瀹氫箟銆備綘鍙互鐩存帴瀵煎叆瀹冧滑锛?

```typescript
import type { Session, Message, Part } from "@openagt/sdk"
```

鎵€鏈夌被鍨嬪潎鏍规嵁鏈嶅姟鍣ㄧ殑 OpenAPI 瑙勮寖鐢熸垚锛屽彲鍦?a href={typesUrl}>绫诲瀷鏂囦欢</a>涓煡鐪嬨€?

---

## 閿欒澶勭悊

SDK 鍙兘浼氭姏鍑洪敊璇紝浣犲彲浠ユ崟鑾峰苟澶勭悊杩欎簺閿欒锛?

```typescript
try {
  await client.session.get({ path: { id: "invalid-id" } })
} catch (error) {
  console.error("Failed to get session:", (error as Error).message)
}
```

---

## 缁撴瀯鍖栬緭鍑?

浣犲彲浠ラ€氳繃鎸囧畾甯︽湁 JSON Schema 鐨?`format` 鏉ヨ姹傛ā鍨嬭繑鍥炵粨鏋勫寲鐨?JSON 杈撳嚭銆傛ā鍨嬩細浣跨敤 `StructuredOutput` 宸ュ叿杩斿洖绗﹀悎浣?Schema 鐨勭粡杩囬獙璇佺殑 JSON銆?

### 鍩烘湰鐢ㄦ硶

```typescript
const result = await client.session.prompt({
  path: { id: sessionId },
  body: {
    parts: [{ type: "text", text: "Research Anthropic and provide company info" }],
    format: {
      type: "json_schema",
      schema: {
        type: "object",
        properties: {
          company: { type: "string", description: "Company name" },
          founded: { type: "number", description: "Year founded" },
          products: {
            type: "array",
            items: { type: "string" },
            description: "Main products",
          },
        },
        required: ["company", "founded"],
      },
    },
  },
})

// Access the structured output
console.log(result.data.info.structured_output)
// { company: "Anthropic", founded: 2021, products: ["Claude", "Claude API"] }
```

### 杈撳嚭鏍煎紡绫诲瀷

| 绫诲瀷          | 鎻忚堪                                    |
| ------------- | --------------------------------------- |
| `text`        | 榛樿鍊笺€傛爣鍑嗘枃鏈搷搴旓紙鏃犵粨鏋勫寲杈撳嚭锛?   |
| `json_schema` | 杩斿洖绗﹀悎鎵€鎻愪緵 Schema 鐨勭粡杩囬獙璇佺殑 JSON |

### JSON Schema 鏍煎紡

浣跨敤 `type: 'json_schema'` 鏃讹紝闇€鎻愪緵浠ヤ笅瀛楁锛?

| 瀛楁         | 绫诲瀷            | 鎻忚堪                                  |
| ------------ | --------------- | ------------------------------------- |
| `type`       | `'json_schema'` | 蹇呭～銆傛寚瀹?JSON Schema 妯″紡           |
| `schema`     | `object`        | 蹇呭～銆傚畾涔夎緭鍑虹粨鏋勭殑 JSON Schema 瀵硅薄 |
| `retryCount` | `number`        | 鍙€夈€傞獙璇侀噸璇曟鏁帮紙榛樿鍊硷細2锛?      |

### 閿欒澶勭悊

濡傛灉妯″瀷鍦ㄦ墍鏈夐噸璇曞悗浠嶆棤娉曠敓鎴愭湁鏁堢殑缁撴瀯鍖栬緭鍑猴紝鍝嶅簲涓細鍖呭惈 `StructuredOutputError`锛?

```typescript
if (result.data.info.error?.name === "StructuredOutputError") {
  console.error("Failed to produce structured output:", result.data.info.error.message)
  console.error("Attempts:", result.data.info.error.retries)
}
```

### 鏈€浣冲疄璺?

1. **鍦?Schema 灞炴€т腑鎻愪緵娓呮櫚鐨勬弿杩?*锛屽府鍔╂ā鍨嬬悊瑙ｉ渶瑕佹彁鍙栫殑鏁版嵁
2. **浣跨敤 `required`** 鎸囧畾鍝簺瀛楁蹇呴』瀛樺湪
3. **淇濇寔 Schema 绠€娲?* 鈥?澶嶆潅鐨勫祵濂?Schema 鍙兘浼氳妯″瀷鏇撮毦姝ｇ‘濉厖
4. **璁剧疆鍚堥€傜殑 `retryCount`** 鈥?瀵逛簬澶嶆潅 Schema 鍙鍔犻噸璇曟鏁帮紝瀵逛簬绠€鍗?Schema 鍙噺灏?

---

## API

SDK 閫氳繃绫诲瀷瀹夊叏鐨勫鎴风鏆撮湶鎵€鏈夋湇鍔″櫒 API銆?

---

### Global

| 鏂规硶              | 鎻忚堪                     | 鍝嶅簲                                 |
| ----------------- | ------------------------ | ------------------------------------ |
| `global.health()` | 妫€鏌ユ湇鍔″櫒鍋ュ悍鐘舵€佸拰鐗堟湰 | `{ healthy: true, version: string }` |

---

#### 绀轰緥

```javascript
const health = await client.global.health()
console.log(health.data.version)
```

---

### App

| 鏂规硶           | 鎻忚堪               | 鍝嶅簲                                        |
| -------------- | ------------------ | ------------------------------------------- |
| `app.log()`    | 鍐欏叆涓€鏉℃棩蹇?      | `boolean`                                   |
| `app.agents()` | 鍒楀嚭鎵€鏈夊彲鐢ㄧ殑浠ｇ悊 | <a href={typesUrl}><code>Agent[]</code></a> |

---

#### 绀轰緥

```javascript
// Write a log entry
await client.app.log({
  body: {
    service: "my-app",
    level: "info",
    message: "Operation completed",
  },
})

// List available agents
const agents = await client.app.agents()
```

---

### Project

| 鏂规硶                | 鎻忚堪         | 鍝嶅簲                                          |
| ------------------- | ------------ | --------------------------------------------- |
| `project.list()`    | 鍒楀嚭鎵€鏈夐」鐩?| <a href={typesUrl}><code>Project[]</code></a> |
| `project.current()` | 鑾峰彇褰撳墠椤圭洰 | <a href={typesUrl}><code>Project</code></a>   |

---

#### 绀轰緥

```javascript
// List all projects
const projects = await client.project.list()

// Get current project
const currentProject = await client.project.current()
```

---

### Path

| 鏂规硶         | 鎻忚堪         | 鍝嶅簲                                     |
| ------------ | ------------ | ---------------------------------------- |
| `path.get()` | 鑾峰彇褰撳墠璺緞 | <a href={typesUrl}><code>Path</code></a> |

---

#### 绀轰緥

```javascript
// Get current path information
const pathInfo = await client.path.get()
```

---

### Config

| 鏂规硶                 | 鎻忚堪                 | 鍝嶅簲                                                                                                  |
| -------------------- | -------------------- | ----------------------------------------------------------------------------------------------------- |
| `config.get()`       | 鑾峰彇閰嶇疆淇℃伅         | <a href={typesUrl}><code>Config</code></a>                                                            |
| `config.providers()` | 鍒楀嚭鎻愪緵鍟嗗拰榛樿妯″瀷 | `{ providers: `<a href={typesUrl}><code>Provider[]</code></a>`, default: { [key: string]: string } }` |

---

#### 绀轰緥

```javascript
const config = await client.config.get()

const { providers, default: defaults } = await client.config.providers()
```

---

### Sessions

| 鏂规硶                                                       | 鎻忚堪                       | 澶囨敞                                                                                                                                                                                           |
| ---------------------------------------------------------- | -------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `session.list()`                                           | 鍒楀嚭浼氳瘽                   | 杩斿洖 <a href={typesUrl}><code>Session[]</code></a>                                                                                                                                             |
| `session.get({ path })`                                    | 鑾峰彇浼氳瘽                   | 杩斿洖 <a href={typesUrl}><code>Session</code></a>                                                                                                                                               |
| `session.children({ path })`                               | 鍒楀嚭瀛愪細璇?                | 杩斿洖 <a href={typesUrl}><code>Session[]</code></a>                                                                                                                                             |
| `session.create({ body })`                                 | 鍒涘缓浼氳瘽                   | 杩斿洖 <a href={typesUrl}><code>Session</code></a>                                                                                                                                               |
| `session.delete({ path })`                                 | 鍒犻櫎浼氳瘽                   | 杩斿洖 `boolean`                                                                                                                                                                                 |
| `session.update({ path, body })`                           | 鏇存柊浼氳瘽灞炴€?              | 杩斿洖 <a href={typesUrl}><code>Session</code></a>                                                                                                                                               |
| `session.init({ path, body })`                             | 鍒嗘瀽搴旂敤骞跺垱寤?`AGENTS.md` | 杩斿洖 `boolean`                                                                                                                                                                                 |
| `session.abort({ path })`                                  | 涓姝ｅ湪杩愯鐨勪細璇?        | 杩斿洖 `boolean`                                                                                                                                                                                 |
| `session.share({ path })`                                  | 鍒嗕韩浼氳瘽                   | 杩斿洖 <a href={typesUrl}><code>Session</code></a>                                                                                                                                               |
| `session.unshare({ path })`                                | 鍙栨秷鍒嗕韩浼氳瘽               | 杩斿洖 <a href={typesUrl}><code>Session</code></a>                                                                                                                                               |
| `session.summarize({ path, body })`                        | 鎬荤粨浼氳瘽                   | 杩斿洖 `boolean`                                                                                                                                                                                 |
| `session.messages({ path })`                               | 鍒楀嚭浼氳瘽涓殑娑堟伅           | 杩斿洖 `{ info: `<a href={typesUrl}><code>Message</code></a>`, parts: `<a href={typesUrl}><code>Part[]</code></a>`}[]`                                                                           |
| `session.message({ path })`                                | 鑾峰彇娑堟伅璇︽儏               | 杩斿洖 `{ info: `<a href={typesUrl}><code>Message</code></a>`, parts: `<a href={typesUrl}><code>Part[]</code></a>`}`                                                                             |
| `session.prompt({ path, body })`                           | 鍙戦€佹彁绀鸿瘝娑堟伅             | `body.noReply: true` 杩斿洖 UserMessage锛堜粎娉ㄥ叆涓婁笅鏂囷級銆傞粯璁よ繑鍥炲甫鏈?AI 鍝嶅簲鐨?<a href={typesUrl}><code>AssistantMessage</code></a>銆傛敮鎸侀€氳繃 `body.outputFormat` 浣跨敤[缁撴瀯鍖栬緭鍑篯(#缁撴瀯鍖栬緭鍑? |
| `session.command({ path, body })`                          | 鍚戜細璇濆彂閫佸懡浠?            | 杩斿洖 `{ info: `<a href={typesUrl}><code>AssistantMessage</code></a>`, parts: `<a href={typesUrl}><code>Part[]</code></a>`}`                                                                    |
| `session.shell({ path, body })`                            | 鎵ц shell 鍛戒护            | 杩斿洖 <a href={typesUrl}><code>AssistantMessage</code></a>                                                                                                                                      |
| `session.revert({ path, body })`                           | 鎾ゅ洖娑堟伅                   | 杩斿洖 <a href={typesUrl}><code>Session</code></a>                                                                                                                                               |
| `session.unrevert({ path })`                               | 鎭㈠宸叉挙鍥炵殑娑堟伅           | 杩斿洖 <a href={typesUrl}><code>Session</code></a>                                                                                                                                               |
| `postSessionByIdPermissionsByPermissionId({ path, body })` | 鍝嶅簲鏉冮檺璇锋眰               | 杩斿洖 `boolean`                                                                                                                                                                                 |

---

#### 绀轰緥

```javascript
// Create and manage sessions
const session = await client.session.create({
  body: { title: "My session" },
})

const sessions = await client.session.list()

// Send a prompt message
const result = await client.session.prompt({
  path: { id: session.id },
  body: {
    model: { providerID: "anthropic", modelID: "claude-3-5-sonnet-20241022" },
    parts: [{ type: "text", text: "Hello!" }],
  },
})

// Inject context without triggering AI response (useful for plugins)
await client.session.prompt({
  path: { id: session.id },
  body: {
    noReply: true,
    parts: [{ type: "text", text: "You are a helpful assistant." }],
  },
})
```

---

### Files

| 鏂规硶                      | 鎻忚堪                 | 鍝嶅簲                                                                                |
| ------------------------- | -------------------- | ----------------------------------------------------------------------------------- |
| `find.text({ query })`    | 鎼滅储鏂囦欢涓殑鏂囨湰     | 鍖呭惈 `path`銆乣lines`銆乣line_number`銆乣absolute_offset`銆乣submatches` 鐨勫尮閰嶅璞℃暟缁?|
| `find.files({ query })`   | 鎸夊悕绉版煡鎵炬枃浠跺拰鐩綍 | `string[]`锛堣矾寰勶級                                                                  |
| `find.symbols({ query })` | 鏌ユ壘宸ヤ綔鍖虹鍙?      | <a href={typesUrl}><code>Symbol[]</code></a>                                        |
| `file.read({ query })`    | 璇诲彇鏂囦欢             | `{ type: "raw" \| "patch", content: string }`                                       |
| `file.status({ query? })` | 鑾峰彇宸茶窡韪枃浠剁殑鐘舵€?| <a href={typesUrl}><code>File[]</code></a>                                          |

`find.files` 鏀寔浠ヤ笅鍙€夋煡璇㈠瓧娈碉細

- `type`锛歚"file"` 鎴?`"directory"`
- `directory`锛氳鐩栨悳绱㈢殑椤圭洰鏍圭洰褰?
- `limit`锛氭渶澶х粨鏋滄暟锛?鈥?00锛?

---

#### 绀轰緥

```javascript
// Search and read files
const textResults = await client.find.text({
  query: { pattern: "function.*opencode" },
})

const files = await client.find.files({
  query: { query: "*.ts", type: "file" },
})

const directories = await client.find.files({
  query: { query: "packages", type: "directory", limit: 20 },
})

const content = await client.file.read({
  query: { path: "src/index.ts" },
})
```

---

### TUI

| 鏂规硶                           | 鎻忚堪             | 鍝嶅簲      |
| ------------------------------ | ---------------- | --------- |
| `tui.appendPrompt({ body })`   | 鍚戞彁绀鸿瘝杩藉姞鏂囨湰 | `boolean` |
| `tui.openHelp()`               | 鎵撳紑甯姪瀵硅瘽妗?  | `boolean` |
| `tui.openSessions()`           | 鎵撳紑浼氳瘽閫夋嫨鍣?  | `boolean` |
| `tui.openThemes()`             | 鎵撳紑涓婚閫夋嫨鍣?  | `boolean` |
| `tui.openModels()`             | 鎵撳紑妯″瀷閫夋嫨鍣?  | `boolean` |
| `tui.submitPrompt()`           | 鎻愪氦褰撳墠鎻愮ず璇?  | `boolean` |
| `tui.clearPrompt()`            | 娓呴櫎鎻愮ず璇?      | `boolean` |
| `tui.executeCommand({ body })` | 鎵ц鍛戒护         | `boolean` |
| `tui.showToast({ body })`      | 鏄剧ず Toast 閫氱煡  | `boolean` |

---

#### 绀轰緥

```javascript
// Control TUI interface
await client.tui.appendPrompt({
  body: { text: "Add this to prompt" },
})

await client.tui.showToast({
  body: { message: "Task completed", variant: "success" },
})
```

---

### Auth

| 鏂规硶                | 鎻忚堪         | 鍝嶅簲      |
| ------------------- | ------------ | --------- |
| `auth.set({ ... })` | 璁剧疆璁よ瘉鍑嵁 | `boolean` |

---

#### 绀轰緥

```javascript
await client.auth.set({
  path: { id: "anthropic" },
  body: { type: "api", key: "your-api-key" },
})
```

---

### Events

| 鏂规硶                | 鎻忚堪               | 鍝嶅簲               |
| ------------------- | ------------------ | ------------------ |
| `event.subscribe()` | 鏈嶅姟鍣ㄥ彂閫佺殑浜嬩欢娴?| 鏈嶅姟鍣ㄥ彂閫佺殑浜嬩欢娴?|

---

#### 绀轰緥

```javascript
// Listen to real-time events
const events = await client.event.subscribe()
for await (const event of events.stream) {
  console.log("Event:", event.type, event.properties)
}
```
