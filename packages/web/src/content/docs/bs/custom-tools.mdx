---
title: Prilago膽eni alati
description: Kreirajte alate koje LLM mo啪e pozvati u otvorenom kodu.
---

Prilago膽eni alati su funkcije koje kreirate i koje LLM mo啪e pozvati tokom razgovora. Oni rade zajedno sa [ugra膽enim opencode](/docs/tools) alatima kao 拧to su `read`, `write` i `bash`.

---

## Kreiranje alata

Alati su definisani kao **TypeScript** ili **JavaScript** datoteke. Me膽utim, definicija alata mo啪e pozvati skripte napisane na **bilo kom jeziku** 鈥?TypeScript ili JavaScript se koriste samo za samu definiciju alata.

---

### Lokacija

Mogu se definisati:

- Lokalno postavljanjem u `.opencode/tools/` direktorij va拧eg projekta.
- Ili globalno, postavljanjem u `~/.config/opencode/tools/`.

---

### Struktura

Najlak拧i na膷in za kreiranje alata je kori拧tenje pomo膰nika `tool()` koji pru啪a sigurnost tipa i validaciju.

```ts title=".opencode/tools/database.ts" {1}
import { tool } from "@openagt/plugin"

export default tool({
  description: "Query the project database",
  args: {
    query: tool.schema.string().describe("SQL query to execute"),
  },
  async execute(args) {
    // Your database logic here
    return `Executed query: ${args.query}`
  },
})
```

**ime datoteke** postaje **naziv alata**. Gore navedeno je kreirano pomo膰u `database` alata.

---

#### Vi拧e alata po datoteci

Tako膽er mo啪ete izvesti vi拧e alata iz jedne datoteke. Svaki izvoz postaje **poseban alat** pod nazivom **`<filename>_<exportname>`**:

```ts title=".opencode/tools/math.ts"
import { tool } from "@openagt/plugin"

export const add = tool({
  description: "Add two numbers",
  args: {
    a: tool.schema.number().describe("First number"),
    b: tool.schema.number().describe("Second number"),
  },
  async execute(args) {
    return args.a + args.b
  },
})

export const multiply = tool({
  description: "Multiply two numbers",
  args: {
    a: tool.schema.number().describe("First number"),
    b: tool.schema.number().describe("Second number"),
  },
  async execute(args) {
    return args.a * args.b
  },
})
```

Ovo stvara dva alata: `math_add` i `math_multiply`.

---

#### Sukob imena s ugra膽enim alatima

Prilago膽eni alati su prepoznati po imenu. Ako prilago膽eni alat koristi isto ime kao ugra膽eni alat, prilago膽eni alat ima prednost.

Na primjer, ova datoteka zamjenjuje ugra膽eni `bash` alat:

```ts title=".opencode/tools/bash.ts"
import { tool } from "@openagt/plugin"

export default tool({
  description: "Restricted bash wrapper",
  args: {
    command: tool.schema.string(),
  },
  async execute(args) {
    return `blocked: ${args.command}`
  },
})
```

:::note
Preferirajte jedinstvena imena osim ako namjerno ne 啪elite zamijeniti ugra膽eni alat. Ako 啪elite onemogu膰iti ugra膽eni alat, ali ne i nadja膷ati ga, koristite [dozvole](/docs/permissions).
:::

---

### Argumenti

Mo啪ete koristiti `tool.schema`, 拧to je samo [Zod](https://zod.dev), da definirate tipove argumenata.

```ts "tool.schema"
args: {
  query: tool.schema.string().describe("SQL query to execute")
}
```

Tako膽er mo啪ete direktno uvesti [Zod](https://zod.dev) i vratiti obi膷an objekt:

```ts {6}
import { z } from "zod"

export default {
  description: "Tool description",
  args: {
    param: z.string().describe("Parameter description"),
  },
  async execute(args, context) {
    // Tool implementation
    return "result"
  },
}
```

---

### Kontekst

Alati primaju kontekst o trenutnoj sesiji:

```ts title=".opencode/tools/project.ts" {8}
import { tool } from "@openagt/plugin"

export default tool({
  description: "Get project information",
  args: {},
  async execute(args, context) {
    // Access context information
    const { agent, sessionID, messageID, directory, worktree } = context
    return `Agent: ${agent}, Session: ${sessionID}, Message: ${messageID}, Directory: ${directory}, Worktree: ${worktree}`
  },
})
```

Koristite `context.directory` za radni direktorij sesije.
Koristite `context.worktree` za korijen git radnog stabla.

---

## Primjeri

### Pisanje alata u Python-u

Mo啪ete pisati svoje alate na bilo kom jeziku koji 啪elite. Evo primjera koji zbraja dva broja koriste膰i Python.
Prvo kreirajte alat kao Python skriptu:

```python title=".opencode/tools/add.py"
import sys

a = int(sys.argv[1])
b = int(sys.argv[2])
print(a + b)
```

Zatim kreirajte definiciju alata koja ga poziva:

```ts title=".opencode/tools/python-add.ts" {10}
import { tool } from "@openagt/plugin"
import path from "path"

export default tool({
  description: "Add two numbers using Python",
  args: {
    a: tool.schema.number().describe("First number"),
    b: tool.schema.number().describe("Second number"),
  },
  async execute(args, context) {
    const script = path.join(context.worktree, ".opencode/tools/add.py")
    const result = await Bun.$`python3 ${script} ${args.a} ${args.b}`.text()
    return result.trim()
  },
})
```

Ovdje koristimo [`Bun.$`](https://bun.com/docs/runtime/shell) uslu啪ni program za pokretanje Python skripte.
