---
title: Dodaci
description: Napi拧ite vlastite dodatke za pro拧irenje OpenAGt.
---

Dodaci vam omogu膰avaju da pro拧irite OpenAGt spajanjem na razli膷ite doga膽aje i prilago膽avanjem pona拧anja. Mo啪ete kreirati dodatke za dodavanje novih funkcija, integraciju sa eksternim uslugama ili izmenu zadanog pona拧anja OpenAGt.
Za primjere, pogledajte [plugins](/docs/ecosystem#plugins) kreirane od strane zajednice.

---

## Kori拧tenje dodatka

Postoje dva na膷ina za u膷itavanje dodataka.

### Iz lokalnih datoteka

Postavite JavaScript ili TypeScript datoteke u direktorij dodataka.

- `.opencode/plugins/` - Dodaci na nivou projekta
- `~/.config/opencode/plugins/` - Globalni dodaci
  Datoteke u ovim direktorijumima se automatski u膷itavaju pri pokretanju.

---

### Iz npm-a

Navedite npm pakete u va拧oj konfiguracijskoj datoteci.

```json title="opencode.json"
{
  "$schema": "https://github.com/Yecyi/OpenAGt/raw/dev/packages/web/dist/config.json",
  "plugin": ["opencode-helicone-session", "opencode-wakatime", "@my-org/custom-plugin"]
}
```

Podr啪ani su i regularni i npm paketi sa opsegom.
Pregledajte dostupne dodatke u [ecosystem](/docs/ecosystem#plugins).

---

### Kako se instaliraju dodaci

**npm dodaci** se instaliraju automatski pomo膰u Bun pri pokretanju. Paketi i njihove zavisnosti su ke拧irani u `~/.cache/opencode/node_modules/`.
**Lokalni dodaci** se u膷itavaju direktno iz direktorija dodataka. Da biste koristili vanjske pakete, morate kreirati `package.json` unutar svog konfiguracijskog direktorija (pogledajte [Zavisnosti](#dependencies)) ili objaviti dodatak na npm i [dodati ga u svoju konfiguraciju](/docs/config#plugins).

---

### Redoslijed u膷itavanja

Dodaci se u膷itavaju iz svih izvora i svi zaka膷njaci rade u nizu. Redoslijed u膷itavanja je:

1. Globalna konfiguracija (`~/.config/opencode/opencode.json`)
2. Konfiguracija projekta (`opencode.json`)
3. Globalni direktorij dodataka (`~/.config/opencode/plugins/`)
4. Direktorij dodataka projekta (`.opencode/plugins/`)
   Duplicirani npm paketi sa istim imenom i verzijom se u膷itavaju jednom. Me膽utim, lokalni dodatak i npm dodatak sa sli膷nim nazivima se u膷itavaju odvojeno.

---

## Kreiranje dodatka

Dodatak je **JavaScript/TypeScript modul** koji izvozi jedan ili vi拧e dodataka
funkcije. Svaka funkcija prima objekt konteksta i vra膰a hooks objekt.

---

### Zavisnosti

Lokalni dodaci i prilago膽eni alati mogu koristiti vanjske npm pakete. Dodajte `package.json` u svoj konfiguracijski direktorij sa zavisnostima koje su vam potrebne.

```json title=".opencode/package.json"
{
  "dependencies": {
    "shescape": "^2.1.0"
  }
}
```

OpenAGt pokre膰e `bun install` pri pokretanju da ih instalira. Va拧i dodaci i alati ih zatim mogu uvesti.

```ts title=".opencode/plugins/my-plugin.ts"
import { escape } from "shescape"

export const MyPlugin = async (ctx) => {
  return {
    "tool.execute.before": async (input, output) => {
      if (input.tool === "bash") {
        output.args.command = escape(output.args.command)
      }
    },
  }
}
```

---

### Osnovna struktura

```js title=".opencode/plugins/example.js"
export const MyPlugin = async ({ project, client, $, directory, worktree }) => {
  console.log("Plugin initialized!")

  return {
    // Hook implementations go here
  }
}
```

Funkcija dodatka prima:

- `project`: Trenutne informacije o projektu.
- `directory`: Trenutni radni direktorij.
- `worktree`: Putanja git radnog stabla.
- `client`: Opencode SDK klijent za interakciju sa AI.
- `$`: Bun's [shell API](https://bun.com/docs/runtime/shell) za izvr拧avanje naredbi.

---

### Podr拧ka za TypeScript

Za TypeScript dodatke, mo啪ete uvesti tipove iz paketa dodataka:

```ts title="my-plugin.ts" {1}
import type { Plugin } from "@openagt/plugin"

export const MyPlugin: Plugin = async ({ project, client, $, directory, worktree }) => {
  return {
    // Type-safe hook implementations
  }
}
```

---

### Doga膽aji

Dodaci se mogu pretplatiti na doga膽aje kao 拧to je prikazano ispod u odjeljku Primjeri. Evo liste razli膷itih dostupnih doga膽aja.

#### Komandni doga膽aji

- `command.executed`

#### Doga膽aji datoteka

- `file.edited`
- `file.watcher.updated`

#### Instalacijski doga膽aji

- `installation.updated`

#### LSP doga膽aji

- `lsp.client.diagnostics`
- `lsp.updated`

#### Doga膽aji poruka

- `message.part.removed`
- `message.part.updated`
- `message.removed`
- `message.updated`

#### Doga膽aji dozvola

- `permission.asked`
- `permission.replied`

#### Serverski doga膽aji

- `server.connected`

#### Doga膽aji sesije

- `session.created`
- `session.compacted`
- `session.deleted`
- `session.diff`
- `session.error`
- `session.idle`
- `session.status`
- `session.updated`

#### Todo doga膽aji

- `todo.updated`

#### Shell doga膽aji

- `shell.env`

#### Doga膽aji alata

- `tool.execute.after`
- `tool.execute.before`

#### TUI doga膽aji

- `tui.prompt.append`
- `tui.command.execute`
- `tui.toast.show`

---

## Primjeri

Evo nekoliko primjera dodataka koje mo啪ete koristiti za pro拧irenje OpenAGt.

### Slanje obavje拧tenja

Po拧aljite obavje拧tenja kada se dogode odre膽eni doga膽aji:

```js title=".opencode/plugins/notification.js"
export const NotificationPlugin = async ({ project, client, $, directory, worktree }) => {
  return {
    event: async ({ event }) => {
      // Send notification on session completion
      if (event.type === "session.idle") {
        await $`osascript -e 'display notification "Session completed!" with title "opencode"'`
      }
    },
  }
}
```

Koristimo `osascript` za pokretanje AppleScript-a na macOS-u. Ovdje ga koristimo za slanje obavje拧tenja.
:::note
Ako alat dodatka koristi isto ime kao ugra膽eni alat, alat dodatka ima prednost.
:::

---

### .env za拧tita

Sprije膷ite opencode da 膷ita `.env` fajlove:

```javascript title=".opencode/plugins/env-protection.js"
export const EnvProtection = async ({ project, client, $, directory, worktree }) => {
  return {
    "tool.execute.before": async (input, output) => {
      if (input.tool === "read" && output.args.filePath.includes(".env")) {
        throw new Error("Do not read .env files")
      }
    },
  }
}
```

---

### Ubacivanje varijabli okru啪enja

Ubacite varijable okru啪enja u sva izvr拧avanja ljuske (AI alati i korisni膷ki terminali):

```javascript title=".opencode/plugins/inject-env.js"
export const InjectEnvPlugin = async () => {
  return {
    "shell.env": async (input, output) => {
      output.env.MY_API_KEY = "secret"
      output.env.PROJECT_ROOT = input.cwd
    },
  }
}
```

---

### Prilago膽eni alati

Dodaci tako膽er mogu dodati prilago膽ene alate u opencode:

```ts title=".opencode/plugins/custom-tools.ts"
import { type Plugin, tool } from "@openagt/plugin"

export const CustomToolsPlugin: Plugin = async (ctx) => {
  return {
    tool: {
      mytool: tool({
        description: "This is a custom tool",
        args: {
          foo: tool.schema.string(),
        },
        async execute(args, context) {
          const { directory, worktree } = context
          return `Hello ${args.foo} from ${directory} (worktree: ${worktree})`
        },
      }),
    },
  }
}
```

Pomo膰nik `tool` kreira prilago膽eni alat koji opencode mo啪e pozvati. Uzima funkciju Zod sheme i vra膰a definiciju alata sa:

- `description`: 艩ta alat radi
- `args`: Zod 拧ema za argumente alata
- `execute`: Funkcija koja se pokre膰e kada se pozove alat
  Va拧i prilago膽eni alati 膰e biti dostupni za opencode zajedno sa ugra膽enim alatima.

---

### Bilje啪enje

Koristite `client.app.log()` umjesto `console.log` za strukturirano bilje啪enje:

```ts title=".opencode/plugins/my-plugin.ts"
export const MyPlugin = async ({ client }) => {
  await client.app.log({
    body: {
      service: "my-plugin",
      level: "info",
      message: "Plugin initialized",
      extra: { foo: "bar" },
    },
  })
}
```

Nivoi su: `debug`, `info`, `warn`, `error`. Pogledajte [SDK dokumentaciju](https://github.com/Yecyi/OpenAGt/docs/sdk) za detalje.

### Kuke za sa啪imanje

Prilagodite kontekst uklju膷en kada se sesija zbije:

```ts title=".opencode/plugins/compaction.ts"
import type { Plugin } from "@openagt/plugin"

export const CompactionPlugin: Plugin = async (ctx) => {
  return {
    "experimental.session.compacting": async (input, output) => {
      // Inject additional context into the compaction prompt
      output.context.push(`
## Custom Context

Include any state that should persist across compaction:
- Current task status
- Important decisions made
- Files being actively worked on
`)
    },
  }
}
```

`experimental.session.compacting` kuka se aktivira prije nego 拧to LLM generira sa啪etak nastavka. Koristite ga za ubacivanje konteksta specifi膷nog za domenu koji bi zadani prompt za sa啪imanje propustio.
Tako膽er mo啪ete u potpunosti zamijeniti prompt za sabijanje postavljanjem `output.prompt`:

```ts title=".opencode/plugins/custom-compaction.ts"
import type { Plugin } from "@openagt/plugin"

export const CustomCompactionPlugin: Plugin = async (ctx) => {
  return {
    "experimental.session.compacting": async (input, output) => {
      // Replace the entire compaction prompt
      output.prompt = `
You are generating a continuation prompt for a multi-agent swarm session.

Summarize:
1. The current task and its status
2. Which files are being modified and by whom
3. Any blockers or dependencies between agents
4. The next steps to complete the work

Format as a structured prompt that a new agent can use to resume work.
`
    },
  }
}
```

Kada je `output.prompt` postavljen, on u potpunosti zamjenjuje zadani prompt za sa啪imanje. Niz `output.context` se zanemaruje u ovom slu膷aju.
