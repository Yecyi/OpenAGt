---
title: Eklentiler
description: OpenAGt'u geni艧letmek i莽in kendi eklentilerinizi yaz谋n.
---

Eklentiler, 莽e艧itli olaylara ba臒lanarak ve davran谋艧谋 枚zelle艧tirerek OpenAGt'u geni艧letmenize olanak tan谋r. Yeni 枚zellikler eklemek, harici hizmetlerle entegrasyon sa臒lamak veya OpenAGt'un varsay谋lan davran谋艧谋n谋 de臒i艧tirmek i莽in eklentiler olu艧turabilirsiniz.

脰rnekler i莽in topluluk taraf谋ndan olu艧turulan [eklentilere](/docs/ecosystem#plugins) g枚z at谋n.

---

## Eklenti kullan谋m谋

Eklentileri y眉klemenin iki yolu vard谋r.

---

### Yerel dosyalardan

JavaScript veya TypeScript dosyalar谋n谋 eklenti dizinine yerle艧tirin.

- `.opencode/plugins/` - Proje d眉zeyinde eklentiler
- `~/.config/opencode/plugins/` - Genel eklentiler

Bu dizinlerdeki dosyalar ba艧lang谋莽ta otomatik olarak y眉klenir.

---

### npm'den

Yap谋land谋rma dosyan谋zda npm paketlerini belirtin.

```json title="opencode.json"
{
  "$schema": "https://github.com/Yecyi/OpenAGt/raw/dev/packages/web/dist/config.json",
  "plugin": ["opencode-helicone-session", "opencode-wakatime", "@my-org/custom-plugin"]
}
```

Hem normal hem de kapsaml谋 npm paketleri desteklenir.

[Ekosistem](/docs/ecosystem#plugins)'deki mevcut eklentilere g枚z at谋n.

---

### Eklentiler nas谋l kurulur?

**npm eklentileri** ba艧lang谋莽ta Bun kullan谋larak otomatik olarak y眉klenir. Paketler ve ba臒谋ml谋l谋klar谋 `~/.cache/opencode/node_modules/`'da 枚nbelle臒e al谋n谋r.

**Yerel eklentiler** do臒rudan eklenti dizininden y眉klenir. Harici paketleri kullanmak i莽in, sisteminizin dizininde bir `package.json` olu艧turman谋z (bkz. [Ba臒谋ml谋l谋klar](#dependencies)) veya eklentiyi npm ve [yap谋land谋rman谋za eklemeniz](/docs/config#plugins) gerekir.

---

### Y眉kleme s谋ras谋

Eklentiler t眉m kaynaklardan y眉klenir ve t眉m kancalar s谋rayla 莽al谋艧谋r. Y眉kleme s谋ras谋 艧枚yledir:

1. Global config (`~/.config/opencode/opencode.json`)
2. Project config (`opencode.json`)
3. Global eklenti dizini (`~/.config/opencode/plugins/`)
4. Proje eklenti dizini (`.opencode/plugins/`)

Ayn谋 ad ve s眉r眉me sahip yinelenen npm paketleri bir kez y眉klenir. Ancak benzer adlara sahip bir yerel eklenti ve bir npm eklentisinin her ikisi de ayr谋 ayr谋 y眉klenir.

---

## Eklenti olu艧turma

Eklenti, bir veya daha fazla eklenti i艧levini d谋艧a aktaran bir **JavaScript/TypeScript mod眉l眉d眉r**. Her i艧lev bir ba臒lam nesnesi al谋r ve bir kanca nesnesi d枚nd眉r眉r.

---

### Ba臒谋ml谋l谋klar

Yerel eklentiler ve 枚zel ara莽lar harici npm paketlerini kullanabilir. 陌htiyac谋n谋z olan ba臒谋ml谋l谋klar谋 i莽eren config dizininize bir `package.json` ekleyin.

```json title=".opencode/package.json"
{
  "dependencies": {
    "shescape": "^2.1.0"
  }
}
```

OpenAGt bunlar谋 y眉klemek i莽in ba艧lang谋莽ta `bun install` komutunu 莽al谋艧t谋r谋r. Eklentileriniz ve ara莽lar谋n谋z daha sonra bunlar谋 i莽e aktarabilir.

```ts title=".opencode/plugins/my-plugin.ts"
import { escape } from "shescape"

export const MyPlugin = async (ctx) => {
  return {
    "tool.execute.before": async (input, output) => {
      if (input.tool === "bash") {
        output.args.command = escape(output.args.command)
      }
    },
  }
}
```

---

### Temel yap谋

```js title=".opencode/plugins/example.js"
export const MyPlugin = async ({ project, client, $, directory, worktree }) => {
  console.log("Plugin initialized!")

  return {
    // Hook implementations go here
  }
}
```

Eklenti i艧levi 艧unlar谋 al谋r:

- `project`: Mevcut proje bilgisi.
- `directory`: G眉ncel 莽al谋艧ma dizini.
- `worktree`: Git 莽al谋艧ma a臒ac谋 yolu.
- `client`: Yapay zeka ile etkile艧im kurmak i莽in bir OpenAGt SDK istemcisi.
- `$`: Bun'un komutlar谋 y眉r眉tmek i莽in kulland谋臒谋 [shell API](https://bun.com/docs/runtime/shell).

---

### TypeScript deste臒i

TypeScript eklentileri i莽in t眉rleri eklenti paketinden i莽e aktarabilirsiniz:

```ts title="my-plugin.ts" {1}
import type { Plugin } from "@openagt/plugin"

export const MyPlugin: Plugin = async ({ project, client, $, directory, worktree }) => {
  return {
    // Type-safe hook implementations
  }
}
```

---

### Olaylar

Eklentiler a艧a臒谋daki 脰rnekler b枚l眉m眉nde g枚r眉ld眉臒眉 gibi etkinliklere abone olabilirler. Burada mevcut farkl谋 etkinliklerin bir listesi bulunmaktad谋r.

#### Komut Olaylar谋

- `command.executed`

#### Dosya Olaylar谋

- `file.edited`
- `file.watcher.updated`

#### Kurulum Olaylar谋

- `installation.updated`

#### LSP Olaylar谋

- `lsp.client.diagnostics`
- `lsp.updated`

#### Mesaj Olaylar谋

- `message.part.removed`
- `message.part.updated`
- `message.removed`
- `message.updated`

#### 陌zin Olaylar谋

- `permission.asked`
- `permission.replied`

#### Sunucu Olaylar谋

- `server.connected`

#### Oturum Olaylar谋

- `session.created`
- `session.compacted`
- `session.deleted`
- `session.diff`
- `session.error`
- `session.idle`
- `session.status`
- `session.updated`

#### Yap谋lacaklar Olaylar谋

- `todo.updated`

#### Kabuk Olaylar谋

- `shell.env`

#### Ara莽 Olaylar谋

- `tool.execute.after`
- `tool.execute.before`

#### TUI Olaylar谋

- `tui.prompt.append`
- `tui.command.execute`
- `tui.toast.show`

---

## 脰rnekler

OpenAGt'u geni艧letmek i莽in kullanabilece臒iniz baz谋 eklenti 枚rneklerini burada bulabilirsiniz.

---

### Bildirim G枚nderme

Belirli olaylar meydana geldi臒inde bildirim g枚nderin:

```js title=".opencode/plugins/notification.js"
export const NotificationPlugin = async ({ project, client, $, directory, worktree }) => {
  return {
    event: async ({ event }) => {
      // Send notification on session completion
      if (event.type === "session.idle") {
        await $`osascript -e 'display notification "Session completed!" with title "opencode"'`
      }
    },
  }
}
```

macOS'ta AppleScript'i 莽al谋艧t谋rmak i莽in `osascript` kullan谋yoruz. Burada bildirim g枚ndermek i莽in kullan谋yoruz.

:::note
OpenAGt masa眉st眉 uygulamas谋n谋 kullan谋yorsan谋z yan谋t haz谋r oldu臒unda veya oturum hatalar谋 olu艧tu臒unda otomatik olarak sistem bildirimleri g枚nderebilir.
:::

---

### .env Korumas谋

OpenAGt'un `.env` dosyalar谋n谋 okumas谋n谋 枚nleyin:

```javascript title=".opencode/plugins/env-protection.js"
export const EnvProtection = async ({ project, client, $, directory, worktree }) => {
  return {
    "tool.execute.before": async (input, output) => {
      if (input.tool === "read" && output.args.filePath.includes(".env")) {
        throw new Error("Do not read .env files")
      }
    },
  }
}
```

---

### Ortam de臒i艧kenlerini enjekte etme

Ortam de臒i艧kenlerini t眉m kabuk y眉r眉tmeye (AI ara莽lar谋 ve kullan谋c谋 terminalleri) enjekte edin:

```javascript title=".opencode/plugins/inject-env.js"
export const InjectEnvPlugin = async () => {
  return {
    "shell.env": async (input, output) => {
      output.env.MY_API_KEY = "secret"
      output.env.PROJECT_ROOT = input.cwd
    },
  }
}
```

---

### 脰zel ara莽lar

Eklentiler ayr谋ca OpenAGt'a 枚zel ara莽lar da ekleyebilir:

```ts title=".opencode/plugins/custom-tools.ts"
import { type Plugin, tool } from "@openagt/plugin"

export const CustomToolsPlugin: Plugin = async (ctx) => {
  return {
    tool: {
      mytool: tool({
        description: "This is a custom tool",
        args: {
          foo: tool.schema.string(),
        },
        async execute(args, context) {
          const { directory, worktree } = context
          return `Hello ${args.foo} from ${directory} (worktree: ${worktree})`
        },
      }),
    },
  }
}
```

`tool` yard谋mc谋s谋, OpenAGt'un 莽a臒谋rabilece臒i 枚zel bir ara莽 olu艧turur. Bir Zod 艧emas谋 i艧levini al谋r ve a艧a臒谋dakileri i莽eren bir ara莽 tan谋m谋 d枚nd眉r眉r:

- `description`: Ara莽 ne yapar?
- `args`: Arac谋n arg眉manlar谋 i莽in Zod 艧emas谋
- `execute`: Ara莽 莽a臒r谋ld谋臒谋nda 莽al谋艧an fonksiyon

脰zel ara莽lar谋n谋z, yerle艧ik ara莽lar谋n yan谋 s谋ra OpenAGt'a da haz谋r olacakt谋r.

:::note
E臒er bir eklenti arac谋 yerle艧ik bir ara莽la ayn谋 ad谋 kullan谋rsa, eklenti arac谋 枚ncelik kazan谋r.
:::

---

### G眉nl眉臒e kaydetme

Yap谋land谋r谋lm谋艧 g眉nl眉k kayd谋 i莽in `console.log` yerine `client.app.log()` kullan谋n:

```ts title=".opencode/plugins/my-plugin.ts"
export const MyPlugin = async ({ client }) => {
  await client.app.log({
    body: {
      service: "my-plugin",
      level: "info",
      message: "Plugin initialized",
      extra: { foo: "bar" },
    },
  })
}
```

Seviyeler: `debug`, `info`, `warn`, `error`. Ayr谋nt谋lar i莽in [SDK belgelerine](https://github.com/Yecyi/OpenAGt/docs/sdk) bak谋n.

---

### S谋k谋艧t谋rma kancalar谋

Bir oturum s谋k谋艧t谋r谋ld谋臒谋nda i莽erilen ba臒lam谋 枚zelle艧tirin:

```ts title=".opencode/plugins/compaction.ts"
import type { Plugin } from "@openagt/plugin"

export const CompactionPlugin: Plugin = async (ctx) => {
  return {
    "experimental.session.compacting": async (input, output) => {
      // Inject additional context into the compaction prompt
      output.context.push(`
## Custom Context

Include any state that should persist across compaction:
- Current task status
- Important decisions made
- Files being actively worked on
`)
    },
  }
}
```

`experimental.session.compacting` kancas谋, LLM bir devam 枚zeti olu艧turmadan 枚nce tetiklenir. Varsay谋lan s谋k谋艧t谋rma isteminin ka莽谋raca臒谋 etki alan谋na 枚zg眉 ba臒lam谋 enjekte etmek i莽in bunu kullan谋n.

Ayr谋ca `output.prompt` ayar谋n谋 yaparak s谋k谋艧t谋rma istemini tamamen de臒i艧tirebilirsiniz:

```ts title=".opencode/plugins/custom-compaction.ts"
import type { Plugin } from "@openagt/plugin"

export const CustomCompactionPlugin: Plugin = async (ctx) => {
  return {
    "experimental.session.compacting": async (input, output) => {
      // Replace the entire compaction prompt
      output.prompt = `
You are generating a continuation prompt for a multi-agent swarm session.

Summarize:
1. The current task and its status
2. Which files are being modified and by whom
3. Any blockers or dependencies between agents
4. The next steps to complete the work

Format as a structured prompt that a new agent can use to resume work.
`
    },
  }
}
```

`output.prompt` ayarland谋臒谋nda, varsay谋lan s谋k谋艧t谋rma isteminin tamamen yerini al谋r. Bu durumda `output.context` dizisi dikkate al谋nmaz.
