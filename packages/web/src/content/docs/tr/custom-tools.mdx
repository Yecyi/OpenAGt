---
title: 脰zel Ara莽lar
description: LLM'un opencode'da 莽a臒谋rabilece臒i ara莽lar谋 birle艧tirebilir.
---

脰zel ara莽lar, LLM'un konu艧malar s谋ras谋nda arayabilece臒i, olu艧turdu臒unuz i艧levlerdir. opencode'un [built-in tools](/docs/tools) ile birlikte `read`, `write` ve `bash` gibi 莽al谋艧谋rlar.

---

## Ara莽 olu艧turma

Ara莽lar **TypeScript** veya **JavaScript** dosyalar谋 olarak tan谋mlan谋r. Ancak ara莽 tan谋m谋 **herhangi bir dilde** yaz谋lm谋艧 komut dosyalar谋n谋 莽a臒谋rabilir; TypeScript veya JavaScript yaln谋zca ara莽 tan谋m谋n谋n kendisi i莽in kullan谋l谋r.

---

### Konum

Bunlar tan谋mlanabilir:

- Yerel olarak bunlar谋 projenizin `.opencode/tools/` dizinine yerle艧tirerek.
- Veya k眉resel olarak bunlar谋 `~/.config/opencode/tools/` i莽ine yerle艧tirerek.

---

### Yap谋

Ara莽 olu艧turman谋n en kolay yolu, t眉r g眉venli臒i ve do臒rulama sa臒layan `tool()` yard谋mc谋s谋n谋 kullanmakt谋r.

```ts title=".opencode/tools/database.ts" {1}
import { tool } from "@openagt/plugin"

export default tool({
  description: "Query the project database",
  args: {
    query: tool.schema.string().describe("SQL query to execute"),
  },
  async execute(args) {
    // Your database logic here
    return `Executed query: ${args.query}`
  },
})
```

**dosya ad谋**, **ara莽 ad谋** olur. Yukar谋dakiler bir `database` arac谋 olu艧turur.

---

#### Dosya ba艧谋na birden fazla ara莽

Ayr谋ca tek bir dosyadan birden fazla arac谋 d谋艧a aktarabilirsiniz. Her d谋艧a aktarma, **`<filename>_<exportname>`** ad谋yla **ayr谋 bir ara莽** haline gelir:

```ts title=".opencode/tools/math.ts"
import { tool } from "@openagt/plugin"

export const add = tool({
  description: "Add two numbers",
  args: {
    a: tool.schema.number().describe("First number"),
    b: tool.schema.number().describe("Second number"),
  },
  async execute(args) {
    return args.a + args.b
  },
})

export const multiply = tool({
  description: "Multiply two numbers",
  args: {
    a: tool.schema.number().describe("First number"),
    b: tool.schema.number().describe("Second number"),
  },
  async execute(args) {
    return args.a * args.b
  },
})
```

Bu iki ara莽 olu艧turur: `math_add` ve `math_multiply`.

---

#### Yerle艧ik ara莽larla ad 莽ak谋艧malar谋

脰zel ara莽lar, ara莽 ad谋na g枚re anahtarlan谋r. 脰zel bir ara莽 yerle艧ik bir ara莽la ayn谋 ad谋 kullan谋yorsa, 枚zel ara莽 枚nceliklidir.

脰rne臒in, bu dosya yerle艧ik `bash` arac谋n谋n yerini al谋r:

```ts title=".opencode/tools/bash.ts"
import { tool } from "@openagt/plugin"

export default tool({
  description: "Restricted bash wrapper",
  args: {
    command: tool.schema.string(),
  },
  async execute(args) {
    return `blocked: ${args.command}`
  },
})
```

:::note
Kas谋tl谋 olarak yerle艧ik bir arac谋 de臒i艧tirmek istemiyorsan谋z benzersiz adlar谋 tercih edin. Yerle艧ik bir arac谋 devre d谋艧谋 b谋rakmak ancak ge莽ersiz k谋lmak istemiyorsan谋z [izinleri](/docs/permissions) kullan谋n.
:::

---

### Arg眉manlar

Ba臒谋ms谋z de臒i艧ken t眉rlerini tan谋mlamak i莽in yaln谋zca [Zod](https://zod.dev) olan `tool.schema` 枚臒esini kullanabilirsiniz.

```ts "tool.schema"
args: {
  query: tool.schema.string().describe("SQL query to execute")
}
```

Ayr谋ca [Zod](https://zod.dev) 枚臒esini do臒rudan i莽e aktarabilir ve d眉z bir nesne d枚nd眉rebilirsiniz:

```ts {6}
import { z } from "zod"

export default {
  description: "Tool description",
  args: {
    param: z.string().describe("Parameter description"),
  },
  async execute(args, context) {
    // Tool implementation
    return "result"
  },
}
```

---

### Ba臒lam

Ara莽lar ge莽erli oturumla ilgili ba臒lam谋 al谋r:

```ts title=".opencode/tools/project.ts" {8}
import { tool } from "@openagt/plugin"

export default tool({
  description: "Get project information",
  args: {},
  async execute(args, context) {
    // Access context information
    const { agent, sessionID, messageID, directory, worktree } = context
    return `Agent: ${agent}, Session: ${sessionID}, Message: ${messageID}, Directory: ${directory}, Worktree: ${worktree}`
  },
})
```

Oturum 莽al谋艧ma dizisi i莽in `context.directory` kullan谋n.
Git 莽al谋艧ma a臒ac谋 k枚k眉 i莽in `context.worktree` kullan谋n.

---

## 脰rnekler

### Python ile ara莽 yazma

Ara莽lar谋n谋z谋 diledi臒iniz dilde yazabilirsiniz. 陌艧te Python kullanarak iki say谋y谋 toplayan bir 枚rnek.

脰ncelikle arac谋 bir Python beti臒i olarak olu艧turun:

```python title=".opencode/tools/add.py"
import sys

a = int(sys.argv[1])
b = int(sys.argv[2])
print(a + b)
```

Ard谋ndan onu 莽a臒谋ran ara莽 tan谋m谋n谋 olu艧turun:

```ts title=".opencode/tools/python-add.ts" {10}
import { tool } from "@openagt/plugin"
import path from "path"

export default tool({
  description: "Add two numbers using Python",
  args: {
    a: tool.schema.number().describe("First number"),
    b: tool.schema.number().describe("Second number"),
  },
  async execute(args, context) {
    const script = path.join(context.worktree, ".opencode/tools/add.py")
    const result = await Bun.$`python3 ${script} ${args.a} ${args.b}`.text()
    return result.trim()
  },
})
```

Burada Python beti臒ini 莽al谋艧t谋rmak i莽in [`Bun.$`](https://bun.com/docs/runtime/shell) yard谋mc谋 program谋n谋 kullan谋yoruz.
