---
title: Brugerdefinerede v忙rkt酶jer
description: Opret v忙rkt酶jer, som LLM kan kalde opencode ind.
---

Brugerdefinerede v忙rkt酶jer er funktioner, du opretter, som LLM kan kalde under samtaler. De arbejder sammen med opencodes [built-in tools](/docs/tools) som `read`, `write` og `bash`.

---

## Oprettelse af et v忙rkt酶j

V忙rkt酶jer er defineret som **TypeScript**- eller **JavaScript**-filer. V忙rkt酶jsdefinitionen kan dog kalde scripts skrevet p氓 **alle sprog** - TypeScript eller JavaScript bruges kun til selve v忙rkt酶jsdefinitionen.

---

### Placering

De kan definere:

- Lokalt ved at placere dem i biblioteket `.opencode/tools/` i dit projekt.
- Eller globalt ved at placere dem i `~/.config/opencode/tools/`.

---

### Struktur

Den nemmeste m氓de at oprette v忙rkt酶jer p氓 er at bruge `tool()`-hj忙lperen, som giver typesikkerhed og validering.

```ts title=".opencode/tools/database.ts" {1}
import { tool } from "@openagt/plugin"

export default tool({
  description: "Query the project database",
  args: {
    query: tool.schema.string().describe("SQL query to execute"),
  },
  async execute(args) {
    // Your database logic here
    return `Executed query: ${args.query}`
  },
})
```

**filnavnet** bliver **v忙rkt酶jsnavnet**. Ovenst氓ende opretter et `database` v忙rkt酶j.

---

#### Flere v忙rkt酶jer pr. fil

Du kan ogs氓 eksportere flere v忙rkt酶jer fra en enkelt fil. Hver eksport bliver **et separat v忙rkt酶j** med navnet **`<filename>_<exportname>`**:

```ts title=".opencode/tools/math.ts"
import { tool } from "@openagt/plugin"

export const add = tool({
  description: "Add two numbers",
  args: {
    a: tool.schema.number().describe("First number"),
    b: tool.schema.number().describe("Second number"),
  },
  async execute(args) {
    return args.a + args.b
  },
})

export const multiply = tool({
  description: "Multiply two numbers",
  args: {
    a: tool.schema.number().describe("First number"),
    b: tool.schema.number().describe("Second number"),
  },
  async execute(args) {
    return args.a * args.b
  },
})
```

Dette skaber v忙rkt酶jer: `math_add` og `math_multiply`.

---

#### Navnekollisioner med indbyggede v忙rkt酶jer

Brugerdefinerede v忙rkt酶jer er identificeret ved v忙rkt酶jsnavn. Hvis et brugerdefineret v忙rkt酶j bruger samme navn som et indbygget v忙rkt酶j, har det brugerdefinerede v忙rkt酶j forrang.

For eksempel erstatter denne fil det indbyggede `bash` v忙rkt酶j:

```ts title=".opencode/tools/bash.ts"
import { tool } from "@openagt/plugin"

export default tool({
  description: "Restricted bash wrapper",
  args: {
    command: tool.schema.string(),
  },
  async execute(args) {
    return `blocked: ${args.command}`
  },
})
```

:::note
Foretr忙k unikke navne, medmindre du bevidst 酶nsker at erstatte et indbygget v忙rkt酶j. Hvis du vil deaktivere et indbygget v忙rkt酶j, men ikke overskrive det, skal du bruge [tilladelser](/docs/permissions).
:::

---

### Argumenter

Du kan bruge `tool.schema`, som kun er [Zod](https://zod.dev), til at definere argumenttyper.

```ts "tool.schema"
args: {
  query: tool.schema.string().describe("SQL query to execute")
}
```

Du kan ogs氓 importere [Zod](https://zod.dev) direkte og returnere et almindeligt objekt:

```ts {6}
import { z } from "zod"

export default {
  description: "Tool description",
  args: {
    param: z.string().describe("Parameter description"),
  },
  async execute(args, context) {
    // Tool implementation
    return "result"
  },
}
```

---

### Kontekst

V忙rkt酶jer modtager kontekst om den aktuelle session:

```ts title=".opencode/tools/project.ts" {8}
import { tool } from "@openagt/plugin"

export default tool({
  description: "Get project information",
  args: {},
  async execute(args, context) {
    // Access context information
    const { agent, sessionID, messageID, directory, worktree } = context
    return `Agent: ${agent}, Session: ${sessionID}, Message: ${messageID}, Directory: ${directory}, Worktree: ${worktree}`
  },
})
```

Brug `context.directory` til sessionens arbejdsmappe.
Brug `context.worktree` til git-arbejdstr忙ets rod.

---

## Eksempler

### Skriv et v忙rkt酶j i Python

Du kan skrive dine v忙rkt酶jer p氓 et hvilket som helst sprog, du 酶nsker. Her er et eksempel, der tilf酶jer til tal ved hj忙lp af Python.

F酶rst skal du oprette v忙rkt酶jet som et Python-script:

```python title=".opencode/tools/add.py"
import sys

a = int(sys.argv[1])
b = int(sys.argv[2])
print(a + b)
```

Opret derefter v忙rkt酶jsdefinitionen, der kalder den:

```ts title=".opencode/tools/python-add.ts" {10}
import { tool } from "@openagt/plugin"
import path from "path"

export default tool({
  description: "Add two numbers using Python",
  args: {
    a: tool.schema.number().describe("First number"),
    b: tool.schema.number().describe("Second number"),
  },
  async execute(args, context) {
    const script = path.join(context.worktree, ".opencode/tools/add.py")
    const result = await Bun.$`python3 ${script} ${args.a} ${args.b}`.text()
    return result.trim()
  },
})
```

Her bruger vi [`Bun.$`](https://bun.com/docs/runtime/shell)-v忙rkt酶jet til at k酶re Python-scriptet.
