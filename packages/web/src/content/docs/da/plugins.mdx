---
title: Plugins
description: Skriv dine egne plugins for at udvide OpenAGt.
---

Plugins giver dig mulighed for at udvide OpenAGt ved at tilslutte sig forskellige begivenheder og tilpasse adf忙rd. Du kan oprette plugins for at tilf酶je nye funktioner, integrere med eksterne tjenester eller 忙ndre OpenCodes standardadf忙rd.

For eksempler, tjek [plugins](/docs/ecosystem#plugins) oprettet af f忙llesskabet.

---

## Brug af plugin

Der er to m氓der at indl忙se plugins p氓.

---

### Fra lokale filer

Placer JavaScript- eller TypeScript-filer i plugin-mappen.

- `.opencode/plugins/` - Plugins p氓 projektniveau
- `~/.config/opencode/plugins/` - Globale plugins

Filer i disse mapper indl忙ses automatisk ved opstart.

---

### Fra npm

Angiv npm-pakker i din konfigurationsfil.

```json title="opencode.json"
{
  "$schema": "https://github.com/Yecyi/OpenAGt/raw/dev/packages/web/dist/config.json",
  "plugin": ["opencode-helicone-session", "opencode-wakatime", "@my-org/custom-plugin"]
}
```

B氓de almindelige og omfangsrige npm-pakker underst酶ttes.

Gennemse tilg忙ngelige plugins i [ecosystem](/docs/ecosystem#plugins).

---

### Installation af plugins

**npm plugins** installeres automatisk ved hj忙lp af Bun ved opstart. Pakker og deres afh忙ngigheder cachelagres i `~/.cache/opencode/node_modules/`.

**Lokale plugins** indl忙ses direkte fra plugin-biblioteket. For at bruge eksterne pakker skal du oprette en `package.json` i din konfigurationsmappe (se [Dependencies](#dependencies)), eller udgive pluginnet til npm og [add it to your config](/docs/config#plugins).

---

### Indl忙sningsr忙kkef酶lge

Plugins indl忙ses fra alle kilder, og alle hooks k酶rer i r忙kkef酶lge. Indl忙sningsr忙kkef酶lgen er:

1. Global konfiguration (`~/.config/opencode/opencode.json`)
2. Projektkonfiguration (`opencode.json`)
3. Global plugin-mappe (`~/.config/opencode/plugins/`)
4. Projekt plugin bibliotek (`.opencode/plugins/`)

Dublerede npm-pakker med samme navn og version indl忙ses 茅n gang. Et lokalt plugin og et npm plugin med lignende navne indl忙ses dog hver for sig.

---

## Oprettelse af plugin

Et plugin er et **JavaScript/TypeScript modul**, der eksporterer et eller flere plugin
funktioner. Hver funktion modtager et kontekstobjekt og returnerer et hooks-objekt.

---

### Afh忙ngigheder

Lokale plugins og brugerdefinerede v忙rkt酶jer kan bruge eksterne npm-pakker. Tilf酶j en `package.json` til din konfigurationsmappe med de afh忙ngigheder, du har brug for.

```json title=".opencode/package.json"
{
  "dependencies": {
    "shescape": "^2.1.0"
  }
}
```

OpenAGt k酶rer `bun install` ved opstart for at installere disse. Dine plugins og v忙rkt酶jer kan derefter importere dem.

```ts title=".opencode/plugins/my-plugin.ts"
import { escape } from "shescape"

export const MyPlugin = async (ctx) => {
  return {
    "tool.execute.before": async (input, output) => {
      if (input.tool === "bash") {
        output.args.command = escape(output.args.command)
      }
    },
  }
}
```

---

### Grundl忙ggende struktur

```js title=".opencode/plugins/example.js"
export const MyPlugin = async ({ project, client, $, directory, worktree }) => {
  console.log("Plugin initialized!")

  return {
    // Hook implementations go here
  }
}
```

Plugin-funktionen modtager:

- `project`: Den aktuelle projektinformation.
- `directory`: Den aktuelle arbejdsmappe.
- `worktree`: Git worktree-stien.
- `client`: En opencode SDK klient til interaktion med AI.
- `$`: Buns [shell API](https://bun.com/docs/runtime/shell) til udf酶relse af kommandoer.

---

### TypeScript-underst酶ttelse

For TypeScript-plugins kan du importere typer fra plugin-pakken:

```ts title="my-plugin.ts" {1}
import type { Plugin } from "@openagt/plugin"

export const MyPlugin: Plugin = async ({ project, client, $, directory, worktree }) => {
  return {
    // Type-safe hook implementations
  }
}
```

---

### Begivenheder

Plugins kan abonnere p氓 begivenheder som vist nedenfor i afsnittet Eksempler. Her er en liste over de forskellige arrangementer.

#### Kommandoh忙ndelser

- `command.executed`

#### Filh忙ndelser

- `file.edited`
- `file.watcher.updated`

#### Installationsh忙ndelser

- `installation.updated`

#### LSP-h忙ndelser

- `lsp.client.diagnostics`
- `lsp.updated`

#### Beskedh忙ndelser

- `message.part.removed`
- `message.part.updated`
- `message.removed`
- `message.updated`

#### Tilladelsesh忙ndelser

- `permission.asked`
- `permission.replied`

#### Serverh忙ndelser

- `server.connected`

#### Sessionsh忙ndelser

- `session.created`
- `session.compacted`
- `session.deleted`
- `session.diff`
- `session.error`
- `session.idle`
- `session.status`
- `session.updated`

#### Todo-h忙ndelser

- `todo.updated`

#### Shell-h忙ndelser

- `shell.env`

#### V忙rkt酶jsh忙ndelser

- `tool.execute.after`
- `tool.execute.before`

#### TUI-h忙ndelser

- `tui.prompt.append`
- `tui.command.execute`
- `tui.toast.show`

---

## Eksempler

Her er nogle eksempler p氓 plugins, du kan bruge til at udvide opencode.

---

### Send meddelelser

Send meddelelser, n氓r visse h忙ndelser indtr忙ffer:

```js title=".opencode/plugins/notification.js"
export const NotificationPlugin = async ({ project, client, $, directory, worktree }) => {
  return {
    event: async ({ event }) => {
      // Send notification on session completion
      if (event.type === "session.idle") {
        await $`osascript -e 'display notification "Session completed!" with title "opencode"'`
      }
    },
  }
}
```

Vi bruger `osascript` til at k酶re AppleScript p氓 macOS. Her bruger vi det til at sende notifikationer.

:::note
Hvis du bruger OpenAGt desktop-appen, kan den sende systemmeddelelser automatisk, n氓r et svar er klar, eller n氓r en session fejler.
:::

---

### .env-beskyttelse

Undg氓 opencode i at l忙se `.env` filer:

```javascript title=".opencode/plugins/env-protection.js"
export const EnvProtection = async ({ project, client, $, directory, worktree }) => {
  return {
    "tool.execute.before": async (input, output) => {
      if (input.tool === "read" && output.args.filePath.includes(".env")) {
        throw new Error("Do not read .env files")
      }
    },
  }
}
```

---

### Injicer milj酶variabler

Injicer milj酶variabler i al shell-udf酶relse (AI-v忙rkt酶jer og brugerterminaler):

```javascript title=".opencode/plugins/inject-env.js"
export const InjectEnvPlugin = async () => {
  return {
    "shell.env": async (input, output) => {
      output.env.MY_API_KEY = "secret"
      output.env.PROJECT_ROOT = input.cwd
    },
  }
}
```

---

### Brugerdefinerede v忙rkt酶jer

Plugins kan ogs氓 tilf酶je brugerdefinerede v忙rkt酶jer til opencode:

```ts title=".opencode/plugins/custom-tools.ts"
import { type Plugin, tool } from "@openagt/plugin"

export const CustomToolsPlugin: Plugin = async (ctx) => {
  return {
    tool: {
      mytool: tool({
        description: "This is a custom tool",
        args: {
          foo: tool.schema.string(),
        },
        async execute(args, context) {
          const { directory, worktree } = context
          return `Hello ${args.foo} from ${directory} (worktree: ${worktree})`
        },
      }),
    },
  }
}
```

`tool`-hj忙lperen opretter et brugerdefineret v忙rkt酶j, som opencode kan kalde. Det tager en Zod-skemafunktion og returnerer en v忙rkt酶jsdefinition med:

- `description`: Hvad v忙rkt酶jet g酶r
- `args`: Zod-skema for v忙rkt酶jets argumenter
- `execute`: Funktion, der k酶rer, n氓r v忙rkt酶jet kaldes

Dine tilpassede v忙rkt酶jer vil v忙re tilg忙ngelige for opencode sammen med indbyggede v忙rkt酶jer.

:::note
Hvis et plugin-v忙rkt酶j bruger samme navn som et indbygget v忙rkt酶j, har plugin-v忙rkt酶jet forrang.
:::

---

### Logning

Brug `client.app.log()` i stedet for `console.log` til struktureret logning:

```ts title=".opencode/plugins/my-plugin.ts"
export const MyPlugin = async ({ client }) => {
  await client.app.log({
    body: {
      service: "my-plugin",
      level: "info",
      message: "Plugin initialized",
      extra: { foo: "bar" },
    },
  })
}
```

Niveauer: `debug`, `info`, `warn`, `error`. Se [SDK documentation](https://github.com/Yecyi/OpenAGt/docs/sdk) for detaljer.

---

### Komprimerings-hooks

Tilpas konteksten inkluderet, n氓r en session komprimeres:

```ts title=".opencode/plugins/compaction.ts"
import type { Plugin } from "@openagt/plugin"

export const CompactionPlugin: Plugin = async (ctx) => {
  return {
    "experimental.session.compacting": async (input, output) => {
      // Inject additional context into the compaction prompt
      output.context.push(`
## Custom Context

Include any state that should persist across compaction:
- Current task status
- Important decisions made
- Files being actively worked on
`)
    },
  }
}
```

`experimental.session.compacting` krogen affyres, f酶r LLM genererer en forts忙ttelsesoversigt. Brug den til at injicere dom忙nespecifik kontekst, som standardkomprimeringsprompten ville g氓 glip af.

Du kan ogs氓 erstatte komprimeringsprompten helt ved at indstille `output.prompt`:

```ts title=".opencode/plugins/custom-compaction.ts"
import type { Plugin } from "@openagt/plugin"

export const CustomCompactionPlugin: Plugin = async (ctx) => {
  return {
    "experimental.session.compacting": async (input, output) => {
      // Replace the entire compaction prompt
      output.prompt = `
You are generating a continuation prompt for a multi-agent swarm session.

Summarize:
1. The current task and its status
2. Which files are being modified and by whom
3. Any blockers or dependencies between agents
4. The next steps to complete the work

Format as a structured prompt that a new agent can use to resume work.
`
    },
  }
}
```

N氓r `output.prompt` er indstillet, erstatter den standardkompressionsprompten fuldst忙ndigt. `output.context`-arrayet ignoreres i dette tilf忙lde.
