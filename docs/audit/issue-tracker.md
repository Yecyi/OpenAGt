# Analysis-doc finding tracker

This file is the index of every actionable finding raised in a `docs/` analysis report or session transcript. New analyses append rows here instead of creating a sixth, seventh, eighth standalone analysis doc that re-discovers the same debts.

## How to use

- **Reading**: each row tells you a finding, where it lives, and whether it's been closed.
- **Adding a finding**: append a row in the matching analysis section below. Use a stable ID like `apr.4.5` (April §4.5) or `may.A1` (May session, A1 step).
- **Closing**: change the status to `closed`, fill in the closing commit (short sha + subject). Keep the row - don't delete it.
- **Wontfix**: change the status to `wontfix`, give a one-sentence reason in the Notes column.

## Legend

- `open` - finding stands; no fix in tree
- `closed` - fix is merged
- `wontfix` - explicit decision not to fix; reason in Notes

---

## Source: docs/OPENAGT_DEEP_ANALYSIS_AND_VNEXT_GUIDE_2026-04.md (2026-04-30)

| ID      | Finding                                                                                                                                                   | File:line at time of finding                                                                                              | Status  | Closing commit / Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| ------- | --------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------- | ------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| apr.4.1 | `environmentMemo` uses fixed key `"environment"` and date-only hash, so multi-model / multi-workspace processes share the cached static prompt block      | `packages/openagt/src/session/system.ts:230-263`                                                                          | closed  | `9bbfbf49f Wave 11 Step A1` - memoKey now partitions by `${providerID}:${api.id}:${directory}:${worktree}:${vcs}:${platform}`                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| apr.4.2 | Bus event persistence writes to global `events.jsonl` without instance/workspace partitioning                                                             | `packages/openagt/src/bus/index.ts`                                                                                       | open    | Not addressed in Wave 11. Phase B Stream 3 candidate.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| apr.4.3 | Bus persistence whitelist has `"tools.changed"` but MCP emits `"mcp.tools.changed"`; events silently dropped                                              | `packages/openagt/src/bus/index.ts:21` vs `packages/openagt/src/mcp/events.ts:9`                                          | closed  | `68744cef2 Wave 11 Step A2` - whitelist renamed; `isCriticalEventType` exported and pinned via test/bus/critical-events.test.ts                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| apr.4.4 | `dangerous-command-detector` is defined but not in the bash execution main path; verdicts diverge between intended and actual safety                      | `packages/openagt/src/security/dangerous-command-detector.ts`, `tool/bash-execution-plan.ts`, `tool/bash.ts`              | open    | Larger refactor; Phase B Stream 3 candidate per the 2026-05-02 plan.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| apr.4.5 | `computeBackoff()` defined in fallback-service.ts but never called - chain rotates at full speed under 429 storm                                          | `packages/openagt/src/provider/fallback-service.ts:47` (definition; no callers)                                           | closed  | `dbd5027dc Wave 11 Step A3` - wired into `next()` when `state.attempts > 0`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| apr.4.6 | Sandbox `writable_paths` advisory; `maxOutputBytes` defined but not enforced; cross-instance resource accounting global                                   | `packages/openagt/src/sandbox/policy.ts`, `packages/openagt/src/sandbox/process-sandbox.ts`                               | partial | 2026-05-06 - `maxOutputBytes` enforcement closed: introduced `SharedBudget` in `sandbox/process-sandbox-output.ts` and threaded it into both `collectStream` calls via `process-sandbox.ts:138-139`; `outputBytesTruncated` now reflects real combined cap instead of being dead. Regression test in `test/sandbox/process-sandbox.test.ts` ("combined output cap (apr.4.6 regression)"). 2026-05-16 audit confirmed earlier milestone wording over-claimed B3-B6: shared accounting is output-only, not generalized to wall-time/child-count, and cross-instance accounting remains open. |
| apr.4.7 | `canRun()` calls `list()` per invocation - sweep of K ready tasks costs O(K\*N) storage reads                                                             | `packages/openagt/src/session/task-runtime.ts:215-246`                                                                    | closed  | `a972cfb63 Wave 11 Step A4` - canRun accepts optional tasks snapshot; dispatch-loop passes pre-fetched allTasks                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| apr.4.8 | Core `packages/openagt` package is too large; `"./*": "./src/*.ts"` export means internal modules are reachable as public API                             | `packages/openagt/package.json`, `packages/openagt/src/effect/app-runtime.ts`                                             | open    | Phase B Stream 4 (boundary trial) per the 2026-05-02 plan.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| apr.5.x | PR CI is Ubuntu-only; focused tests exclude `test/cli/tui`; default build pulls `https://models.dev/api.json` (network); README references stale versions | `.github/workflows/typecheck.yml`, `packages/openagt/script/build.ts`, `packages/openagt/script/generate.ts`, `README.md` | partial | README/version-metadata closed via `4ce5887fa Wave 11 Step A5`. Release build matrix includes Windows, but PR/typecheck CI remains Ubuntu-only and no nightly Windows matrix is pinned here. The `models.dev` generation dependency is still partial unless the checked-in snapshot is the only release input.                                                                                                                                                                                                                                                                             |

---

## Source: 2026-05-02 session analysis (no standalone doc)

The "深度分析这个项目的技术细节，并且点出 What went well, even better if" session of 2026-05-02 added a few items beyond the April set. Recorded here so they survive the conversation.

| ID                         | Finding                                                                                                                                                                          | File:line at time of finding                                                                                                                     | Status | Closing commit / Notes                                                                                                                                                                                                               |
| -------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------ | ------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| may.tdz                    | `personal/service.ts:1-13` documents a structural TDZ trap that any new tool implementation can re-introduce; only defense is a comment                                          | `packages/openagt/src/personal/service.ts:1-13`, `packages/openagt/src/tool/registry.ts:6-18`                                                    | closed | `fd06b23c7 Wave 11 Step A8` - `bun run check:tool-imports` rejects new `personal/personal` / `coordinator/coordinator` value imports from tool/\*.ts                                                                                 |
| may.readme                 | README references v1.16.0 stable + v1.17.0-rc.3 RC; actual stable is v1.20.2, current minor is v1.21.0; `package.json:23` aliases `verify:v1.17` to the v1.16 verifier           | `README.md:68-74,111`, `package.json:23`                                                                                                         | closed | `4ce5887fa Wave 11 Step A5` - README rewritten, `verify:v1.17` deleted, `verify:v1.21` + `script/v1.21-verify.ts` added                                                                                                              |
| may.agents-md              | AGENTS.md says default branch is `dev`; actual default is `main`; anyone following AGENTS.md to compute a diff base produces wrong diff                                          | `AGENTS.md:3-4`                                                                                                                                  | closed | `d12144119 Wave 11 Step A6`                                                                                                                                                                                                          |
| may.opencode-discovery     | `.opencode/{singular,plural}` dual discovery (agent/agents, command/commands, etc.); a user splitting files between forms sees neither                                           | `packages/openagt/src/config/agent.ts:118,135,155`, `command.ts:29`, `expert.ts:75`, `plugin.ts:33`, `tool/registry.ts:167`, `skill/index.ts:23` | closed | `6dab33244 Wave 11 Step A7` - warn-only deprecation via `warnDeprecatedConfigDir`; hard removal v1.23                                                                                                                                |
| may.beta-deps              | Effect 4.0.0-beta.48, Drizzle 1.0.0-beta.19, ai SDK 6.0.168 are all on the critical path; Effect upgrade has unbounded blast radius across every Layer                           | `package.json` workspace catalog                                                                                                                 | open   | Plan recommends a `docs/effect-upgrade-checklist.md` and a fixed monthly window - neither created in Phase A.                                                                                                                        |
| may.flutter-noise          | Flutter placeholder packages shipped in the workspace after the product direction moved to Electron + React, adding install/typecheck noise without a supported release surface. | `packages/openagt_flutter`, `packages/opencode_flutter`, `README.md:24,287`                                                                      | closed | 2026-05-16 - removed the tracked `packages/openagt_flutter` placeholder and the residual `packages/opencode_flutter` directory; active README/architecture docs now direct frontend work toward Electron + React instead of Flutter. |
| may.analysis-proliferation | `docs/` already has 5 standalone analysis docs (`OPENAGT_DEEP_ANALYSIS_AND_VNEXT_GUIDE_2026-04.md` + 4 others); no shared index, so each rediscovers the same debts              | `docs/*.md`                                                                                                                                      | closed | this file. Future analyses must append rows here instead of creating a sixth standalone doc.                                                                                                                                         |

---

## Source: incidental findings during Wave 11 implementation

Findings discovered while doing Phase A that were not in any analysis doc but are real and should be tracked.

| ID                                  | Finding                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | File:line at time of finding                                                                                                                                                                                         | Status | Closing commit / Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| ----------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| inc.kind-migration                  | `personal_memory_note.kind` column added to schema in Wave 5 (`personal/schema.ts:95`, `personal/personal.sql.ts:14`) but no migration ships under `packages/openagt/migration/`. Tests and any deployed instance with an older DB hits `SQLiteError: table personal_memory_note has no column named kind` on memory writes                                                                                                                                                                                 | `packages/openagt/src/personal/personal.ts:119` (the failing call site), `packages/openagt/src/personal/personal.sql.ts:14` (the schema column)                                                                      | closed | `023cfbcea Wave 11 Step A10` - added migration `20260503000000_personal_memory_kind` that ALTER TABLE ADDs `kind text NOT NULL DEFAULT 'belief'` and creates the index. The 12 coordinator-personal.test.ts cases that reproduced the crash now pass.                                                                                                                                                                                                                                                     |
| inc.processor-effect-failures       | `processor-effect.test.ts` fails 2 tests + 1 error with `instance: No context found for instance` from `bus/index.ts:399 publish` during cleanup. Reproduces on `main` independently of Wave 11. Tests time out at exactly 5015 ms each, suggesting a Bus.publish fires from inside a test scope after the surrounding `Instance.provide` context already tore down                                                                                                                                         | `packages/openagt/test/session/processor-effect.test.ts` (the two `mark pending tools as aborted on cleanup` and `complete pending tools` cases), `packages/openagt/src/bus/index.ts:399` (the failing publish path) | closed | 2026-05-06 - root cause was `Effect.ignore` on `Effect.promise(Bus.publish(...))` only catching failures, NOT defects (Bus.publish rejection becomes a defect). Replaced 7 sites of `Effect.ignore` with `Effect.catchCause(log.warn)` in `processor-tool-calls.ts` (×2), `coordinator/task-executor.ts`, `personal/plan-enrichment.ts`, `tool/{read,edit,write}.ts`, `permission/index.ts`. All defects now caught + logged; tool/coordinator lifecycles continue. `processor-effect.test.ts` now 13/13. |
| inc.mpacr-quorum-timeout            | `MPACR skipped critic runtime contract > synthesis enforces quorum before accepting model output` timed out with synthesis stuck in `pending` after critics had settled. The underlying issue was that skipped/failed critic completion relied on async redispatch; synthesis quorum was not re-evaluated deterministically after the last critic settled.                                                                                                                                                  | `packages/openagt/test/agent/mpacr-partial-failure.test.ts:181`, `packages/openagt/src/coordinator/task-executor.ts`                                                                                                 | closed | 2026-05-07 - coordinator task execution now checks dependent MPACR synthesis nodes after critic completion. If all critic dependencies are completed but substantive quorum is unmet, synthesis is marked retryable `partial` with quorum metadata without calling the synthesis prompt. `mpacr-partial-failure.test.ts`, `mpacr-governance.test.ts`, and `mpacr-validation.test.ts` pass and are included in `verify:v1.21`.                                                                             |
| may07.learning-loop-p1              | Phase 1 learning-loop wiring was partial: review/revise verdicts were emitted and calibration/prompt-outcome rows existed, but failed verdict patterns were not converted into expert memory, prompt template history was role-global instead of expert-scoped, and verifier aggregation lacked a pessimistic ground-truth rule.                                                                                                                                                                            | `packages/openagt/src/{coordinator,personal}`, `packages/openagt/test/agent/coordinator-learning-loop.test.ts`                                                                                                       | closed | 2026-05-07 - added SimHash-style failure signatures, review-pattern memory synthesis + reinjection, expert-scoped prompt outcome history, and a pessimistic verifier aggregation primitive. `coordinator-learning-loop.test.ts` is included in `verify:v1.21`.                                                                                                                                                                                                                                            |
| inc.prompt-templates-snapshot-drift | `prompt-templates-snapshot.test.ts` fails ~7/11 byte-equal assertions for reviser/reviewer/planner/verifier/reducer template variants. The tests assert byte-equality against the OLD inlined coordinator.ts:600-799 prompt strings, but v1.21 externalized those into `coordinator/prompts/*.md` and the externalized templates evolved without snapshot refresh. v1.21.0 release notes claimed "239 passing tests" - either the count was over-claimed at release, or these tests broke after v1.21.0 cut | `packages/openagt/test/agent/prompt-templates-snapshot.test.ts:71-200`, `packages/openagt/src/coordinator/prompts/{reviser,reviewer,planner,verifier,reducer}/*.md`                                                  | closed | 2026-05-06 - refreshed the approved prompt-template snapshots to the current externalized template outputs while preserving exact rendered-output coverage.                                                                                                                                                                                                                                                                                                                                               |
| inc.release-verify-crlf-drift       | `bun run release:verify` fails on Windows with `packages/openagt/schema/config.json is out of date`. Root cause is line-ending drift: the committed `schema/config.json` has CRLF (Git autocrlf on Windows) and a fresh `bun run script/schema.ts` writes LF; `release-verify.ts:43` does strict byte-equality comparison without normalization. Same script passes on Linux/macOS where there is no CRLF conversion                                                                                        | `script/release-verify.ts:40-44`, `packages/openagt/schema/config.json` (the CRLF file)                                                                                                                              | closed | 2026-05-06 - `release-verify.ts` now normalizes CRLF/LF before comparing generated schema files, so Windows autocrlf no longer creates false dirty-gate failures.                                                                                                                                                                                                                                                                                                                                         |

---

## Source: 2026-05-06 deep-dive (stability + output-quality round)

| ID                                  | Finding                                                                                                                                                                                                                                                                                                             | File:line at time of finding                                                                                                                                     | Status | Closing commit / Notes                                                                                                                                                                                                                                                                                                                                                                                                                      |
| ----------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| may06.lazy-poison                   | `lazy()` set `loaded=true` BEFORE `fn()`, so a sync throw poisoned the cache to silently return `undefined` on subsequent calls. Manifested as 16/17 misleading `TypeError` failures in `packages/enterprise/test/core/storage.test.ts` instead of the real "No storage adapter configured" cause.                  | `packages/shared/src/util/lazy.ts:7-8`                                                                                                                           | closed | 2026-05-06 - swapped order so `loaded=true` is set only after `value=fn()` succeeds. Enterprise tests now surface the correct "No storage adapter configured" error (still need real S3/R2 creds to actually pass).                                                                                                                                                                                                                         |
| may06.bom-coordinator-intent-test   | `coordinator-intent.test.ts` started with UTF-8 BOM (`EF BB BF`); flagged by oxlint `no-irregular-whitespace`.                                                                                                                                                                                                      | `packages/openagt/test/agent/coordinator-intent.test.ts:1`                                                                                                       | closed | 2026-05-06 - stripped BOM. 18/18 tests still pass.                                                                                                                                                                                                                                                                                                                                                                                          |
| may06.tool-resolution-lint-fp       | `withSafeTaskSlot` `while` loop hit `no-unmodified-loop-condition` false-positive - `activeSafeTasks` IS mutated externally via `safeTaskWaiters` resolver chain.                                                                                                                                                   | `packages/openagt/src/session/prompt/tool-resolution.ts:106`                                                                                                     | closed | 2026-05-06 - added `oxlint-disable-next-line` with reason explaining cross-invocation mutation. oxlint now `0 warnings 0 errors`.                                                                                                                                                                                                                                                                                                           |
| may06.behavior-event-defect-swallow | 7 sites of `Effect.promise(() => Bus.publish(...)).pipe(Effect.ignore)` silently swallowed defects (Bus.publish rejection in `Effect.promise` becomes a defect, NOT a failure; `Effect.ignore` only catches failures). Telemetry events lost without trace; also caused `inc.processor-effect-failures` test crash. | `processor-tool-calls.ts:120-184`, `coordinator/task-executor.ts:78-89`, `personal/plan-enrichment.ts:70`, `tool/{read,edit,write}.ts`, `permission/index.ts:21` | closed | 2026-05-06 - replaced all 7 sites with `Effect.catchCause((cause) => Effect.sync(() => log.warn(..., Cause.pretty(cause))))`. Telemetry losses now visible; lifecycle still continues. Side effect: closes `inc.processor-effect-failures`.                                                                                                                                                                                                 |
| may06.task-outcomes                 | Task terminal results were only stored on the mutable TaskRuntime record. Retries could overwrite visible result fields, and `task_get` had no durable attempt chain for completed/partial/failed/cancelled outcomes.                                                                                               | `packages/openagt/src/session/task-runtime-write-ops.ts`, `packages/openagt/src/tool/task_get.ts`                                                                | closed | 2026-05-06 - added `task_outcome` migration/table, records terminal outcomes from TaskRuntime complete/partial/fail/cancel, links retry attempts with `previous_outcome_id`, and makes `task_get` prefer the latest persisted outcome. Regression coverage in `task-runtime-agentic.test.ts` and `task.test.ts`.                                                                                                                            |
| may06.oauth-stale-cache             | `provider.ts:getLanguage` cached `LanguageModelV3` by `${providerID}/${modelID}` and never checked OAuth `expires`. Once a token expired the cached SDK kept hitting 401 -> fallback to weaker model - silent quality drop.                                                                                         | `packages/openagt/src/provider/provider.ts:193`, `packages/openagt/src/auth/index.ts:18`                                                                         | closed | 2026-05-06 - added expired OAuth model-cache invalidation via `modelExpiry: Map<string, number>` in `ProviderState`; cache hit checks `expiresAtMs <= Date.now()` and invalidates stale SDK instances. 2026-05-06 follow-up adds an optional provider auth `refresh` hook and Codex refresh-token exchange before rebuilding expired OAuth language models; providers without a refresh hook still fall back to invalidation-only behavior. |
| may06.fallback-double-counting      | `fallback-service.ts` had three independent `if`s for "rate limit" / "too many requests" / "overloaded"; a single error message with multiple keywords double-incremented `fallbackByReason["rate_limit"]`. Skewed observability; alerting based on these counters was wrong.                                       | `packages/openagt/src/provider/fallback-service.ts:270-292`                                                                                                      | closed | 2026-05-06 - collapsed into `if/else if`; "rate limit" + "too many requests" share a branch since they're equivalent in practice.                                                                                                                                                                                                                                                                                                           |
| may06.model-cache-options-shadow    | Provider model cache key was `${providerID}/${modelID}`, omitting merged `provider.options + model.options`. Dynamic header / `reasoningEffort` / `thinking` patches were shadowed by stale cache entry.                                                                                                            | `packages/openagt/src/provider/provider.ts:193`                                                                                                                  | closed | 2026-05-06 - cache key now includes `JSON.stringify(mergedOptions)`. Verified `model.options` is read-only (`sdk-runtime.ts:87` does shallow-copy locally) so the key is stable across calls.                                                                                                                                                                                                                                               |
| may06.oauth-pending-leak            | `provider/auth.ts:callback` on `result.type !== "success"` failed with `OauthCallbackFailed` but never deleted the entry from the `pending` Map. Callback throws/rejections also needed cleanup.                                                                                                                    | `packages/openagt/src/provider/auth.ts:204`                                                                                                                      | closed | 2026-05-06 - wrapped the callback result Effect with `Effect.ensuring(...)` so pending entries are removed on success, callback failure result, throw, or rejection. The missing-code path still preserves pending state so the user can retry entering a code.                                                                                                                                                                             |
| may06.symlink-list-no-boundary      | `tool/read.ts:list` followed symlinks via `fs.stat` to determine if target was a directory, without checking if the target stayed inside `instance.directory` / `instance.worktree`. Defense-in-depth: agent already sees the symlink name; the fix only hides whether the target is a directory.                   | `packages/openagt/src/tool/read.ts:88`                                                                                                                           | closed | 2026-05-06 - added `canonicalPath(fullPath)` + `containsCanonicalPath(...)` check before the stat call. Out-of-boundary symlinks now return as plain file names without target-type leak.                                                                                                                                                                                                                                                   |
| may06.synthesize-once-toctou        | `personal/memory-ops.ts:synthesizeOnce` did `hasMemoryTag -> synthesize` across separate Effect yields. Concurrent calls with the same tag both passed the existence check and double-inserted. Polluted personal memory with conflicting beliefs.                                                                  | `packages/openagt/src/personal/memory-ops.ts:304-316`                                                                                                            | closed | 2026-05-06 - added `personal_memory_idempotency` with primary-key tag claims, migration backfill from existing note tags, and `INSERT ... ON CONFLICT DO NOTHING` before note writes. Regression covers concurrent calls and preclaimed DB tags.                                                                                                                                                                                            |
| may06.edit-lsp-feedback             | Edit results only appended human-readable LSP diagnostics, so agents/UI could not reliably compare before/after diagnostics, detect regressions, or drive later repair loops from stable metadata.                                                                                                                  | `packages/openagt/src/tool/edit.ts`, `packages/openagt/src/lsp/feedback.ts`                                                                                      | closed | 2026-05-06 - added structured `lsp_feedback` metadata with before/after counts, deltas, trend, new/fixed errors, workspace counts, and report text. Regression coverage in `test/lsp/feedback.test.ts` and existing edit tests.                                                                                                                                                                                                             |
| may06.enterprise-tests-need-creds   | All 16 of `packages/enterprise/test/core/{storage,share}.test.ts` are integration tests that require `OPENCODE_STORAGE_ADAPTER` + S3/R2 creds. No skip path; default test run shows 16/17 fails.                                                                                                                    | `packages/enterprise/test/core/storage.test.ts`, `packages/enterprise/test/core/share.test.ts`                                                                   | open   | Discovered via `may06.lazy-poison`. Out of scope this round (test-design issue, not a code bug). Future fix: gate with `test.if(process.env.OPENCODE_STORAGE_ADAPTER)` or move to a separate integration test runner.                                                                                                                                                                                                                       |
| may06.flutter-noise-stale           | Tracker row `may.flutter-noise` was stale after the frontend direction changed and Flutter package removal became the chosen fix.                                                                                                                                                                                   | `packages/openagt_flutter/`, `may.flutter-noise` row above                                                                                                       | closed | 2026-05-16 - superseded by the closed `may.flutter-noise` row: Flutter workspace placeholders were removed instead of archived or deferred.                                                                                                                                                                                                                                                                                                 |

---

## Source: 2026-05-16 runtime foundation closure

| ID                            | Finding                                                                                                                                                                           | File:line at time of finding                                                                                                                | Status | Closing commit / Notes                                                                                                                                                                                                                                                     |
| ----------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------- | ------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| may16.personal-sql-claim      | `docs/hardening-status.md` still marked personal memory SQL pushdown and wakeup DB claim as deferred after the implementation landed.                                             | `docs/hardening-status.md`, `packages/openagt/src/personal/{memory-ops,wakeup-ops}.ts`                                                      | closed | 2026-05-16 - memory list/search now push core filters to SQL, memory/inbox/wakeup indexes ship in migration `20260516123000_runtime_foundation_indexes`, and due wakeups are claimed and fired in an immediate DB transaction with concurrent regression coverage.         |
| may16.event-snapshot-gate     | Event snapshot support, snapshot+tail replay tests, storage diagnostics, and release-verify contract existed but the tracker still described snapshots as deferred.               | `docs/hardening-status.md`, `packages/openagt/src/sync/index.ts`, `packages/openagt/src/cli/cmd/db.ts`                                      | closed | 2026-05-16 - `event_snapshot` schema/migration, snapshot adapter APIs, snapshot+tail replay tests, durability checkpoints, and `db status` storage diagnostics are now tracked as implemented and pinned by release verify.                                                |
| may16.lsp-repair-hook         | Edit/write LSP feedback exposed diagnostics metadata but did not enqueue a bounded repair hook, so agents could miss the intended one-shot retry path.                            | `packages/openagt/src/tool/{edit,write}.ts`, `packages/openagt/src/session/prompt/tool-resolver.ts`, `packages/openagt/src/lsp/feedback.ts` | closed | 2026-05-16 - edit/write compute bounded `lsp_repair` plans with attempt and changed-range checks; the prompt tool resolver queues an LSP repair reminder only for retryable in-workspace diagnostics under the cap. Full LSP code-action application remains future work.  |
| may16.runtime-foundation-gate | A single full `bun test --timeout 30000` can exceed the outer command timeout without actionable failure output, leaving release validation dependent on ad hoc focused commands. | `package.json`, `script/verify-runtime-foundation.ts`, `script/release-verify.ts`                                                           | closed | 2026-05-16 - added `bun run verify:runtime-foundation` as a stable grouped matrix for personal/memory, storage/sync, edit/LSP/learning-loop, sandbox/security/shell, and provider/session suites; release verify now runs the grouped gate and checks tracker consistency. |

---

## Releasing this file

When opening a release notes PR, scan this file for `closed` rows whose closing commit is in the release range and surface the corresponding April / May reference in the release notes' "Hazards / debts closed" section. Don't restate the finding; just link to this index by ID (e.g. `apr.4.1`).
